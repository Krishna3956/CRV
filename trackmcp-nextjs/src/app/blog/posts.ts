import type { ArtKey } from "./art";

export type Block =
  | { t: "p"; c: string }
  | { t: "h2"; c: string }
  | { t: "ul"; c: string[] }
  | { t: "code"; c: string }
  | { t: "quote"; c: string }
  | { t: "callout"; title: string; c: string }
  | { t: "faq"; q: string; a: string }
  | { t: "links"; items: { label: string; href: string }[] }
  | { t: "figure"; art: ArtKey; c: string };

export type Post = {
  slug: string;
  title: string;
  tag: string;
  section?: "releases";
  excerpt: string;
  date: string;
  read: string;
  updated?: string;
  verified?: string;
  keywords?: string[];
  related?: string[];
  body: Block[];
};

const p = (c: string): Block => ({ t: "p", c });
const h = (c: string): Block => ({ t: "h2", c });
const ul = (c: string[]): Block => ({ t: "ul", c });
const code = (c: string): Block => ({ t: "code", c });
const quote = (c: string): Block => ({ t: "quote", c });
const faq = (q: string, a: string): Block => ({ t: "faq", q, a });
const links = (items: { label: string; href: string }[]): Block => ({ t: "links", items });
const callout = (title: string, c: string): Block => ({ t: "callout", title, c });

export const posts: Post[] = [
  {
    slug: "claude-max-5x-vs-20x-for-mcp-development",
    title: "Claude Max 5x vs 20x for MCP Development: What Actually Changes?",
    tag: "MCP development",
    excerpt:
      "A practical explanation of Claude Max 5x and 20x for developers building or testing MCP workflows, including usage windows, limits, and what server observability can measure.",
    date: "Sep 12, 2026",
    read: "9 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "Claude Max 5x vs 20x",
      "Claude Max for MCP development",
      "MCP development",
      "MCP server testing",
      "MCP observability",
      "MCP server monitoring",
      "Claude usage limits",
      "MCP telemetry",
    ],
    related: [
      "mcp-server-analytics-guide",
      "mcp-tool-selection-evaluation",
      "mcp-production-readiness-checklist",
      "mcp-server-slos",
      "how-to-monitor-an-mcp-server-in-production",
    ],
    body: [
      p("If you are comparing Claude Max 5x and Max 20x while building MCP servers, the important difference is usage headroom, not a different MCP protocol or a different server runtime. Anthropic describes Max 5x as $100 per month and Max 20x as $200 per month, with each plan named for its relative usage allowance compared with Pro. Prices and limits can change, so read the current official plan documentation before subscribing."),
      callout("Quick answer", "Choose 5x when your development and testing sessions fit comfortably inside the available usage windows. Consider 20x when repeated long sessions, large codebases, or frequent MCP experiments regularly interrupt your work. The higher tier does not remove the need to test the server boundary, measure tool behavior, or control credentials."),
      p("This guide is about the practical decision for MCP developers. It is not a claim that TrackMCP can see Claude account usage, private model reasoning, or provider-side quota decisions. TrackMCP observes the MCP server boundary after a server is instrumented."),
      h("What the 5x and 20x labels mean"),
      p("The labels describe usage relative to Claude Pro. Anthropic's documentation says Max 5x provides five times the usage per session compared with Pro and Max 20x provides twenty times the usage per session compared with Pro. Both plans use five-hour session windows, and Anthropic also documents a separate weekly limit. A session allowance is therefore not the same thing as unlimited usage or a guaranteed four-times increase in every real-world workflow."),
      p("The practical capacity you experience depends on the model, message length, files, codebase size, tools used, and the limits Anthropic applies to the account. A short question and a long agentic coding session consume very different amounts of context and generation. Treat the published multiplier as a plan-level guide, not as a benchmark for your MCP server."),
      h("When 5x is usually enough"),
      ul([
        "You are learning MCP, building a small server, or iterating on a limited set of tools.",
        "Your test prompts are short and your sessions rarely reach a usage reset.",
        "You can run deterministic protocol checks separately from your interactive Claude sessions.",
        "Your main constraint is server correctness, schema clarity, or authentication rather than model usage headroom.",
      ]),
      p("For this stage, the highest-return work is usually a reliable test fixture: initialize the server, list tools, call representative read and write tools, exercise invalid arguments, and record the expected result. A larger subscription does not make an ambiguous tool schema or an unsafe permission boundary easier to debug."),
      h("When 20x may be worth considering"),
      ul([
        "You routinely work through long repository-level sessions and hit the smaller session allowance before the task is complete.",
        "You are testing many MCP tools, clients, or schema variants in one workday.",
        "You have a repeatable development workflow and can identify usage interruption as the bottleneck.",
        "The additional subscription cost is justified by the value of fewer context resets for your work.",
      ]),
      p("The right evidence is your own usage pattern. Keep a short record for a week: which tasks hit a session limit, how long the task was, whether a new session fixed it, and whether the same issue was actually a server failure. This prevents a plan upgrade from masking a slow tool, a retry loop, or a broken workflow."),
      h("How MCP server observability fits into the decision"),
      p("Claude usage limits live on the provider side. A server-boundary observability layer sees a different set of facts: which client connected, what the server exposed, which tools were called, how long the observed calls took, what result or error came back, and whether the application emitted an explicit workflow outcome. These signals help you distinguish a provider usage interruption from a server-side problem, but they do not expose the private model context that led to the call."),
      ul([
        "Measure tool latency and errors so a slow server is not mistaken for a Claude usage problem.",
        "Track repeated calls and sessions when the server has enough evidence to group them.",
        "Compare behavior across clients, environments, tools, schema versions, and representative workflows.",
        "Use explicit completion signals when you need evidence that a workflow finished, rather than assuming a successful tool response was the final answer.",
      ]),
      h("A better way to compare development capacity"),
      p("Instead of asking only whether 5x or 20x is better, separate the decision into three layers. First, measure provider-side usage interruptions in the Claude product. Second, measure MCP server behavior at the protocol boundary. Third, measure the development outcome you care about, such as a passing test fixture, a completed integration, or a reliable production workflow. Each layer has a different owner and a different source of truth."),
      h("What TrackMCP can and cannot tell you"),
      p("TrackMCP can help a team understand its MCP server after the TypeScript or Python SDK wraps the existing server at the server boundary. It can provide server-side context for clients, catalog and tool usage, observed latency, errors, sessions, and explicit outcomes, with bounded and redacted telemetry. It cannot tell you how much of a Claude account's private allowance remains, inspect private reasoning, or prove why a model selected a tool."),
      p("That boundary is useful. If a Claude session stops after a tool call, the server trace can show whether the server returned an error, whether the call was slow, whether the same tool was retried, and whether an explicit outcome was emitted. The trace cannot identify a provider quota decision unless the client or application explicitly reports that event."),
      h("A practical MCP development checklist"),
      ul([
        "Write a small fixture for initialize, tools/list, representative tools/call requests, invalid arguments, and authorization failures.",
        "Capture the client, server, protocol, schema, and test version for every meaningful run.",
        "Instrument the server boundary with redaction and bounded payload controls before testing real data.",
        "Separate provider usage interruptions from server errors, network failures, and tool-level application errors.",
        "Record an explicit workflow outcome when you need to distinguish a successful tool response from completed user work.",
        "Review the current Anthropic plan limits instead of treating a published multiplier as a permanent guarantee.",
      ]),
      faq("Does Max 20x mean four times more usage than Max 5x?", "Not as a universal guarantee. Anthropic describes the plans as five-times and twenty-times usage per session compared with Pro, but also documents session and weekly limits. The real capacity depends on the models, context, files, and workload in your sessions."),
      faq("Can TrackMCP measure my Claude Max usage?", "No. TrackMCP observes the MCP server boundary. It can measure server-side calls, latency, errors, sessions, and explicit workflow outcomes after instrumentation, but it does not see provider-side account quotas or private model reasoning."),
      faq("Which plan should I choose for MCP development?", "Start with the lower tier if your sessions fit and use a repeatable test workflow. Consider the higher tier when your measured bottleneck is repeated session interruption during legitimate development work. Validate the current plan terms before deciding."),
      links([
        { label: "Read Anthropic's current Max plan documentation", href: "https://support.claude.com/en/articles/11049741-what-is-the-max-plan" },
        { label: "Explore MCP observability", href: "/mcp-observability" },
        { label: "Find free MCP server links", href: "/free-mcp-servers" },
        { label: "Browse social media MCP servers", href: "/mcp-servers/social-media" },
        { label: "Read the MCP server analytics guide", href: "/blog/mcp-server-analytics-guide" },
        { label: "Run the MCP server quickstart", href: "/mcp-server-analytics/quickstart" },
        { label: "Read the production monitoring guide", href: "/blog/how-to-monitor-an-mcp-server-in-production" },
      ]),
    ],
  },
  {
    slug: "mcp-tool-naming-conventions",
    title: "MCP Tool Naming Conventions: Names, Titles, and Collisions",
    tag: "MCP fundamentals",
    excerpt:
      "A technical guide to MCP tool names, human-readable titles, collisions, namespaces, and the naming checks that make a large tool catalog easier for clients and agents to use.",
    date: "Sep 12, 2026",
    read: "8 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP tool naming conventions",
      "MCP tool names",
      "MCP tool title",
      "MCP tool name collision",
      "MCP server tool catalog",
    ],
    related: [
      "mcp-tool-schemas",
      "writing-tool-descriptions-agents-use",
      "mcp-tool-selection-evaluation",
      "mcp-tools-resources-prompts",
    ],
    body: [
      p("An MCP tool name is a protocol identifier, not a sentence. It is the stable handle a client uses when it lists a server's tools and sends a tools/call request. A title can make that tool readable in a user interface, while the name must remain predictable for software. Treating the two fields as the same thing creates avoidable compatibility and selection problems."),
      callout("Short answer", "Use a stable, unique, ASCII-friendly name for the protocol contract, then use title and description for the human explanation. Do not put important meaning only in a display label."),
      h("What the MCP specification says about tool names"),
      p("The current MCP tools specification says that names should be one to 128 characters, should be treated as case-sensitive, and should use letters, digits, underscores, hyphens, and dots. Names should not contain spaces or commas and should be unique within a server. These are interoperability guidelines, so a server should validate its own catalog before publishing it."),
      ul([
        "Use lowercase names when you do not have a strong reason to preserve case. Consistency lowers the chance of client-side lookup mistakes.",
        "Choose one separator convention, such as snake_case or dotted namespaces, and apply it to the complete catalog.",
        "Avoid names that differ only by case, punctuation, or an abbreviation that a model will not recognize.",
        "Keep a name stable after release. If the behavior changes materially, add a new versioned name and document the migration.",
      ]),
      h("Name, title, and description have different jobs"),
      p("The name is for machines. The optional title is for display. The description explains when the tool is useful, what it accepts, what it changes, and what it returns. A name such as search_repositories does not tell a client whether the search is fuzzy, scoped to one organization, or read-only. That meaning belongs in the description and input schema."),
      code(`{
  "name": "search_repositories",
  "title": "Search repositories",
  "description": "Find repositories visible to the authenticated user. Read-only. Use the organization filter when the account has more than one organization.",
  "inputSchema": {
    "type": "object",
    "properties": { "query": { "type": "string" } },
    "required": ["query"]
  }
}`),
      h("Why collisions appear in real deployments"),
      p("A server can avoid duplicate names inside its own list and still encounter collisions when a host combines tools from several servers. The host owns the combined catalog, not the individual server. A client may add its own namespace, but a server should not assume that it will happen or depend on a particular host's display convention."),
      p("The practical response is to make names specific without making them unreadable. A domain prefix can help when a server exposes several subsystems, such as billing_list_invoices and billing_get_invoice. A prefix is less useful when it repeats the server name that the host already shows beside every tool. Review the final catalog in the clients your users actually run."),
      h("A catalog review that catches most naming defects"),
      ul([
        "List every name and check for duplicates, case-only differences, whitespace, punctuation, and accidental truncation.",
        "Read the names without the descriptions. If two tools appear interchangeable, make their scope or action more explicit.",
        "Check the title and description for alignment with the actual handler, authorization rule, side effects, and result shape.",
        "Call each tool with valid and invalid arguments. A clean name does not compensate for a misleading schema or an opaque error.",
        "Record the catalog version or deployment identifier so a change in selection can be compared with a known release.",
      ]),
      h("What to measure after a rename"),
      p("A rename changes more than a label. It can change model selection, client caches, saved prompts, allowlists, and the continuity of your analytics. If you must rename a tool, keep the old name during a deprecation window when that is safe, or publish a clear migration. Track calls by the stable name and keep the release or deployment identifier alongside them."),
      p("TrackMCP measures the server boundary: which tools are listed or called, observed duration, errors, client context, and explicit workflow signals when they are emitted. It cannot tell you every tool a host considered and rejected, so a drop in calls is evidence of changed server-boundary behavior, not a complete explanation of the model's decision."),
      h("A naming convention for a team"),
      ul([
        "Use verb_noun for actions, such as create_ticket or list_repositories.",
        "Use a domain prefix only when it prevents real ambiguity in the combined catalog.",
        "Reserve suffixes such as _preview or _v2 for behavior that is intentionally different and documented.",
        "Never encode secrets, tenant identifiers, or user data in a tool name.",
      ]),
      faq("Are MCP tool names case-sensitive?", "The current specification says tool names should be considered case-sensitive. Treat case as significant even if one client normalizes it in its user interface."),
      faq("Can a tool name contain spaces?", "The current naming guidance says names should not contain spaces. Use a stable identifier for name and put the readable phrase in title and description."),
      faq("Does a unique tool name prevent all collisions?", "No. It prevents duplicates within one server. A host that combines multiple servers may still need its own display or routing convention."),
      links([
        { label: "MCP tool names and tool definitions", href: "https://modelcontextprotocol.io/specification/2025-11-25/server/tools" },
        { label: "MCP tool schemas", href: "/blog/mcp-tool-schemas" },
        { label: "Writing tool descriptions agents use", href: "/blog/writing-tool-descriptions-agents-use" },
        { label: "Measure MCP tool usage with TrackMCP", href: "/mcp-tool-analytics" },
      ]),
    ],
  },
  {
    slug: "mcp-retry-safety-idempotency",
    title: "MCP Retry Safety: Idempotency, Duplicate Calls, and Side Effects",
    tag: "MCP reliability",
    excerpt:
      "Learn how to reason about retries in MCP tool calls, use idempotency deliberately, protect state-changing actions, and tell a safe retry from a dangerous duplicate operation.",
    date: "Sep 12, 2026",
    read: "9 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP retry safety",
      "MCP idempotency",
      "MCP duplicate tool calls",
      "MCP tool side effects",
      "MCP server reliability",
    ],
    related: [
      "mcp-incident-response-runbook",
      "mcp-server-slos",
      "how-to-cut-agent-retries",
      "mcp-tool-schemas",
    ],
    body: [
      p("Retries are normal in distributed systems, and MCP servers sit inside a distributed system. A client can retry after a timeout even when the server completed the operation. An agent can call again after receiving an error it did not understand. A proxy or job runner can replay a request. The danger is not the existence of retries. The danger is allowing a duplicate state-changing call to look like a harmless read."),
      callout("Short answer", "Make read operations safe to repeat, give state-changing operations an idempotency strategy, and record enough server-boundary evidence to distinguish a retry from a new workflow step."),
      h("Idempotent does not mean successful"),
      p("An idempotent operation can be applied more than once with the same intended effect. It can still fail, time out, or return an error. Idempotency is a property of repeated execution, not a promise that the first call will finish. For example, deleting an already-deleted record may be safely repeatable but should still report whether the requested state was achieved."),
      p("The MCP specification exposes tool annotations that can describe behavior such as read-only or idempotent, but the specification treats annotations as hints and clients must not blindly trust them. The server remains responsible for enforcing authorization, validating arguments, and making the handler safe for the behavior it advertises."),
      h("Classify tools by side effect"),
      ul([
        "Read-only tools fetch or compute information without changing external state.",
        "Conditionally mutating tools change state only when a precondition or version matches.",
        "Create-style tools can often accept an idempotency key and return the existing result for a duplicate key.",
        "Non-repeatable tools, such as sending an email or charging a payment method, need a stronger boundary than a client retry loop.",
      ]),
      h("Design the idempotency key at the application boundary"),
      p("An idempotency key is useful only if the handler and its downstream operation honor it. Generate or accept a key that represents one intended operation, store the key with the result for a bounded retention period, and return the original result when the same authorized caller repeats it. Do not use a timestamp alone if two requests can arrive in the same time window, and do not make a key global when tenant or user scope is part of the authorization decision."),
      code(`const result = await createInvoice({
  customerId,
  amount,
  idempotencyKey: request.headers["x-idempotency-key"],
});

// The database constraint or downstream API must enforce
// one effect for one authorized key, not just the handler.
return result;`),
      h("Timeouts create the ambiguous result problem"),
      p("Suppose a client sends create_ticket, the server commits the ticket, and the response is lost. The client sees a timeout and retries. If the second call creates another ticket, the protocol was available but the product outcome is wrong. The server needs a way to connect both attempts to one intended operation, or it must make the action safe to query and reconcile before creating again."),
      p("This is also why a retry count is not proof of a failed operation. It is evidence that a caller sent another request. A useful incident investigation combines retry metadata, request timing, the tool's side-effect policy, downstream identifiers, and the final application outcome."),
      h("Return errors that support safe recovery"),
      ul([
        "Tell the client whether the operation may have been applied before the error occurred.",
        "Return a stable error class and a human-readable correction when the input is invalid.",
        "Include a safe operation identifier that can be used for status lookup, not a secret or bearer token.",
        "Do not suggest retrying a non-idempotent operation unless the server has a reconciliation path.",
      ]),
      h("How to test retry behavior"),
      p("Write tests that force a response timeout after the side effect, retry the exact request, send two concurrent requests with the same key, reuse a key with different arguments, and retry after the key retention window. Test authorization scope too. The same idempotency key must not let one user retrieve another user's result."),
      p("At production scale, inspect the rate of repeated calls by tool, client, error class, and deployment. TrackMCP can show observed calls, durations, retry metadata when supplied, and tool-level errors at the server boundary. It does not know whether a client was about to retry, whether a host displayed the error, or whether a downstream payment processor settled a request unless your server emits that outcome."),
      faq("Should every MCP tool be idempotent?", "No. Read operations are often repeatable, but some useful actions have unavoidable side effects. Those tools need explicit confirmation, idempotency or reconciliation, and careful error semantics."),
      faq("Does an MCP idempotentHint guarantee safe retries?", "No. It is a behavioral hint. Clients must treat server-provided annotations as untrusted, and the server must make its implementation match the behavior it advertises."),
      faq("Is a duplicate call always a retry?", "No. It may be a new workflow step, a user request repeated intentionally, or a client recovery attempt. Use timing, request context, retry metadata, and application identifiers rather than call count alone."),
      links([
        { label: "MCP tools and behavioral annotations", href: "https://modelcontextprotocol.io/specification/2025-11-25/server/tools" },
        { label: "MCP incident response runbook", href: "/blog/mcp-incident-response-runbook" },
        { label: "How to cut agent retries", href: "/blog/how-to-cut-agent-retries" },
        { label: "MCP server SLOs", href: "/blog/mcp-server-slos" },
      ]),
    ],
  },
  {
    slug: "mcp-request-timeouts-deadlines",
    title: "MCP Request Timeouts: Deadlines, Cancellation, and Long-Running Work",
    tag: "MCP reliability",
    excerpt:
      "A practical guide to MCP request deadlines, cooperative cancellation, progress, durable tasks, and the telemetry needed to explain slow or abandoned tool calls.",
    date: "Sep 12, 2026",
    read: "9 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP request timeout",
      "MCP cancellation",
      "MCP long running tool",
      "MCP progress notifications",
      "MCP task polling",
    ],
    related: [
      "mcp-progress-notifications",
      "mcp-tasks-extension",
      "mcp-server-slos",
      "latency-that-matters-for-agents",
    ],
    body: [
      p("A timeout is a boundary decision, not a diagnosis. It says that a caller stopped waiting under a particular policy. The server may still be running, may have completed successfully, or may have been cancelled. MCP teams need to model those states separately because a single timeout number can hide dependency slowness, queueing, client impatience, or work that should have been moved to a durable task."),
      callout("Short answer", "Set a deadline for every bounded request, make cancellation cooperative, use progress for active feedback, and use tasks when the work must outlive the original request connection."),
      h("Choose a deadline from the workflow"),
      p("Start with the user or agent workflow, then allocate time across the MCP call and its dependencies. A search tool may need a short deadline. A report-generation tool may need a longer budget but should not hold a connection open while a job runs for minutes. The correct number depends on the operation, client behavior, dependency limits, and the cost of a partial result."),
      ul([
        "Set one server-side maximum so a client cannot create unbounded work.",
        "Propagate a remaining deadline to downstream calls rather than restarting the clock for every dependency.",
        "Reserve time for validation, cleanup, and a useful error response.",
        "Measure the slow tail by tool and outcome. Average latency is not a safe timeout policy.",
      ]),
      h("Cancellation is cooperative"),
      p("MCP supports cancellation notifications. A receiver should stop work when it can, but cancellation does not rewind a side effect that has already happened. A server should check cancellation between expensive stages, pass cancellation to downstream libraries where supported, release resources in a finally path, and make the final state explicit."),
      code(`async function runReport(input, signal) {
  await checkCancelled(signal);
  const rows = await fetchRows(input, { signal });
  await checkCancelled(signal);
  const report = await buildReport(rows, { signal });
  return report;
}`),
      h("Progress tells a client that work is alive"),
      p("A client can include a progress token, and a server can send progress notifications associated with that token. Progress is optional and request-scoped. It does not prove that the host rendered the update, that a user saw it, or that the agent will wait forever. Send meaningful milestones at a bounded rate, and stop sending updates after completion or cancellation."),
      h("When a task is a better fit"),
      p("The tasks extension represents durable state for work that should be polled and retrieved later. The current specification describes statuses such as working, input_required, completed, failed, and cancelled, along with task identifiers, TTLs, and suggested poll intervals. Tasks are currently experimental, so document the client support you require and provide a fallback for clients that do not negotiate it."),
      p("A task does not automatically equal a business success. A completed task means the protocol operation reached its completed state. If the operation creates a deployment, sends a message, or exports a report, emit or store the application outcome that proves the useful result."),
      h("Avoid the timeout and retry trap"),
      ul([
        "Do not retry a state-changing operation simply because the waiting client timed out.",
        "Give long work a status or reconciliation tool so a caller can query the result safely.",
        "Keep a timeout error distinct from a tool execution error and a cancellation result.",
        "Record whether the handler stopped before the side effect, after the side effect, or in an unknown state.",
      ]),
      h("Telemetry for slow MCP calls"),
      p("Capture the tool name, start and end timestamps, outcome class, cancellation or timeout marker, dependency phase when safe, and deployment identifier. Keep payload capture bounded and redacted. A timeline that says a call took 18 seconds is useful; a timeline that also shows it spent 17 seconds waiting on a named dependency is far more actionable, provided the dependency label does not contain sensitive data."),
      p("TrackMCP observes the server boundary and can connect observed latency, tool errors, retries, and explicit workflow signals. It cannot see a client's private deadline policy or model reasoning unless that information is deliberately emitted by a component you control."),
      faq("Does cancelling an MCP request guarantee that the work stopped?", "No. Cancellation is cooperative. The server should attempt to stop work, but an external side effect may already have happened and some dependencies may not support immediate cancellation."),
      faq("Should every long-running MCP tool use tasks?", "No. A bounded operation can use an ordinary request with progress or a clear timeout. Tasks are useful when work must outlive the original request and the client supports the experimental extension."),
      faq("Is a timeout the same as a server error?", "No. A timeout says a caller stopped waiting. The server may have failed, completed, been cancelled, or reached an unknown side-effect state."),
      links([
        { label: "MCP cancellation and transports", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/transports" },
        { label: "MCP tasks and durable request state", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/utilities/tasks" },
        { label: "MCP progress notifications deep dive", href: "/blog/mcp-progress-notifications" },
        { label: "MCP server SLOs", href: "/blog/mcp-server-slos" },
      ]),
    ],
  },
  {
    slug: "mcp-server-auth-discovery-protected-resource-metadata",
    title: "MCP Server Authentication Discovery: 401, WWW-Authenticate, and Metadata",
    tag: "MCP security",
    excerpt:
      "Understand how an HTTP MCP server advertises its authorization server, how clients use Protected Resource Metadata, and how to debug discovery without logging secrets.",
    date: "Sep 12, 2026",
    read: "9 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP authentication discovery",
      "MCP Protected Resource Metadata",
      "MCP WWW-Authenticate",
      "MCP 401 error",
      "MCP OAuth discovery",
    ],
    related: [
      "remote-mcp-oauth-guide",
      "mcp-token-passthrough-security",
      "mcp-authorization-errors",
      "mcp-server-security-checklist",
    ],
    body: [
      p("Remote MCP authentication has two separate questions: how does a client discover which authorization server to use, and how does the MCP server validate the token it receives? Many integration failures happen before a tool call because discovery is incomplete, the resource identifier is wrong, or the client cannot interpret a 401 response."),
      callout("Short answer", "An HTTP MCP server must expose Protected Resource Metadata and clients must use it for discovery. A 401 response can point to that metadata with WWW-Authenticate, while a well-known fallback covers clients that do not receive a usable challenge."),
      h("The protected resource is the MCP server"),
      p("In the MCP authorization model, the remote MCP server is the protected resource. It is not the authorization server that issues tokens. The client asks the resource for the authorization-server location, obtains a token for the MCP resource, and presents that token to the MCP server. Keeping those roles separate prevents a server from treating an upstream access token as if it were automatically valid for every downstream API."),
      h("What a 401 challenge should communicate"),
      p("When the MCP server returns 401 Unauthorized, the WWW-Authenticate header can include a resource_metadata URL. The current authorization specification also describes a scope parameter that can tell the client which scopes are needed for the current request. The header is discovery guidance, not a place for credentials, user data, or a pre-authenticated URL."),
      code(`HTTP/1.1 401 Unauthorized
WWW-Authenticate: Bearer resource_metadata="https://mcp.example.com/.well-known/oauth-protected-resource", scope="files:read"`),
      h("Protected Resource Metadata is a contract"),
      p("The metadata document tells the client which authorization servers protect the MCP resource. The current specification requires an authorization_servers field with at least one authorization server. A server can expose the document at a well-known URL or make its location available through the 401 challenge. Clients are required to support both discovery mechanisms and use the challenge URL when present."),
      ul([
        "Serve the metadata over HTTPS in production.",
        "Use the canonical MCP resource URI consistently in metadata, authorization requests, token requests, and server validation.",
        "Publish only the authorization-server locations and scopes that the client needs to discover the flow.",
        "Test the endpoint without an access token and test the exact path used by the MCP endpoint.",
      ]),
      h("Why resource indicators matter"),
      p("The authorization specification requires clients to include a resource parameter in authorization and token requests. That parameter binds a token to the MCP server it is intended for. The server must validate that the presented token was issued for it and must reject tokens intended for another resource."),
      h("Debug discovery in layers"),
      ul([
        "Transport: confirm DNS, TLS, and the MCP endpoint respond at the expected URL.",
        "Challenge: confirm an unauthenticated request returns 401 and a useful WWW-Authenticate header when that mechanism is used.",
        "Metadata: fetch the advertised document and verify authorization_servers and its content type.",
        "Authorization server: verify the issuer metadata and token endpoint according to the supported discovery mechanism.",
        "Resource binding: confirm the token audience or resource matches the MCP server before a tool executes.",
        "Scopes: test missing, valid, and excessive scopes without logging the token value.",
      ]),
      h("What not to put in logs"),
      p("Do not log bearer tokens, authorization codes, refresh tokens, client secrets, or full authorization URLs containing sensitive state. Log the outcome class, issuer host when safe, resource identifier class, scope names if they are non-sensitive, and a stable request correlation identifier. Redact query values before a URL enters an error or telemetry payload."),
      p("TrackMCP can show server-boundary authorization failures and the tool traffic that follows a successful connection. It does not replace an identity provider audit log, and it does not see a private client redirect or the user's consent screen unless your controlled integration emits that event."),
      faq("Is WWW-Authenticate the only discovery mechanism for MCP?", "No. It can point to Protected Resource Metadata after a 401. The current specification also defines well-known URI fallback behavior for clients that do not receive a usable challenge."),
      faq("Does a valid token for another API work for an MCP server?", "Not automatically. The MCP server must validate that the token was issued for the MCP resource. Downstream APIs need their own credential or an explicit, documented exchange."),
      faq("Should stdio MCP servers implement this OAuth discovery flow?", "The MCP basic specification describes this authorization framework for HTTP-based transports. Stdio deployments typically retrieve credentials from the environment instead."),
      links([
        { label: "MCP authorization and Protected Resource Metadata", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization" },
        { label: "Remote MCP OAuth guide", href: "/blog/remote-mcp-oauth-guide" },
        { label: "MCP token passthrough security", href: "/blog/mcp-token-passthrough-security" },
        { label: "MCP server security checklist", href: "/blog/mcp-server-security-checklist" },
      ]),
    ],
  },
  {
    slug: "mcp-tool-catalog-design",
    title: "MCP Tool Catalog Design: Make Large Server Catalogs Easier to Use",
    tag: "MCP design",
    excerpt:
      "A practical design guide for large MCP tool catalogs, covering grouping, descriptions, pagination, change notifications, schemas, permissions, and selection evidence.",
    date: "Sep 12, 2026",
    read: "8 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP tool catalog design",
      "MCP tools list pagination",
      "large MCP server tool list",
      "MCP tool discovery",
      "MCP tool selection",
    ],
    related: [
      "mcp-tool-naming-conventions",
      "mcp-pagination-nextcursor",
      "mcp-tool-list-caching-ttlms-cachescope",
      "mcp-tool-selection-evaluation",
    ],
    body: [
      p("A large MCP catalog is an interface, not an inventory dump. Every extra tool competes for attention, context, and selection. A catalog that is technically complete can still be hard for an agent to use when names overlap, descriptions omit constraints, or the list changes between discovery and execution."),
      callout("Short answer", "Design the catalog around user tasks, keep each tool contract narrow, paginate without exposing cursor internals, invalidate caches when the catalog changes, and measure selection and execution separately."),
      h("Start with the tasks users need to complete"),
      p("Group tools by domain and workflow, then decide whether every action deserves a separate tool. A tool that accepts ten unrelated modes may reduce the number of names but increase schema ambiguity. Several narrow tools may be easier to authorize and explain, while too many near-duplicates can make selection worse. The right boundary is the one that makes intent, permission, and failure behavior clear."),
      ul([
        "Give every tool one primary job and one recognizable result shape.",
        "Put authorization and side-effect information in the description and annotations where appropriate.",
        "Avoid exposing internal implementation details that do not help a client choose the tool.",
        "Keep administrative or destructive tools separate from routine read tools in naming and permission policy.",
      ]),
      h("Pagination is part of the catalog contract"),
      p("MCP list operations can return a nextCursor when more results are available. A cursor is opaque. The client should pass it back unchanged and continue until the server omits it. The server owns page size and cursor semantics. Do not make clients parse an offset, assume a stable page length, or silently discard a partial catalog."),
      code(`let cursor: string | undefined;
do {
  const page = await client.listTools(cursor);
  consume(page.tools);
  cursor = page.nextCursor;
} while (cursor);`),
      p("Production clients should bound the number of pages and detect a repeated cursor. Servers should make a reasonable effort to keep a page walk coherent, but a rapidly changing catalog can still create additions, removals, or duplicates between pages. Document the behavior and include a catalog or deployment identifier in debugging output."),
      h("Cache freshness is not authorization"),
      p("A client may cache a tools/list response to avoid repeated discovery. Freshness and visibility are separate decisions. A catalog that varies by user, tenant, role, or token must not be shared merely because it has a TTL. If the server advertises list-change notifications, clients can invalidate or refresh their view before the nominal TTL expires."),
      h("Descriptions and schemas should agree"),
      p("The description is the selection guidance and the input schema is the validation contract. If the description says a filter is optional but the schema requires it, agents will retry or abandon the tool. If a field accepts an enum but the description uses different labels, selection will be unreliable. Review the rendered catalog, not only the source code."),
      h("Discovery is not execution"),
      p("A tool can be listed, selected, and then rejected by authorization or input validation. It can also be called successfully but fail inside its result with isError true. Measure these stages separately: catalog exposure, selection evidence when available, call attempt, validation outcome, execution outcome, and workflow result."),
      p("TrackMCP observes what reaches the server boundary. It can help you see which tools are called, which clients connect, where calls fail, how latency changes, and whether explicit workflow outcomes improve. It cannot see tools that a host considered but never sent to the server, and it cannot infer a complete model decision from the absence of a call."),
      h("A release checklist for catalog changes"),
      ul([
        "Check names for uniqueness, allowed characters, and stable meaning.",
        "Validate every input and output schema, including empty-input tools.",
        "Test first page, middle page, final page, repeated cursor, and changing-catalog behavior.",
        "Test permissions against a fresh catalog and a stale cached catalog.",
        "Run representative workflows with at least two clients and compare selection, errors, retries, and completion.",
      ]),
      faq("Should an MCP server expose every backend operation as a tool?", "No. Expose the smallest useful set that maps to real workflows and has clear authorization, schemas, and failure behavior."),
      faq("Can clients assume that a tools/list response is complete?", "Only after following nextCursor until it is omitted, and only within the limits of the server's catalog consistency behavior. A partial first page is not a complete catalog."),
      faq("Does a tool call prove that an agent selected the best tool?", "No. It proves that the call reached the server. Evaluation needs a defined intent, expected tool or outcome, and evidence from the host or workflow when that evidence is available."),
      links([
        { label: "MCP tools and tool catalogs", href: "https://modelcontextprotocol.io/specification/2025-11-25/server/tools" },
        { label: "MCP pagination and opaque cursors", href: "/blog/mcp-pagination-nextcursor" },
        { label: "MCP tool list caching", href: "/blog/mcp-tool-list-caching-ttlms-cachescope" },
        { label: "MCP tool selection evaluation", href: "/blog/mcp-tool-selection-evaluation" },
      ]),
    ],
  },
  {
    slug: "mcp-pagination-nextcursor",
    title: "MCP Pagination: How nextCursor Works and How to Implement It Safely",
    tag: "MCP protocol deep dive",
    excerpt:
      "A production guide to MCP cursor pagination, including nextCursor handling, empty cursors, invalid cursors, repeated pages, client limits, and cache consistency.",
    date: "Sep 12, 2026",
    read: "14 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP pagination",
      "MCP nextCursor",
      "MCP tools/list pagination",
      "MCP cursor pagination",
      "MCP invalid cursor",
      "MCP pagination example",
      "MCP server monitoring",
    ],
    related: [
      "mcp-tool-schemas",
      "mcp-2026-07-28-migration-guide",
      "mcp-server-analytics-guide",
      "how-to-monitor-an-mcp-server-in-production",
    ],
    body: [
      p("MCP pagination is a cursor-based protocol utility for list operations that may return more data than one response should carry. A client requests a list such as tools/list, resources/list, resources/templates/list, or prompts/list. The server returns the current page and may include nextCursor. If nextCursor is present, the client sends another list request with that exact value in cursor. The client continues until the server omits nextCursor."),
      callout("Short answer", "Treat nextCursor as an opaque continuation token. Do not parse it, increment it, compare it to a page number, or replace it with an empty value. Follow it exactly, bound the loop, and make a repeated cursor a visible failure rather than an infinite request loop."),
      p("This article is verified against the MCP 2025-11-25 pagination specification and the 2026-07-28 tools specification on September 12, 2026. The pagination concept is shared across protocol eras, but modern list results also include cache hints and modern clients can use different discovery and transport envelopes."),
      h("What MCP pagination covers"),
      p("MCP defines pagination for four list operations: tools/list, resources/list, resources/templates/list, and prompts/list. The server chooses the page size. The client cannot assume that the first page contains a particular number of tools, that page sizes are stable between requests, or that a cursor encodes an offset."),
      ul([
        "tools/list returns the tools currently available to the requesting client.",
        "resources/list returns concrete resources the server can provide.",
        "resources/templates/list returns URI templates for dynamic resources.",
        "prompts/list returns the prompt templates exposed by the server.",
      ]),
      h("The wire flow"),
      code(`{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/list",
  "params": { "cursor": "opaque-cursor-from-a-previous-page" }
}`),
      p("The response contains the page collection and, when more results are available, nextCursor. The name and encoding of the cursor belong to the server. It might be an encoded database position, a signed continuation token, or a value backed by a short-lived snapshot. A client must not need to know which."),
      code(`{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "resultType": "complete",
    "tools": [{ "name": "search", "description": "Search records", "inputSchema": { "type": "object" } }],
    "nextCursor": "opaque-cursor-for-the-next-page",
    "ttlMs": 300000,
    "cacheScope": "public"
  }
}`),
      h("Why cursors are opaque"),
      p("An offset-based client assumes that the server has a stable ordered array and that a page number has the same meaning later. Production lists often come from a database query, an authorization-filtered catalog, or a changing remote service. An opaque cursor lets the server choose a safe continuation strategy without making its storage design part of the protocol."),
      p("The client should only make one decision from the value: whether the response contains a nextCursor that the server expects to be sent back. It should not decode a base64-looking string, add one to a numeric string, sort cursors, or store one as a durable bookmark across unrelated sessions."),
      h("The empty cursor edge case"),
      p("Older documentation and implementations sometimes treat an empty string as if it meant that pagination is finished. That is unsafe for an opaque protocol value. The robust rule is to distinguish absence from presence. A missing or null nextCursor means there is no next page. If a server emits an empty string as a cursor, the client should preserve it and send it back, unless the implementation has a documented defensive limit and reports the malformed server behavior."),
      h("A bounded client loop"),
      code(`let cursor: string | undefined;
const seen = new Set<string>();
const allTools = [];

for (let pageNumber = 0; pageNumber < 64; pageNumber += 1) {
  const page = await client.listTools(cursor ? { cursor } : undefined);
  allTools.push(...page.tools);

  if (page.nextCursor === undefined) break;
  if (seen.has(page.nextCursor)) {
    throw new Error("MCP server repeated a pagination cursor");
  }
  seen.add(page.nextCursor);
  cursor = page.nextCursor;
}`),
      p("The page cap is a safety boundary, not a protocol rule. Choose it from the largest catalog your client is meant to handle, the response-size budget, and the time available during discovery. A client should return a clear incomplete-catalog error when the limit is reached. Silently presenting only the first 64 pages makes missing tools look like a server configuration problem."),
      h("Server implementation rules"),
      ul([
        "Return a stable order while the underlying collection is unchanged.",
        "Validate cursors and return a JSON-RPC invalid-params error for an unknown, expired, or malformed cursor.",
        "Keep a cursor bound to the query, authorization context, and any snapshot assumptions it depends on.",
        "Never place secrets, raw customer data, or authorization material directly in a cursor unless it is protected and bounded.",
        "Make the final page omit nextCursor instead of inventing a sentinel such as done or null-as-a-string.",
        "Define what happens when the collection changes between page requests. Duplicates and gaps may be unavoidable without a snapshot.",
      ]),
      h("Pagination and changing catalogs"),
      p("A tools/list response is a view of a catalog at a moment in time. If tools are added or removed while a client is walking pages, the client may observe duplicates, gaps, or a cursor that is no longer valid. That is not solved by parsing the cursor. It is solved by a server choosing an appropriate snapshot or by a client detecting that the catalog changed and restarting from the beginning."),
      p("The 2026-07-28 protocol also gives list results ttlMs and cacheScope hints. These fields tell a client how long a response may be considered fresh and whether it may be shared. They do not create a consistent snapshot across pages. Each page has its own freshness and authorization considerations."),
      h("Pagination versus streaming"),
      p("Pagination is client-pulled and discrete. The client decides when to request the next page and can process or discard a page before continuing. Streaming is a delivery mechanism for an ongoing flow. A large tool result may need its own application-level design, but the standard MCP pagination utility does not automatically add nextCursor to arbitrary tools/call results."),
      h("How to debug missing tools"),
      ul([
        "Capture the first tools/list response and check whether nextCursor is present.",
        "Confirm that the client sends the exact cursor in the next request and does not transform it.",
        "Compare the number of requested pages with the number of pages returned.",
        "Check for repeated cursors, invalid-params responses, response-size limits, and client page caps.",
        "Check whether authorization changes between pages and whether the server binds cursors to the correct caller.",
        "Record the protocol revision because a 2026 client may also expect cache hints and modern request metadata.",
      ]),
      h("What TrackMCP can and cannot tell you"),
      p("TrackMCP can help an MCP server team inspect server-boundary discovery events, page counts, observed durations, repeated-cursor findings, and truncation when the instrumented server emits those signals. That evidence can show that a server returned a nextCursor or that a client requested only the first page."),
      p("It cannot prove that a client displayed every tool, that a model considered every tool, or that a client used a cursor correctly after the request left the server. Those are separate host and client behaviors. Keeping that boundary explicit is the difference between useful telemetry and an invented end-to-end claim."),
      faq("Does MCP pagination use page numbers?", "No. MCP uses opaque cursor tokens. The server chooses the cursor format and page size, while the client sends the nextCursor value back as cursor."),
      faq("What does a missing nextCursor mean?", "It means the server did not advertise another page. A client should stop. It should not guess that a page is complete from the number of items returned."),
      faq("Does pagination apply to tools/call results?", "The standard MCP pagination utility applies to list operations. A tool that returns a large dataset can design its own cursor argument and result contract, but that is an application-level tool design unless a future protocol extension standardizes it."),
      links([
        { label: "MCP pagination specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/server/utilities/pagination" },
        { label: "MCP 2026 tools specification", href: "https://modelcontextprotocol.io/specification/2026-07-28/server/tools" },
        { label: "MCP tool schemas", href: "/blog/mcp-tool-schemas" },
        { label: "MCP 2026 migration guide", href: "/blog/mcp-2026-07-28-migration-guide" },
        { label: "MCP server analytics guide", href: "/blog/mcp-server-analytics-guide" },
      ]),
    ],
  },
  {
    slug: "mcp-tool-list-caching-ttlms-cachescope",
    title: "MCP Tool List Caching: ttlMs, cacheScope, and Stale Catalogs Explained",
    tag: "MCP protocol deep dive",
    excerpt:
      "Understand how MCP clients cache tool catalogs, how ttlMs and cacheScope work, how listChanged invalidates stale data, and where authorization boundaries matter.",
    date: "Sep 12, 2026",
    read: "15 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP ttlMs",
      "MCP cacheScope",
      "MCP tools/list caching",
      "MCP stale tools list",
      "MCP listChanged notification",
      "MCP tool cache invalidation",
      "MCP subscriptions/listen",
    ],
    related: [
      "mcp-pagination-nextcursor",
      "mcp-2026-07-28-migration-guide",
      "mcp-tool-schemas",
      "mcp-server-analytics-guide",
    ],
    body: [
      p("MCP tool-list caching exists to reduce repeated discovery round trips without making freshness or privacy an implicit guess. In the 2026-07-28 protocol revision, cacheable list and resource results carry ttlMs and cacheScope. ttlMs is a freshness hint in milliseconds. cacheScope says whether the response is safe to share broadly or must stay within the requesting authorization context."),
      callout("Short answer", "Use ttlMs to describe freshness, cacheScope to describe visibility, and listChanged or subscriptions/listen as an invalidation signal when supported. These fields do not replace authorization, guarantee immutable data, or make a changing multi-page catalog a consistent snapshot."),
      p("This article is verified against the MCP 2026-07-28 documentation and SEP-2549 on September 12, 2026. Older 2025-era servers may omit these fields, and clients must continue to interoperate with that behavior."),
      h("Why MCP needs its own cache hints"),
      p("MCP is transport-agnostic. An HTTP server could use HTTP cache headers, but MCP also works over stdio and other transports where HTTP headers do not exist. Embedding the hint in the result keeps the semantic decision with the protocol message. A client can understand the same freshness policy regardless of how the message traveled."),
      p("The problem is practical. A host may list tools at startup, cache the catalog, and continue using it for many turns. If a server adds a tool, removes a tool, changes a schema, or filters tools differently for a caller, the host needs a way to decide when its copy is stale."),
      h("ttlMs is a freshness hint"),
      ul([
        "ttlMs: 0 means the result should be treated as immediately stale.",
        "A positive ttlMs tells the client how long it may consider the result fresh after receipt.",
        "An absent value in an older response should be treated conservatively, not as an infinite lifetime.",
        "A negative value is invalid and should be treated as zero rather than as a long-lived cache.",
        "TTL is not a promise that the underlying data cannot change before expiry.",
      ]),
      code(`const receivedAt = Date.now();
const freshUntil = receivedAt + result.ttlMs;
const isFresh = Date.now() < freshUntil;`),
      p("The freshness clock begins when the client receives the result. TTL is not automatically a polling interval. A client may re-fetch when it needs the data and discovers that the result is stale. If it chooses to poll, it should use backoff and jitter so a large client population does not create a synchronized discovery spike."),
      h("cacheScope is a privacy boundary"),
      p("cacheScope has two important values. public means the response does not contain user-specific data and may be stored or served by a shared intermediary. private means the response belongs to the requesting authorization context and must not be shared across users or tokens."),
      p("A private tool catalog is common when authorization controls which tools a caller can see. The same endpoint can therefore return different tools to different identities. A shared cache that ignores cacheScope can turn a performance optimization into a cross-user data disclosure. Cache hints are not access control, so the server must still enforce authorization on every request."),
      h("The interaction with listChanged"),
      p("A server can advertise that its tools list changes and send notifications/tools/list_changed when it changes. In the modern 2026-07-28 model, the client opts into notification delivery through subscriptions/listen. When a relevant notification arrives, the cached result becomes stale immediately even if its TTL has not expired. The client can then re-fetch tools/list."),
      code(`{
  "jsonrpc": "2.0",
  "method": "subscriptions/listen",
  "params": {
    "notifications": { "toolsListChanged": true }
  }
}`),
      p("TTL and notification invalidation solve different parts of the problem. TTL gives a client a bounded freshness window when no push signal arrives. A notification gives an early invalidation signal when the server knows the list changed. Either mechanism can exist without the other."),
      h("Caching paginated tool lists"),
      p("Each page of a paginated list is an independently cacheable response. The cursor belongs in the cache key. A client must not reuse page two from one authorization context for another, and it must not assume that a fresh first page makes every later page fresh. If a cursor expires, the safe recovery is to discard the cached pages and restart from the beginning."),
      p("There is no cross-page consistency guarantee. If the catalog changes between requests, a client can see duplicates or gaps. If an application needs a consistent complete catalog, the server must provide an appropriate snapshot strategy or the client must re-fetch from the beginning and validate the resulting catalog version when one is available."),
      h("A server decision table"),
      ul([
        "A static, identical public catalog can use a positive TTL and public scope.",
        "A catalog filtered by user, tenant, role, or token should use private scope.",
        "A rapidly changing catalog should use a short TTL and a reliable change notification path when the transport supports it.",
        "A result containing user-specific resources should not be marked public merely because the endpoint itself is public.",
        "If the server cannot prove that a result is safe to share, private or zero TTL is the safer posture.",
      ]),
      h("Debugging a stale tools list"),
      ul([
        "Record the protocol revision and whether the response contained ttlMs and cacheScope.",
        "Compare the cached result timestamp with the advertised TTL.",
        "Check whether the server sent a list-changed notification and whether the client was subscribed.",
        "Verify that the client invalidated the correct cache key, including authorization context and cursor.",
        "Compare the re-fetched catalog with the response actually received, not with an assumption about what the server should return.",
        "Check whether a client-side SDK automatically aggregates pages and hides the raw page boundaries.",
      ]),
      h("Modern versus legacy behavior"),
      p("The 2025-era protocol used the initialize handshake and older notification delivery patterns. The 2026-07-28 revision removes protocol-level sessions, carries request metadata per request, and introduces subscriptions/listen for change notifications. A server that supports both eras needs tests for both. A client that only knows the older era should ignore unknown cache fields and continue using conservative freshness behavior."),
      h("What TrackMCP can and cannot tell you"),
      p("TrackMCP can help show when a server exposed a catalog, when tools changed at the server boundary, which client identity and protocol metadata were available, and whether later tool calls reached the server. This can reveal patterns such as a server updating its catalog while a client continues calling an older tool set."),
      p("TrackMCP cannot invalidate a host's cache, observe an internal cache hit, or prove that a client used the latest catalog. It also cannot make a public cache safe. The server and client remain responsible for choosing and enforcing the correct authorization context."),
      faq("Does ttlMs guarantee that tools stay unchanged?", "No. ttlMs is a freshness hint. A server may change the underlying data before the TTL expires, and a relevant change notification should invalidate the cached result when one is available."),
      faq("Can an authenticated tools/list response be public?", "It can be public only when the returned catalog is genuinely safe to share across authorization contexts. Authentication on the endpoint alone does not make a result private or public."),
      faq("Is listChanged the same as caching?", "No. listChanged is a signal that the catalog changed. ttlMs describes how long a response may be considered fresh. They can work together, but neither replaces authorization or a bounded refresh strategy."),
      links([
        { label: "MCP caching specification", href: "https://modelcontextprotocol.io/specification/draft/server/utilities/caching" },
        { label: "SEP-2549: TTL for list results", href: "https://modelcontextprotocol.org/seps/2549-TTL-for-list-results" },
        { label: "MCP 2026-07-28 release", href: "https://blog.modelcontextprotocol.io/posts/2026-07-28/" },
        { label: "MCP pagination deep dive", href: "/blog/mcp-pagination-nextcursor" },
        { label: "MCP 2026 migration guide", href: "/blog/mcp-2026-07-28-migration-guide" },
      ]),
    ],
  },
  {
    slug: "mcp-progress-notifications",
    title: "MCP Progress Notifications: progressToken, Client Support, and Long-Running Tools",
    tag: "MCP protocol deep dive",
    excerpt:
      "Learn how MCP progressToken and notifications/progress work, why progress may not appear in a client, and how to design long-running tools with honest fallbacks.",
    date: "Sep 12, 2026",
    read: "13 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP progressToken",
      "MCP notifications/progress",
      "MCP progress notifications not working",
      "MCP long running tool",
      "MCP tool timeout",
      "MCP Inspector progress notifications",
    ],
    related: [
      "mcp-tasks-extension",
      "latency-that-matters-for-agents",
      "mcp-server-slos",
      "mcp-json-rpc-messages-explained",
    ],
    body: [
      p("MCP progress notifications are optional, request-scoped updates for long-running work. A client that wants updates includes a progressToken in the request metadata. The receiving side may then send notifications/progress with that token, a progress value, an optional total, and an optional human-readable message. The token connects an update to the request that is still in progress."),
      callout("The important limitation", "A server must not assume that every client sends a progressToken or renders notifications. If the token is absent, the server has no request-scoped progress channel to use. A final result, a bounded task flow, or an application status endpoint may be a better fallback."),
      p("This article is verified against the MCP 2025-11-25 progress specification and current SDK and client documentation on September 12, 2026. Support is not uniform across hosts. A successful implementation in MCP Inspector does not prove that another host will request or display progress."),
      h("The progress flow"),
      code(`{
  "jsonrpc": "2.0",
  "id": 7,
  "method": "tools/call",
  "params": {
    "name": "build_report",
    "arguments": { "project": "acme" },
    "_meta": { "progressToken": "request-7-progress" }
  }
}`),
      p("The server may send updates during the operation. Each update repeats the same progressToken. The progress value should increase as work advances. total is optional because the server may not know the total amount of work in advance."),
      code(`{
  "jsonrpc": "2.0",
  "method": "notifications/progress",
  "params": {
    "progressToken": "request-7-progress",
    "progress": 4,
    "total": 10,
    "message": "Collected four data sources"
  }
}`),
      h("What progressToken does and does not mean"),
      ul([
        "It correlates a progress update with an active request.",
        "It is not a public job ID, a durable task handle, or proof that the user saw an update.",
        "It must be unique among active requests from the sender.",
        "It can be a string or an integer, but the receiver should treat it as a correlation value rather than an encoded instruction.",
        "A receiver may choose not to send progress even when a token was provided.",
      ]),
      h("Why progress does not appear"),
      p("The most common explanation is that the client did not request progress. The protocol makes the token opt-in. Another explanation is that the client requested it but does not expose or render notifications/progress in its user interface. A third is that a transport or SDK buffered the messages until the final result, or that the server sent updates after the request had already completed."),
      ul([
        "Log whether the incoming request contained progressToken before starting work.",
        "Log the token only in a controlled, non-sensitive form and never confuse it with an authorization credential.",
        "Send updates from the same request context and stop after completion or cancellation.",
        "Check the host's documentation or a reproducible client test instead of inferring support from its MCP branding.",
        "Test with a small deterministic tool before debugging a large external workflow.",
      ]),
      h("Progress values and rate limits"),
      p("Progress is not a log stream. Sending one notification for every row, network retry, or internal function call can flood a client and add work to the same connection that carries the result. Choose meaningful milestones, coalesce frequent updates, and apply a time or count based rate limit."),
      p("The progress number should increase even when total is unknown. It can represent completed stages, processed records, or an estimated unit of work, but the meaning should be stable within one operation. Do not reset progress to zero when moving between internal phases unless the application clearly defines a different nested progress model."),
      h("Progress versus task polling"),
      p("Progress notifications are a best-effort view of an active request. Tasks are a durable asynchronous protocol extension for operations that may outlive the original request or connection. A task can be polled with tasks/get and can expose a final result or error. Progress can supplement a task, but it is not a replacement for durable state."),
      h("Progress versus cancellation"),
      p("A progress update tells the receiver how work is advancing. It does not request that work stop. Cancellation has its own semantics, and in the 2025-era protocol the sender can use notifications/cancelled for an in-progress request. A server should connect cancellation to an AbortSignal or equivalent cleanup path and continue to handle the race where cancellation arrives after work has completed."),
      h("A safe server pattern"),
      code(`async function buildReport(ctx: { progressToken?: string }) {
  const steps = ["load", "query", "format", "save"];

  for (let index = 0; index < steps.length; index += 1) {
    await runStep(steps[index]);
    if (ctx.progressToken !== undefined) {
      await sendProgress({
        progressToken: ctx.progressToken,
        progress: index + 1,
        total: steps.length,
        message: "Completed a report stage",
      });
    }
  }

  return { ok: true };
}`),
      p("The example makes progress conditional on the request context. In a real SDK, use its request metadata and notification API rather than inventing a parallel wire format. Keep the progress path non-blocking where possible, because telemetry or UI updates should not turn into a new failure point for the tool itself."),
      h("Testing matrix"),
      ul([
        "No progressToken: the tool completes without sending notifications.",
        "One token: updates correlate to the correct request.",
        "Two concurrent tokens: updates never cross between requests.",
        "Unknown token: the server does not emit an unrelated notification.",
        "Non-increasing progress: the server or client reports a validation failure in a test fixture.",
        "Slow client: updates are bounded and the final result remains deliverable.",
        "Cancellation race: work cleans up whether cancellation arrives before or after the final result.",
        "Client that ignores notifications: the user still receives an honest final state or task handle.",
      ]),
      h("What TrackMCP can and cannot tell you"),
      p("TrackMCP can correlate observed tool duration, server-side errors, explicit workflow outcomes, and any bounded progress-related event that the instrumented server chooses to emit. This helps answer whether the server spent time in a slow operation and whether it eventually returned a result."),
      p("TrackMCP cannot tell you that a progress notification was displayed, that a host updated its UI, or that a model used the message to decide what to do next. Those are client and host behaviors outside the server boundary."),
      faq("Does every MCP client support progress notifications?", "No. Progress is optional, and support varies by client. A client may omit progressToken or may not render notifications even when the server sends them."),
      faq("What should a server do when progressToken is missing?", "Continue without progress notifications and use a final result, a durable task extension, or an application-specific status flow when the operation needs a longer-lived status channel."),
      faq("Is progress the same as a task?", "No. Progress is request-scoped and best effort. A task is durable asynchronous state that a client can poll and retrieve after the original request or connection is gone."),
      links([
        { label: "MCP progress specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/utilities/progress" },
        { label: "MCP schema reference", href: "https://modelcontextprotocol.io/specification/2025-11-25/schema" },
        { label: "MCP tasks extension", href: "https://modelcontextprotocol.io/extensions/tasks/overview" },
        { label: "Latency that matters for agents", href: "/blog/latency-that-matters-for-agents" },
        { label: "MCP server SLOs", href: "/blog/mcp-server-slos" },
      ]),
    ],
  },
  {
    slug: "mcp-tasks-extension",
    title: "MCP Tasks Extension: Async Tool Calls, Polling, Input, and Cancellation",
    tag: "MCP protocol deep dive",
    excerpt:
      "A version-aware guide to the MCP Tasks extension, including task handles, tasks/get polling, tasks/update input, TTL, authorization, and cooperative cancellation.",
    date: "Sep 12, 2026",
    read: "16 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP tasks extension",
      "MCP tasks/get",
      "MCP tasks/update",
      "MCP tasks/cancel",
      "MCP async tool call",
      "MCP long running task",
      "MCP task cancellation",
    ],
    related: [
      "mcp-progress-notifications",
      "mcp-2026-07-28-migration-guide",
      "how-to-monitor-an-mcp-server-in-production",
      "mcp-server-slos",
    ],
    body: [
      p("The MCP Tasks extension gives a server a standard way to turn a long-running tools/call into a durable task handle. Instead of holding one request open until a batch job, approval flow, or external job completes, the server can return a task result. The client then polls tasks/get, follows the suggested interval, supplies mid-flight input through tasks/update when needed, and sends tasks/cancel when the user asks to stop."),
      callout("Cancellation is cooperative", "A successful tasks/cancel response acknowledges the cancellation request. It does not prove that the worker stopped, that external side effects were rolled back, or that the task will reach cancelled instead of another terminal state."),
      p("This article is verified against the MCP Tasks extension documentation and SEP-2663 on September 12, 2026. Tasks were experimental in the 2025-11-25 core specification and are an extension in the modern 2026-07-28 ecosystem. Do not mix the two wire models in one implementation without a compatibility plan."),
      h("When to use a task"),
      ul([
        "The operation can take minutes or hours, such as a batch export or build.",
        "The underlying service already returns a job ID and supports status polling.",
        "The client may disconnect and still needs to retrieve the final result later.",
        "The operation pauses for human input or approval.",
        "The work produces meaningful intermediate status but should not keep one request open.",
      ]),
      p("A normal tool result is still the right choice for a short operation. A task adds durable state, storage, authorization, cleanup, polling traffic, and compatibility work. It should not be added only because a tool has several internal function calls."),
      h("Extension negotiation comes first"),
      p("A server must check that the client declared support for the Tasks extension before returning a task result. A client that does not understand the extension cannot be expected to poll tasks/get or interpret the task discriminator. A compatible server may use a normal synchronous result as a fallback, reject the operation with a clear error, or expose a separate tool contract depending on the product's needs."),
      code(`{
  "_meta": {
    "io.modelcontextprotocol/clientCapabilities": {
      "extensions": { "io.modelcontextprotocol/tasks": {} }
    }
  }
}`),
      h("Creating a task from tools/call"),
      p("The server responds with a task handle instead of the final tool content. The task should be durably recorded before the response is sent. The task ID must be unguessable, bound to the correct authorization context, and treated as sensitive state because it may grant access to status or results."),
      code(`{
  "jsonrpc": "2.0",
  "id": 10,
  "result": {
    "resultType": "task",
    "task": {
      "taskId": "task-opaque-9d3f",
      "status": "working",
      "createdAt": "2026-09-12T09:00:00Z",
      "lastUpdatedAt": "2026-09-12T09:00:00Z",
      "ttlMs": 3600000,
      "pollIntervalMs": 5000
    }
  }
}`),
      h("Polling tasks/get"),
      p("The client polls tasks/get with the task ID and should respect pollIntervalMs rather than creating a tight loop. The server returns the current state. On completed, the result contains the final tool result. On failed, the error contains the failure details. The client must tolerate status changes between polls and should not assume that a single status response is a transactionally complete view of every worker side effect."),
      code(`{
  "jsonrpc": "2.0",
  "id": 11,
  "method": "tasks/get",
  "params": { "taskId": "task-opaque-9d3f" }
}`),
      ul([
        "working means the task is still executing.",
        "input_required means the server needs a response before continuing.",
        "completed means a final result is available.",
        "failed means the associated operation ended with an error.",
        "cancelled means cancellation was recorded as the task's terminal state.",
      ]),
      h("Mid-flight input with tasks/update"),
      p("A task can enter input_required when a tool needs approval or another piece of user information. The client sends the accepted, declined, or cancelled input through tasks/update. The server may accept partial responses and keep the task waiting for the remaining keys. This separates reading state through tasks/get from writing a response through tasks/update."),
      h("Why tasks/cancel is not a kill switch"),
      p("Cancellation is cooperative. The server acknowledges intent and should try to stop the work, but the worker may already be finishing, may have committed an external side effect, or may be running in a system that cannot interrupt the current step. A task can therefore remain working briefly after the acknowledgement or reach a different terminal state if completion won the race."),
      p("The cancellation path must be idempotent from the application's perspective. Repeated user clicks, network retries, or duplicate delivery should not create a second destructive operation. The worker should check cancellation state at safe points, release resources, and record which side effects already happened."),
      h("Durability, TTL, and authorization"),
      ul([
        "Persist task state outside the request process when tasks must survive disconnects or restarts.",
        "Expire task state according to ttlMs and document whether results are deleted immediately or retained for a bounded period.",
        "Authorize tasks/get, tasks/update, and tasks/cancel against the caller and task owner on every request.",
        "Do not treat a task ID as a substitute for authorization. If it acts as a bearer handle, make it high entropy and short-lived.",
        "Keep the task result bounded and apply the same privacy and redaction rules as a synchronous tool result.",
        "Use a poll interval that protects the task store and reflects how quickly the underlying work can change.",
      ]),
      h("The current extension is not the old task model"),
      p("The 2025-11-25 experimental task design included a different set of methods and fields. In the current Tasks extension, the core flow is tools/call returning a task, tasks/get for state and results, tasks/update for input, and tasks/cancel for cancellation. The extension documentation does not make every historical method interchangeable with the modern flow. Pin the protocol and extension versions in tests and SDK configuration."),
      h("Testing a task implementation"),
      ul([
        "Client does not advertise the extension: the server returns a synchronous result or a clear unsupported response.",
        "Task is durably created before the create response is sent.",
        "Duplicate tasks/get calls are safe and do not advance the underlying job.",
        "The poll interval is honored and a client handles a delayed or missing poll response.",
        "Input_required accepts a partial update and resumes only after required input is complete.",
        "Cancellation races with completion and produces a documented final state.",
        "Unknown, expired, or cross-tenant task IDs are rejected without revealing task data.",
        "A worker restart can recover or fail the task according to a documented policy.",
      ]),
      h("What TrackMCP can and cannot tell you"),
      p("TrackMCP can show that a server received the task-creating tool call, returned a task handle, received later polls or updates, and emitted explicit application outcomes when the application reports them. Those events help a server team understand whether the protocol path is being exercised and where observed latency accumulates."),
      p("TrackMCP cannot inspect a private worker queue, know whether an external provider committed a side effect, or infer that a task's completed status means the customer's business goal succeeded. Emit a deliberate workflow outcome when that stronger claim matters."),
      faq("Does MCP Tasks replace progress notifications?", "No. Tasks provide durable state and polling for longer-lived work. Progress notifications are optional updates for an active request and can supplement a task when the client supports them."),
      faq("Does tasks/cancel stop the worker immediately?", "No. Cancellation is cooperative. The server acknowledges the request and should attempt to stop the work, but completion or external side effects may race with cancellation."),
      faq("Is tasks/list part of the current Tasks extension?", "The current extension centers on tasks/get, tasks/update, and tasks/cancel. Do not assume that methods from the older experimental task model are part of the modern extension."),
      links([
        { label: "MCP Tasks extension overview", href: "https://modelcontextprotocol.io/extensions/tasks/overview" },
        { label: "MCP Tasks extension specification", href: "https://tasks.extensions.modelcontextprotocol.io/specification/draft/tasks" },
        { label: "SEP-2663: Tasks Extension", href: "https://modelcontextprotocol.org/seps/2663-tasks-extension" },
        { label: "MCP progress notifications", href: "/blog/mcp-progress-notifications" },
        { label: "MCP 2026 migration guide", href: "/blog/mcp-2026-07-28-migration-guide" },
      ]),
    ],
  },
  {
    slug: "mcp-elicitation-form-url-mode",
    title: "MCP Elicitation Explained: Form Mode, URL Mode, and Secure User Consent",
    tag: "MCP protocol deep dive",
    excerpt:
      "Understand MCP elicitation form mode, URL mode, multi-round requests, user consent, third-party authorization, and the data each path exposes.",
    date: "Sep 12, 2026",
    read: "15 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP elicitation",
      "MCP elicitation form mode",
      "MCP URL mode elicitation",
      "MCP elicitation OAuth",
      "MCP server ask user for input",
      "MCP user consent",
    ],
    related: [
      "remote-mcp-oauth-guide",
      "mcp-token-passthrough-security",
      "mcp-authorization-errors",
      "mcp-tasks-extension",
    ],
    body: [
      p("MCP elicitation lets a server ask the user for information during a request instead of requiring every value up front. The protocol has two different interaction modes. Form mode collects structured, non-sensitive information through the MCP client. URL mode sends the user to an out-of-band web interaction for sensitive data, third-party authorization, or payment confirmation. The distinction is a privacy and trust boundary, not merely a user-interface preference."),
      callout("The security rule", "Never use form mode to collect passwords, API keys, access tokens, payment credentials, or other secrets. Use a properly bound URL-mode flow or another out-of-band mechanism, and make sure the user who completes the browser interaction is the user who initiated it."),
      p("This article is verified against the MCP elicitation specifications, SEP-1036, and the 2026-07-28 multi-round-trip design on September 12, 2026. The wire flow differs between 2025-era and modern protocol revisions, so a production implementation must test the exact client and SDK versions it supports."),
      h("Form mode"),
      p("Form mode is an in-band structured request. The server sends a human-readable message and a requested schema. The client presents the fields, validates them according to its own policy, and returns accept, decline, or cancel with structured content when appropriate. The content is visible to the MCP client and can be passed back into the server request."),
      code(`{
  "mode": "form",
  "message": "Choose a deployment region",
  "requestedSchema": {
    "type": "object",
    "properties": {
      "region": { "type": "string", "enum": ["us", "eu"] }
    },
    "required": ["region"]
  }
}`),
      p("Good form-mode inputs include a non-sensitive preference, a date, a project name, or a confirmation that does not itself disclose a credential. The server must still validate the returned value. A client-rendered form is not a replacement for server-side validation or authorization."),
      h("URL mode"),
      p("URL mode is for an interaction that should not pass sensitive data through the MCP client. The server provides a valid URL, an elicitationId, and a message explaining why the user needs to open it. The client gives the user context and asks for consent to open the URL. The browser flow then happens outside the MCP message exchange."),
      code(`{
  "mode": "url",
  "elicitationId": "elicit-opaque-2048",
  "url": "https://mcp.example.com/connect?elicitationId=elicit-opaque-2048",
  "message": "Authorize access to the selected repository"
}`),
      p("URL mode does not authorize the MCP client's access to the MCP server. The MCP bearer token remains the same. URL mode is for the server obtaining user input or authorization from another system on the user's behalf. Confusing those two flows can produce an incomplete security design."),
      h("Bind the browser flow to the right user"),
      p("A URL can be copied, modified, opened by another person, or replayed. The server must bind the elicitation to the user who started it and verify that binding again when the browser callback arrives. A common design is a server-owned connect page that checks a user session before redirecting to a third-party authorization provider. The exact mechanism depends on the deployment, but it must resist an attacker changing the elicitation URL or substituting another user's session."),
      ul([
        "Use a short-lived, unguessable elicitation identifier.",
        "Store the initiating user or authorization subject with the elicitation state.",
        "Do not put secrets or bearer tokens in the URL query string.",
        "Use an exact callback and state validation strategy for the third-party flow.",
        "Treat accept as consent to open the interaction, not proof that the external action completed.",
        "Expire completed, cancelled, and abandoned elicitations.",
      ]),
      h("Accept, decline, cancel, and complete"),
      p("An accepted form contains user-provided content. A declined or cancelled form should let the server stop safely without treating the result as an error caused by the user. In URL mode, accept means the user consented to the client opening the URL. It does not necessarily mean that the external OAuth, payment, or credential step succeeded. The server should communicate completion separately when the out-of-band action finishes."),
      h("The modern multi-round-trip flow"),
      p("The 2026-07-28 revision removes the need for a server to send a separate server-to-client JSON-RPC request during a stateless call. Instead, a server can return an input_required result containing an elicitation request. The client fulfills it and retries the original operation with inputResponses and, when present, the opaque requestState. The retried request must use a fresh JSON-RPC ID."),
      code(`{
  "result": {
    "resultType": "input_required",
    "inputRequests": {
      "region": {
        "method": "elicitation/create",
        "params": { "mode": "form", "message": "Choose a region" }
      }
    },
    "requestState": "opaque-server-state"
  }
}`),
      p("Legacy 2025-era clients may use the older server-to-client request channel and capability negotiation. A server supporting both should keep the semantics aligned while using the wire pattern appropriate to the negotiated revision. Do not assume that a client supporting form mode also supports URL mode."),
      h("Elicitation and MCP authorization are different"),
      p("MCP authorization protects access to the MCP server itself. Elicitation can be used after that connection exists when the server needs the user to authorize a third-party service or provide sensitive information through a browser. The two flows may be related in a product, but they have different tokens, endpoints, trust decisions, and completion signals."),
      h("Client compatibility tests"),
      ul([
        "Client declares form support and renders a valid non-sensitive schema.",
        "Client declines a form and the tool exits without a side effect.",
        "Client cancels a form and the server cleans up pending state.",
        "Client supports URL mode and shows the full URL with user consent.",
        "Client does not support URL mode and receives a safe, actionable fallback.",
        "The user opens the URL in a different browser session and the server rejects the mismatch.",
        "The browser flow completes after the original MCP request has timed out.",
        "A duplicate retry does not create two authorizations or two payments.",
      ]),
      h("What TrackMCP can and cannot tell you"),
      p("TrackMCP can report server-boundary evidence such as an elicitation request, the mode emitted by the application, a subsequent retry, a tool error, or an explicit workflow outcome. It can help a team see where server-side flows stop and which client metadata was available."),
      p("TrackMCP cannot see a private browser session, validate that a user read a consent screen, or know that a third-party OAuth provider completed unless the application emits a bounded completion event. URL contents and credential values should remain outside telemetry unless a deliberate, compliant data policy says otherwise."),
      faq("Can form elicitation collect an API key?", "No. Form mode data passes through the MCP client and should be limited to non-sensitive structured input. Use URL mode or another out-of-band secure flow for secrets."),
      faq("Does URL mode authorize the MCP server?", "No. URL mode is for an out-of-band interaction that the server needs on the user's behalf. MCP client authorization is a separate flow."),
      faq("Does accepting a URL elicitation mean the OAuth flow succeeded?", "No. Accept means the user consented to the client opening the URL. The server needs a separate, securely bound completion check for the external interaction."),
      links([
        { label: "MCP elicitation documentation", href: "https://modelcontextprotocol.io/specification/draft/client/elicitation" },
        { label: "MCP 2025-11-25 elicitation specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/client/elicitation" },
        { label: "SEP-1036: URL mode elicitation", href: "https://modelcontextprotocol.org/seps/1036-url-mode-elicitation-for-secure-out-of-band-intera" },
        { label: "Remote MCP OAuth guide", href: "/blog/remote-mcp-oauth-guide" },
        { label: "MCP token passthrough security", href: "/blog/mcp-token-passthrough-security" },
      ]),
    ],
  },
  {
    slug: "what-is-an-mcp-server",
    title: "What Is an MCP Server? A Practical Guide for Beginners",
    tag: "MCP fundamentals",
    excerpt:
      "Understand what an MCP server is, what it exposes to AI applications, how it connects to an MCP client, and what to check before running one in production.",
    date: "Sep 12, 2026",
    read: "6 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "what is an MCP server",
      "MCP server",
      "Model Context Protocol server",
      "MCP server explained",
      "MCP server monitoring",
    ],
    related: [
      "what-is-the-model-context-protocol",
      "mcp-host-client-server-architecture",
      "mcp-tools-resources-prompts",
      "mcp-production-readiness-checklist",
    ],
    body: [
      p("An MCP server is a program that makes tools, resources, or prompt templates available to an AI application through the Model Context Protocol. The server does not have to be a large hosted service. It can be a local process started over standard input and output, or a remote service reached over HTTP. Its job is to expose a clear, permissioned interface that a compatible client can discover and use."),
      callout("Short answer", "An MCP server is the capability provider in an MCP connection. It exposes an MCP surface, while the host application and its client decide when and how that surface is used."),
      h("What an MCP server does"),
      p("During initialization, an MCP server identifies itself, agrees on a protocol version with the client, and declares the capabilities it supports. After that handshake, it can list and serve the primitives it has implemented. A calendar server might expose tools for creating events and resources for reading calendars. A code server might expose repository resources and tools for running a search."),
      ul([
        "Tools are callable functions that can retrieve information or take an action.",
        "Resources are addressable data that an application can attach as context.",
        "Prompts are reusable templates that a user can choose in a compatible client.",
        "Notifications can tell a client that a tool, resource, or prompt catalog has changed.",
      ]),
      h("MCP server versus API"),
      p("An MCP server can call existing APIs, databases, filesystems, or internal services. MCP is the interface presented to an AI application, not a requirement to replace every backend API. A well-designed server maps a small set of useful, safe capabilities onto existing systems and keeps authorization, validation, and failure behavior explicit."),
      h("Local and remote MCP servers"),
      p("A local server commonly runs as a child process and communicates over stdio. A remote server uses an HTTP transport and can be deployed like another service. The deployment choice affects authentication, scaling, secrets, networking, and how a team monitors the server, but both forms share the same protocol concepts."),
      h("What to check before production"),
      ul([
        "Every tool has a precise name, description, input schema, and safe error path.",
        "Destructive actions require an intentional authorization boundary and useful audit metadata.",
        "The server has timeouts, bounded work, structured logs, and a clear shutdown behavior.",
        "Telemetry is minimized and redacted before sensitive arguments or results leave the process.",
        "You can tell whether a successful transport response contained a tool-level error.",
      ]),
      p("TrackMCP helps server teams measure the boundary they own: client connections, catalog and tool usage, observed latency, server-side errors, sessions, and explicit workflow outcomes when those signals are emitted. It does not expose private model reasoning or every event in the host application."),
      faq("Does an MCP server contain the AI model?", "Usually no. An MCP server provides capabilities to a host application. The host typically owns the model interaction and creates a client connection to the server."),
      faq("Can an MCP server be local?", "Yes. A local MCP server can run as a process connected over stdio. Remote servers can use an HTTP transport and apply their own authentication and deployment controls."),
      links([
        { label: "Official MCP server overview", href: "https://modelcontextprotocol.io/specification/2025-11-25/server" },
        { label: "MCP production readiness checklist", href: "/blog/mcp-production-readiness-checklist" },
        { label: "MCP server analytics guide", href: "/blog/mcp-server-analytics-guide" },
      ]),
    ],
  },
  {
    slug: "what-is-an-mcp-client",
    title: "What Is an MCP Client? How AI Apps Connect to MCP Servers",
    tag: "MCP fundamentals",
    excerpt:
      "Learn what an MCP client is, how it maintains a server connection, and why the client is different from both the host application and the MCP server.",
    date: "Sep 12, 2026",
    read: "6 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "what is an MCP client",
      "MCP client",
      "MCP client server connection",
      "AI application MCP client",
      "MCP architecture",
    ],
    related: [
      "what-is-an-mcp-server",
      "mcp-host-client-server-architecture",
      "how-to-connect-an-mcp-server-to-an-ai-client",
      "understanding-your-mcp-client-mix",
    ],
    body: [
      p("An MCP client is the component that maintains a connection to one MCP server and exchanges protocol messages with it. An AI host application can create several clients, usually one per server connection, so it can combine capabilities from different servers while keeping their sessions and permissions separate."),
      callout("Short answer", "The client is the protocol-speaking connection manager. It negotiates capabilities, sends requests, receives results, and routes server messages to the host application."),
      h("Client, host, and server are different roles"),
      p("The host is the application that coordinates the model, user experience, permissions, and multiple server connections. The client is created and managed by that host for a particular server. The server exposes tools, resources, and prompts. Keeping these roles distinct makes it easier to reason about trust, telemetry, and failure boundaries."),
      ul([
        "Host: coordinates the user, model, consent, and collection of context.",
        "Client: owns one stateful protocol connection and its negotiated capabilities.",
        "Server: provides the domain-specific tools, resources, and prompts.",
      ]),
      h("What happens when a client connects"),
      p("The client begins with an initialize request that includes its protocol version, client capabilities, and implementation information. The server responds with the version and server capabilities it will use. The client then sends an initialized notification before normal operations begin. Both sides must stay within the capabilities they negotiated."),
      h("Why client identity matters"),
      p("The same MCP server can receive very different traffic from different clients. Client metadata can help a server team understand adoption, transport behavior, and compatibility. It should be treated as an observed identity signal, not automatically as a verified person, organization, or model identity."),
      h("Client-side facts a server may not see"),
      p("A server boundary does not automatically reveal the host's complete conversation, the model's private reasoning, the tools selected but never called, or the final user-facing answer. Server telemetry can explain what arrived and what the server returned, but it should not be presented as a complete trace of the agent run."),
      h("Practical debugging questions"),
      ul([
        "Did initialization complete, and which protocol version and capabilities were agreed?",
        "Did the client list tools or resources before calling one?",
        "Did the client retry because of a timeout, a validation error, or an application-level failure?",
        "Does the issue reproduce for one client or for every client using the server?",
      ]),
      faq("Can one host use more than one MCP client?", "Yes. The MCP architecture allows a host to manage multiple client instances, with each client maintaining a connection to a particular server."),
      faq("Is an MCP client the same as an AI chatbot?", "No. A chatbot or host may contain an MCP client, but the client itself is the protocol component that connects the host to a server."),
      links([
        { label: "Official MCP architecture", href: "https://modelcontextprotocol.io/specification/2025-11-25/architecture" },
        { label: "MCP client mix analytics", href: "/blog/understanding-your-mcp-client-mix" },
        { label: "Connect an MCP server to an AI client", href: "/blog/how-to-connect-an-mcp-server-to-an-ai-client" },
      ]),
    ],
  },
  {
    slug: "mcp-host-client-server-architecture",
    title: "MCP Host, Client, and Server: What Each Component Does",
    tag: "MCP fundamentals",
    excerpt:
      "A clear explanation of the MCP host, client, and server architecture, including connection ownership, capability negotiation, permissions, and observability boundaries.",
    date: "Sep 12, 2026",
    read: "7 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP architecture",
      "MCP host client server",
      "MCP server architecture",
      "MCP client server model",
      "Model Context Protocol architecture",
    ],
    related: ["what-is-an-mcp-client", "what-is-an-mcp-server", "mcp-tools-resources-prompts", "mcp-observability-guide"],
    body: [
      p("MCP uses a host, client, and server architecture to connect AI applications with external capabilities. The three roles are easy to blur together because they can run in one process or across different machines. They still have different responsibilities, and those boundaries matter when you design permissions, debug failures, or choose what telemetry to collect."),
      callout("The mental model", "The host coordinates the AI experience, the client maintains a single server connection, and the server exposes domain capabilities. One host can manage many clients, while each client has a one-to-one relationship with a server connection."),
      h("The host is the coordinator"),
      p("The host application owns the user experience and usually coordinates the language model, consent prompts, policy decisions, context assembly, and multiple server connections. It decides which servers are available and how their results are presented. The protocol does not require every host to expose the same interface."),
      h("The client is the connection boundary"),
      p("A host creates a client for each server connection. The client handles initialization, protocol version agreement, capability negotiation, request and notification routing, and session lifecycle. Keeping one client connection associated with one server makes permission and failure behavior easier to isolate."),
      h("The server is the capability provider"),
      p("The server implements the domain behavior. It may expose tools, resources, prompts, logging, completions, or other negotiated capabilities. Servers can be local processes or remote services. They should validate inputs, enforce their own authorization rules, and avoid assuming that a connected client is automatically trusted."),
      h("How a request travels"),
      ul([
        "The host makes a capability available through a configured client.",
        "The client sends a protocol request to the server over the selected transport.",
        "The server validates the method and arguments, performs the operation, and returns a result or error.",
        "The client routes the response to the host, which decides how it affects the model or user interface.",
      ]),
      h("Where observability belongs"),
      p("Each role has a different view. Host telemetry can include model and user experience context. Client telemetry can show connection and routing behavior. Server-boundary telemetry can show what the server received and returned, including tool names, observed duration, and application-level errors. None of these views should be described as the entire agent trace unless the system actually correlates all of them."),
      h("Common architecture mistakes"),
      ul([
        "Treating the server as if it owns the model's hidden reasoning.",
        "Assuming one successful connection proves that every tool is usable.",
        "Letting a server trust client-provided labels as complete identity or authorization.",
        "Collecting full arguments and results by default when metadata is sufficient.",
      ]),
      faq("Can host, client, and server run in one process?", "They can be colocated, but their protocol roles remain distinct. Many local integrations use one application to start and communicate with a server process."),
      faq("Why is the one-client-to-one-server relationship useful?", "It gives the host an isolated place to manage a server's lifecycle, negotiated capabilities, permissions, and failures."),
      links([
        { label: "Official MCP architecture", href: "https://modelcontextprotocol.io/specification/2025-11-25/architecture" },
        { label: "MCP observability guide", href: "/blog/mcp-observability-guide" },
        { label: "MCP server analytics guide", href: "/blog/mcp-server-analytics-guide" },
      ]),
    ],
  },
  {
    slug: "mcp-tools-resources-prompts",
    title: "MCP Tools, Resources, and Prompts: The Three Primitives Explained",
    tag: "MCP fundamentals",
    excerpt:
      "Learn the difference between MCP tools, resources, and prompts, who controls each primitive, and how to design a server that agents can use safely.",
    date: "Sep 12, 2026",
    read: "7 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP tools resources prompts",
      "MCP primitives",
      "MCP tools explained",
      "MCP resources explained",
      "MCP prompt templates",
    ],
    related: ["what-is-an-mcp-server", "mcp-tool-schemas", "writing-tool-descriptions-agents-use", "mcp-tool-selection-evaluation"],
    body: [
      p("MCP servers expose three core primitives: tools, resources, and prompts. They all make useful context or capabilities available to a host, but they are not interchangeable. The most important difference is control: tools are generally model-controlled, resources are application-controlled, and prompts are user-controlled."),
      callout("A quick distinction", "Use a tool for an action or computation, a resource for addressable context, and a prompt for a reusable interaction template. The host decides how each primitive appears in its experience."),
      h("Tools are callable actions"),
      p("A tool has a name, description, and input schema. A client can discover the catalog and request a tool call with structured arguments. Tools may read data, transform it, or take an external action. Because a tool can have side effects, its authorization, validation, idempotency, and error behavior deserve the same attention as any other action API."),
      h("Resources are addressable context"),
      p("A resource is identified by a URI and represents data that a server makes available to a client or host. It might be a file, schema, record, document, or another domain object. Resource templates can describe URI patterns. The host application decides how resources are presented and when their contents become model context."),
      h("Prompts are reusable templates"),
      p("Prompts are predefined interaction templates that a user can choose. A prompt can include arguments and can assemble a useful starting context for a task. The prompt primitive is not the same as a hidden system prompt. It is a server-provided template offered through the protocol and controlled by the host's user experience."),
      h("The control model matters"),
      ul([
        "Model-controlled tools should have narrow scope, clear descriptions, and safe defaults.",
        "Application-controlled resources should respect the host's context and consent decisions.",
        "User-controlled prompts should be discoverable, labeled, and explicit about their expected inputs.",
      ]),
      h("How to choose a primitive"),
      p("Ask what the capability represents. If it changes a system or performs a computation, it is likely a tool. If it is data that can be fetched or subscribed to, it is likely a resource. If it is a reusable way to start a task, it is likely a prompt. A server can use more than one primitive for the same domain, but each should have a clear contract."),
      h("What to measure"),
      p("A server team should separate primitive discovery from use. A tool can be listed often but called rarely. A resource can be fetched without leading to a completed workflow. A prompt can be selected but never result in a server operation. Instrumentation should preserve those distinctions instead of collapsing every interaction into one activity count."),
      faq("Are tools the only MCP primitive agents use?", "No. Tools, resources, and prompts are all part of the MCP server feature set. The host application controls how each is exposed and used."),
      faq("Can a resource change?", "Yes. Servers can support resource subscriptions and list-change notifications when they advertise the relevant capabilities."),
      links([
        { label: "Official MCP server feature overview", href: "https://modelcontextprotocol.io/specification/2025-11-25/server" },
        { label: "MCP tool schemas", href: "/blog/mcp-tool-schemas" },
        { label: "Writing tool descriptions agents use", href: "/blog/writing-tool-descriptions-agents-use" },
      ]),
    ],
  },
  {
    slug: "how-mcp-works-step-by-step",
    title: "How MCP Works: The Request Lifecycle from Discovery to Tool Result",
    tag: "MCP fundamentals",
    excerpt:
      "Follow a typical MCP request from initialization and capability negotiation through discovery, tool execution, errors, and connection shutdown.",
    date: "Sep 12, 2026",
    read: "7 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "how MCP works",
      "MCP request lifecycle",
      "MCP tool call flow",
      "MCP initialization",
      "MCP protocol flow",
    ],
    related: ["mcp-host-client-server-architecture", "mcp-capability-negotiation", "mcp-json-rpc-messages-explained", "instrumenting-the-mcp-protocol-layer"],
    body: [
      p("MCP works as a negotiated client-server session. Before a tool can be called, the client and server establish a protocol version, exchange capabilities, and complete initialization. The client can then discover the server's available primitives, send a request, receive a result or error, and close the connection when the session ends."),
      callout("The lifecycle in one line", "Initialize, negotiate, discover, request, validate, execute, return, and observe the outcome. A transport response alone is only one point in that sequence."),
      h("1. The host creates a client"),
      p("The host application selects a server and creates an MCP client for the connection. The transport may be stdio for a local process or Streamable HTTP for a remote service. The client is responsible for speaking the protocol over that transport."),
      h("2. Initialization establishes the session"),
      p("The client sends an initialize request with its supported protocol version, client capabilities, and implementation details. The server responds with the version and server capabilities it will use. The client follows with an initialized notification. Normal operations begin only after this handshake."),
      h("3. Discovery makes the catalog visible"),
      p("The client can request the server's tools, resources, or prompts when the corresponding capabilities are available. The returned names, descriptions, schemas, and URIs give the host enough information to decide what to present or what an agent may call."),
      h("4. A request is validated and executed"),
      p("For a tool call, the client sends a JSON-RPC request with a method and structured arguments. The server validates the method, arguments, authorization, and domain constraints before doing work. It returns a result or a protocol error. A tool result can also carry an application-level error even when the transport itself completed successfully."),
      h("5. Notifications and updates can occur"),
      p("Servers can send notifications such as catalog changes or progress updates when those capabilities are negotiated. Clients and servers should still apply timeouts and bounded work so a slow or misbehaving operation does not exhaust resources."),
      h("6. Shutdown closes the connection"),
      p("MCP does not require a separate shutdown message. A stdio client closes the child process input and waits for the process to exit, while HTTP sessions close their associated connections. Operational telemetry should make the end of a session distinguishable from a failed connection."),
      h("A useful debugging timeline"),
      ul([
        "Initialization failed: inspect protocol version and capability compatibility.",
        "Discovery failed: inspect the server catalog and the negotiated feature.",
        "The call was rejected: inspect authorization, method, schema, and validation.",
        "The call returned an error: inspect tool behavior and downstream dependencies.",
        "The call succeeded but the workflow stopped: inspect the host-side context and outcome signal.",
      ]),
      faq("Does MCP always use HTTP?", "No. The protocol defines stdio for local communication and Streamable HTTP for remote communication. Implementations can also add custom transports."),
      faq("Does a 200 response mean the tool worked?", "Not necessarily. A successful transport response can contain a tool result that indicates an application-level error. Inspect the MCP result, not only the HTTP status."),
      links([
        { label: "Official MCP lifecycle specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/lifecycle" },
        { label: "MCP protocol instrumentation", href: "/blog/instrumenting-the-mcp-protocol-layer" },
        { label: "How to monitor an MCP server", href: "/blog/how-to-monitor-an-mcp-server-in-production" },
      ]),
    ],
  },
  {
    slug: "mcp-json-rpc-messages-explained",
    title: "MCP Messages Explained: JSON-RPC Methods, Results, and Errors",
    tag: "MCP fundamentals",
    excerpt:
      "Understand the JSON-RPC message types used by MCP, how request IDs work, and how to read protocol errors and tool-level failures during debugging.",
    date: "Sep 12, 2026",
    read: "7 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP JSON-RPC",
      "MCP messages",
      "MCP request response",
      "MCP protocol errors",
      "JSON-RPC MCP server",
    ],
    related: ["how-mcp-works-step-by-step", "mcp-capability-negotiation", "mcp-tool-schemas", "how-to-debug-mcp-tool-errors"],
    body: [
      p("MCP messages use JSON-RPC 2.0. That gives clients and servers a standard shape for requests, results, errors, and notifications while MCP defines the methods and data structures that carry protocol capabilities. Reading these message shapes is one of the fastest ways to debug an MCP connection."),
      callout("The key distinction", "A request expects a response and has an ID. A notification has no response ID. A protocol error describes a problem processing the request, while a tool result can report an application-level failure inside a valid response."),
      h("Requests have methods and IDs"),
      p("A request includes the JSON-RPC version, an ID, a method name, and optional parameters. The ID lets the client match a response to the operation that created it. Methods such as initialize, tools/list, and tools/call describe MCP operations, while the parameters carry the structured input for that method."),
      code('{\n  "jsonrpc": "2.0",\n  "id": 7,\n  "method": "tools/call",\n  "params": {\n    "name": "search",\n    "arguments": { "query": "MCP" }\n  }\n}'),
      h("Results return successful work"),
      p("A result uses the same request ID and includes a result object. For a tool call, the result can contain content items and an isError signal. The server may have handled the protocol request correctly while the underlying action still failed, so clients should inspect the result semantics."),
      h("Errors identify request handling failures"),
      p("An error response includes a code, message, and optional data. It can indicate an invalid request, an unknown method, invalid parameters, or a server-side failure. The message is useful for diagnosis, but production telemetry should avoid copying secrets or unbounded user input into logs."),
      h("Notifications do not expect a result"),
      p("Notifications omit the ID because the sender does not expect a response. MCP uses them for events such as initialized and catalog-change notifications. A client should not wait for a response to a notification, and a server should not manufacture one."),
      h("How to debug a captured exchange"),
      ul([
        "Confirm the message is valid JSON-RPC and uses jsonrpc 2.0.",
        "Match every response ID to the request that created it.",
        "Separate a protocol error from a valid tool result with isError set.",
        "Check method names, argument shapes, and negotiated capabilities.",
        "Redact tokens and sensitive arguments before storing or sharing an exchange.",
      ]),
      p("TrackMCP's server-boundary view is designed to preserve useful event semantics without requiring raw message payloads. That makes it possible to investigate tool usage, duration, errors, and sessions while keeping capture bounded and privacy-aware."),
      faq("Is every MCP error an HTTP error?", "No. MCP messages can carry protocol errors and tool-level error results independently of the transport status code."),
      faq("Why are request IDs important?", "They let a client match responses to requests, which is essential when several operations are in flight at the same time."),
      links([
        { label: "Official MCP messages specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/messages" },
        { label: "How to debug MCP tool errors", href: "/blog/how-to-debug-mcp-tool-errors" },
        { label: "MCP tool schemas", href: "/blog/mcp-tool-schemas" },
      ]),
    ],
  },
  {
    slug: "mcp-capability-negotiation",
    title: "MCP Capability Negotiation: Versions, Features, and Initialization",
    tag: "MCP fundamentals",
    excerpt:
      "See how MCP clients and servers agree on protocol versions and optional capabilities, and learn how negotiation failures show up in production.",
    date: "Sep 12, 2026",
    read: "6 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP capability negotiation",
      "MCP initialization",
      "MCP protocol version",
      "MCP capabilities",
      "MCP compatibility",
    ],
    related: ["mcp-host-client-server-architecture", "how-mcp-works-step-by-step", "mcp-2026-07-28-migration-guide", "mcp-production-readiness-checklist"],
    body: [
      p("Capability negotiation is the part of MCP initialization where a client and server agree on the protocol version and optional features they can use together. It prevents one side from assuming that the other side supports a feature it never advertised."),
      callout("Why it matters", "A connection can be reachable and authenticated yet still be incompatible. Record the negotiated version and capabilities when diagnosing a server that works in one client but not another."),
      h("Version agreement comes first"),
      p("The client begins by sending an initialize request with a protocol version it supports. The server responds with the version that will govern the session. If the two sides cannot agree, the session should not proceed to normal operations."),
      h("Capabilities describe optional features"),
      p("Capabilities are grouped by client and server responsibilities. A server can advertise tools, resources, prompts, logging, completions, or other supported features. A client can advertise support for roots, sampling, elicitation, tasks, or other features. Optional sub-capabilities can describe things such as list-change notifications or resource subscriptions."),
      h("Negotiated does not mean universal"),
      p("The server may implement a feature in its codebase but still need to advertise it for the client to use it. After initialization, both sides must respect the negotiated set. A client should not send a request for an unadvertised capability, and a server should not emit feature-specific notifications without declaring the relevant support."),
      h("Diagnosing negotiation problems"),
      ul([
        "Capture the client and server protocol versions without guessing from a product name.",
        "Compare the advertised capability object with the method or notification that failed.",
        "Check whether the issue is version mismatch, missing capability, invalid parameters, or a server runtime error.",
        "Test more than one client when the server behaves differently across hosts.",
      ]),
      h("Operational telemetry for compatibility"),
      p("Version and capability dimensions can have high diagnostic value, but they should remain bounded and low-cardinality. A server team can use them to find clients that still use an older transport or a feature set that the current release no longer exercises. They should not collect raw message bodies when a small set of validated fields answers the question."),
      faq("Can capabilities change during a session?", "Some catalogs can change and servers can send the corresponding notifications when supported. The initial capability negotiation still establishes which feature families are available for the session."),
      faq("Does a negotiated capability guarantee a successful tool call?", "No. It only says that the feature is available. Authorization, validation, downstream dependencies, and application behavior can still cause a call to fail."),
      links([
        { label: "Official MCP lifecycle specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/lifecycle" },
        { label: "MCP migration guide", href: "/blog/mcp-2026-07-28-migration-guide" },
        { label: "MCP production readiness checklist", href: "/blog/mcp-production-readiness-checklist" },
      ]),
    ],
  },
  {
    slug: "mcp-transports-stdio-and-streamable-http",
    title: "MCP Transports Explained: stdio and Streamable HTTP",
    tag: "MCP fundamentals",
    excerpt:
      "Compare MCP's standard stdio and Streamable HTTP transports, including deployment shape, authentication, scaling, and observability tradeoffs.",
    date: "Sep 12, 2026",
    read: "7 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "MCP transports",
      "MCP stdio",
      "MCP Streamable HTTP",
      "remote MCP server",
      "local MCP server",
    ],
    related: ["what-is-an-mcp-server", "remote-mcp-oauth-guide", "opentelemetry-for-mcp-servers", "how-to-monitor-an-mcp-server-in-production"],
    body: [
      p("An MCP transport carries JSON-RPC messages between a client and a server. The standard choices are stdio for local process communication and Streamable HTTP for remote services. The transport changes how the server is launched, secured, scaled, and monitored, but it does not change the meaning of tools, resources, prompts, or capability negotiation."),
      callout("Choose by deployment boundary", "Use stdio when the host can safely manage a local process. Use Streamable HTTP when the server needs to be a remote service with network authentication, independent deployment, and operational scaling."),
      h("stdio for local processes"),
      p("With stdio, the client starts or connects to a server process and exchanges messages over standard input and output. The server must keep stdout reserved for valid protocol messages, while diagnostics belong on stderr. This transport can make local setup simple, but the host still needs to manage process lifetime, permissions, environment variables, and resource limits."),
      h("Streamable HTTP for remote servers"),
      p("Streamable HTTP lets a client communicate with a server endpoint over HTTP. It is a natural fit for a deployed service that needs network authorization, load balancing, independent releases, and centralized monitoring. Teams must design authentication and authorization for the MCP resource, validate origin and host behavior as appropriate, and protect the endpoint from misuse."),
      h("SSE terminology and compatibility"),
      p("Older MCP deployments may use an HTTP plus server-sent events pattern. When maintaining one, confirm the exact protocol revision and compatibility behavior in the official specification and SDK documentation. Do not assume that a transport label alone tells you which session, streaming, or authorization features are available."),
      h("Transport-aware monitoring"),
      ul([
        "For stdio, monitor process exits, stderr diagnostics, startup time, and message validity.",
        "For HTTP, monitor authentication outcomes, connection status, request duration, response semantics, and downstream dependencies.",
        "For both, monitor initialization, discovery, tool calls, application errors, and explicit workflow outcomes.",
        "Keep payload capture minimized and ensure telemetry cannot block the tool path.",
      ]),
      h("Transport is not the whole health story"),
      p("An HTTP endpoint can be reachable while initialization fails. A stdio process can stay alive while a tool returns an application error. Reliable MCP monitoring follows the protocol lifecycle and user path, not only the socket or process."),
      faq("Is stdio suitable for production?", "It can be suitable for local or tightly managed deployments. The right choice depends on the host, security model, process management, and operational requirements."),
      faq("Does Streamable HTTP automatically solve authentication?", "No. The transport provides a network communication mechanism. The server still needs an appropriate authentication and authorization design."),
      links([
        { label: "Official MCP transports specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/transports" },
        { label: "Remote MCP OAuth guide", href: "/blog/remote-mcp-oauth-guide" },
        { label: "OpenTelemetry for MCP servers", href: "/blog/opentelemetry-for-mcp-servers" },
      ]),
    ],
  },
  {
    slug: "how-to-build-an-mcp-server",
    title: "How to Build an MCP Server: A Production-Minded Starting Guide",
    tag: "MCP fundamentals",
    excerpt:
      "A practical starting guide to building an MCP server, from choosing a narrow capability surface to schemas, authorization, testing, and production telemetry.",
    date: "Sep 12, 2026",
    read: "8 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "how to build an MCP server",
      "build MCP server",
      "MCP server tutorial",
      "MCP server development",
      "MCP server SDK",
    ],
    related: ["what-is-an-mcp-server", "mcp-tools-resources-prompts", "mcp-tool-schemas", "mcp-server-security-checklist", "instrument-your-mcp-server-in-one-line"],
    body: [
      p("Building an MCP server starts with a small, well-defined capability surface. The best first server does not expose an entire database or internal API. It exposes a few useful tools or resources with clear schemas, predictable errors, explicit permissions, and tests that reflect how a real client will use them."),
      callout("Start with the contract", "Write down the tool name, description, input schema, authorization rule, side effects, timeout, and expected result before wiring the server to a backend."),
      h("1. Choose one useful job"),
      p("Pick a narrow task that a host application can ask for repeatedly, such as searching approved records, reading a project status, or creating a draft. Define what success means and which actions must require confirmation. A narrow job makes schema design and testing more honest."),
      h("2. Select the SDK and transport"),
      p("Use an official or well-supported MCP SDK for your language, then choose stdio for a local process or Streamable HTTP for a remote service. Keep transport setup separate from domain logic so the server can be tested without requiring a full host application."),
      h("3. Define primitives and schemas"),
      p("Expose a tool when the server performs an action or computation. Expose a resource when the client needs addressable context. Use prompts for reusable user-selected templates. For each tool, write descriptions that explain when to use it, what the inputs mean, what it changes, and how failures are reported."),
      h("4. Enforce safety at the server boundary"),
      ul([
        "Validate every argument on the server, even if the client supplied a schema.",
        "Authorize the actual action and resource, not just the presence of a client connection.",
        "Use least privilege and separate read, write, destructive, and administrative operations.",
        "Set deadlines and bound result size, concurrency, and downstream work.",
        "Never log bearer tokens, credentials, or raw sensitive payloads by default.",
      ]),
      h("5. Test the real failure modes"),
      p("Test initialization, capability negotiation, discovery, valid calls, missing arguments, invalid values, authorization failures, timeouts, downstream errors, and malformed results. Include a test that distinguishes a valid MCP response containing an application error from a transport-level failure."),
      h("6. Add production observability"),
      p("Track connection and initialization health, tool usage, observed latency, error state, session behavior, and explicit workflow outcomes. Record the client and tool dimensions needed for decisions, but keep telemetry bounded. TrackMCP's SDK wraps the existing MCP server at the server boundary so teams can add MCP-aware measurement without rewriting the server's domain logic."),
      h("A sensible first release"),
      ul([
        "One narrow read-only tool or resource with a stable schema.",
        "Clear authorization and a documented data boundary.",
        "Unit and integration tests for success and failure paths.",
        "A timeout and redacted structured telemetry path.",
        "A runbook for discovery failures, tool errors, and downstream outages.",
      ]),
      faq("Should an MCP server expose one tool or many?", "Start with the smallest useful surface. Add capabilities when they have a clear user job, stable contract, and tested permission model."),
      faq("Do I need to build a new backend for MCP?", "No. An MCP server can wrap existing services, APIs, or databases. MCP defines the interface exposed to the host application."),
      links([
        { label: "Official MCP quickstart", href: "https://modelcontextprotocol.io/quickstart/server" },
        { label: "MCP server security checklist", href: "/blog/mcp-server-security-checklist" },
        { label: "Instrument your MCP server", href: "/blog/instrument-your-mcp-server-in-one-line" },
      ]),
    ],
  },
  {
    slug: "how-to-connect-an-mcp-server-to-an-ai-client",
    title: "How to Connect an MCP Server to an AI Client",
    tag: "MCP fundamentals",
    excerpt:
      "Learn the general process for connecting an MCP server to an AI client, including transport configuration, initialization, permissions, discovery, and testing.",
    date: "Sep 12, 2026",
    read: "7 min read",
    updated: "Sep 12, 2026",
    verified: "Sep 12, 2026",
    keywords: [
      "connect MCP server to AI client",
      "MCP client setup",
      "MCP server connection",
      "configure MCP server",
      "MCP integration guide",
    ],
    related: ["what-is-an-mcp-client", "what-is-an-mcp-server", "mcp-transports-stdio-and-streamable-http", "remote-mcp-oauth-guide"],
    body: [
      p("Connecting an MCP server to an AI client means configuring a host application to create a client connection, start or reach the server, complete initialization, and expose the server's negotiated capabilities to the user or model. The exact settings differ by host, but the protocol steps are consistent."),
      callout("Before you connect", "Know whether the server is local or remote, which transport it uses, which credentials it expects, and which tools are safe to expose to the host."),
      h("1. Confirm the server contract"),
      p("Read the server's setup documentation and identify its transport, command or URL, environment variables, authentication method, protocol version, and expected capabilities. Check that the server's tools and resources match the job you intend to give the AI client."),
      h("2. Configure the host"),
      p("A host typically needs a server entry containing either a local command and arguments or a remote endpoint and authorization settings. Store credentials in the host's secret mechanism rather than in a checked-in configuration file. Use the least privileged account that can complete the intended task."),
      h("3. Complete initialization"),
      p("When the host starts the connection, its MCP client sends initialize and the server responds with a protocol version and capabilities. The client then sends initialized. If this step fails, a tool catalog will not be trustworthy because normal operations have not begun."),
      h("4. Inspect discovery"),
      p("Ask the client to list the available tools, resources, or prompts. Review names, descriptions, argument schemas, and side effects before enabling actions. A tool that is technically callable may still be inappropriate for an untrusted workflow or broad user group."),
      h("5. Run a small test"),
      ul([
        "Start with a read-only tool or resource and a known-safe input.",
        "Confirm the server receives the expected request and returns the expected result.",
        "Test an invalid input and verify that the error is clear and bounded.",
        "Test the authentication failure path without exposing the credential in logs.",
        "Check the client and server logs for transport noise versus actual MCP errors.",
      ]),
      h("6. Monitor the connection in use"),
      p("Once a connection works, observe initialization, discovery, tool calls, duration, result state, retries, and session behavior. TrackMCP can show the server-side part of that path. The host may still contain context and model events that never cross the server boundary."),
      h("When a connection fails"),
      p("Classify the first broken step. A process launch failure is different from invalid JSON-RPC, a protocol version mismatch, an authentication rejection, a missing capability, a schema error, a downstream outage, or a tool result with isError set. Fixing the wrong layer often creates a second problem without explaining the first one."),
      faq("Can any AI client connect to any MCP server?", "Compatibility depends on the host's MCP support, the server's protocol version and capabilities, the transport, and the authentication and permission model."),
      faq("Should I enable every tool after connecting?", "No. Review the tool catalog and expose only the capabilities needed for the intended workflow. Start with read-only access where possible."),
      links([
        { label: "Official MCP client architecture", href: "https://modelcontextprotocol.io/specification/2025-11-25/architecture" },
        { label: "MCP transports explained", href: "/blog/mcp-transports-stdio-and-streamable-http" },
        { label: "Remote MCP OAuth guide", href: "/blog/remote-mcp-oauth-guide" },
      ]),
    ],
  },
  {
    slug: "best-mcp-observability-tools-for-production-servers",
    title: "Best MCP Observability Tools for Production Servers",
    tag: "MCP observability",
    excerpt:
      "A practical comparison of MCP server observability tools, from server-boundary analytics to broader agent traces, infrastructure telemetry, and OpenTelemetry stacks.",
    date: "Sep 7, 2026",
    read: "10 min read",
    updated: "Sep 7, 2026",
    verified: "Sep 7, 2026",
    keywords: [
      "MCP observability",
      "MCP analytics",
      "MCP server monitoring",
      "MCP telemetry",
      "MCP trace explorer",
      "MCP production monitoring",
      "AI observability tools",
      "LLM observability",
      "agent observability",
    ],
    related: [
      "mcp-observability-guide",
      "mcp-server-analytics-guide",
      "how-to-monitor-an-mcp-server-in-production",
      "mcp-server-slos",
      "opentelemetry-for-mcp-servers",
    ],
    body: [
      p("MCP observability is the practice of understanding what happens when an AI client connects to an MCP server, discovers its capabilities, calls a tool, and either continues or stops. It is narrower than general LLM observability and broader than an HTTP uptime check. The useful question is not only whether the endpoint responded, but whether the server exposed the right catalog, handled the call, returned an application-level result, and gave the workflow a chance to finish."),
      callout("Quick answer", "For MCP server owners, start with a tool-aware server-boundary layer. Add broader LLM traces, infrastructure monitoring, and OpenTelemetry when you need visibility outside the server process. The best stack is often complementary, not one universal replacement."),
      p("This comparison reflects publicly documented capabilities reviewed on September 7, 2026. MCP specifications, SDKs, and product integrations are moving quickly, so verify the current documentation and supported versions before making a production decision."),
      h("What MCP server observability should measure"),
      p("An MCP server sits between a client and the tools, resources, or prompts that the server exposes. A production view should make the protocol boundary legible without claiming access to private model reasoning or every event in the host application."),
      ul([
        "Connections, transport, protocol version, and available client metadata.",
        "Catalog discovery, tool names, descriptions, schemas, and catalog changes.",
        "Tool calls, observed duration, result state, and application errors inside successful transport responses.",
        "Sessions, retries, repeated calls, and ordered server-boundary events when the product has enough evidence to group them.",
        "Explicit workflow outcomes when the application emits them, kept separate from the weaker signal of a successful tool response.",
        "Redaction, payload limits, access controls, and failure behavior for the telemetry system itself.",
      ]),
      h("MCP observability versus broader LLM observability"),
      p("Broader LLM and agent observability platforms usually trace model calls, prompts, retrieval, tool use, feedback, evaluations, token usage, and application spans. That is valuable when the question is why an agent chose a path or whether a response was good. MCP server observability starts at the other side of that boundary: what the server actually received, what it exposed, what it executed, and what it returned."),
      p("The difference matters because a server can receive a perfectly valid tool call without seeing the model's hidden reasoning, the full user prompt, the provider's internal behavior, or the final answer. A server-side trace is evidence about the server path, not proof of the complete agent run."),
      h("How the main options fit"),
      h("TrackMCP"),
      p("TrackMCP is designed for teams that own and operate MCP servers. Its SDK wraps an existing server at the server boundary and records the MCP-specific signals available there: clients, catalog and schema context, tool usage, observed latency, application-level errors, sessions, and explicit workflow outcomes. Telemetry is bounded and redacted, with metadata-only capture available when arguments and results should not leave the process."),
      p("TrackMCP is a good fit when the decision is about server adoption, tool reliability, client behavior, or where an observed workflow stopped. It does not claim to see private reasoning, hidden prompts, provider-side token costs, or every client-side event. It is an MCP server observability layer, not a replacement for general infrastructure monitoring or a complete LLM trace."),
      links([
        { label: "TrackMCP MCP observability", href: "/mcp-observability" },
        { label: "TrackMCP documentation", href: "/docs" },
        { label: "MCP analytics comparison", href: "/mcp-server-analytics/compare" },
      ]),
      h("Sentry"),
      p("Sentry documents generally available MCP monitoring for most server-side JavaScript SDK-based MCP servers. Its documented coverage includes transport usage, client activity, tool calls and resources, arguments and results, latency, throughput, and failures. That makes Sentry one of the closest direct alternatives for JavaScript teams that already use Sentry for application errors and performance."),
      p("Sentry is a good fit when error tracking and application performance are the center of the team's workflow, especially when the server runs in a supported JavaScript environment. The public MCP monitoring documentation reviewed here does not establish the same TypeScript and Python SDK parity, MCP-specific workflow-outcome model, or TrackMCP's exact redaction semantics. Treat those as verification questions, not assumptions."),
      links([{ label: "Sentry MCP monitoring documentation", href: "https://sentry.io/changelog/mcp-monitoring---generally-available/" }]),
      h("Datadog"),
      p("Datadog approaches MCP observability as part of a broad monitoring platform. Its documentation covers MCP client and server instrumentation, including server-side initialize and tools/call methods, client metadata, and optional tools/list interception for intent capture. The wider platform connects those signals to APM, logs, metrics, infrastructure, security, and Agent Observability workflows."),
      p("Datadog is a strong fit for organizations that already standardize on Datadog and want MCP data in the same operational system. The tradeoff is product breadth and configuration complexity. Teams evaluating it against TrackMCP should compare the MCP-specific questions they need answered, not only the total number of features in the platform."),
      links([{ label: "Datadog MCP instrumentation documentation", href: "https://docs.datadoghq.com/llm_observability/instrument/auto_instrumentation/" }]),
      h("Grafana and OpenTelemetry"),
      p("Grafana and OpenTelemetry are better understood as an extensible observability stack than as one narrowly defined MCP product. Grafana documents MCP observability dashboards for protocol health, tool performance, sessions, transport, and related signals. Its MCP server documentation also shows Prometheus metrics and OpenTelemetry traces and logs, with transport-specific differences that teams need to understand."),
      p("This option fits teams that want control over collectors, storage, dashboards, and data routing, or that already operate Prometheus, Tempo, Loki, and Grafana. It can provide broad context, but the team owns more of the semantic model, instrumentation, alerting, and maintenance. A productized MCP layer may still complement it when server-specific analytics should be available without assembling every query and panel in-house."),
      links([
        { label: "Grafana MCP observability documentation", href: "https://grafana.com/docs/grafana-cloud/observe-and-act/monitor-applications/ai-observability/mcp-observability/" },
        { label: "Grafana MCP metrics and tracing documentation", href: "https://grafana.com/docs/grafana/latest/developer-resources/mcp/developer/observability-metrics-and-tracing/" },
      ]),
      h("Langfuse"),
      p("Langfuse focuses on LLM and agent application observability, evaluation, prompt management, and related workflows. Its data model includes traces, observations, and sessions, and its documentation covers tool and retrieval steps, OpenTelemetry, token and cost tracking, and linking MCP client and server operations through trace metadata. It also provides an MCP server for querying Langfuse data from compatible assistants."),
      p("Langfuse is a good fit when the central question spans the full AI application, especially model calls, evaluations, prompts, feedback, and self-hosted deployment. It can be complementary to TrackMCP when a team needs both application-level traces and a focused view of what its MCP server actually exposes and receives. The public documentation reviewed here should be used to verify the exact server-side MCP coverage for a specific deployment."),
      links([
        { label: "Langfuse MCP tracing documentation", href: "https://langfuse.com/docs/observability/features/mcp-tracing" },
        { label: "Langfuse MCP server documentation", href: "https://langfuse.com/docs/api-and-data-platform/features/mcp-server" },
      ]),
      h("LangSmith"),
      p("LangSmith is centered on tracing, debugging, evaluating, and monitoring LLM applications and agents. Its official tutorial traces individual model calls and complete application pipelines, records metadata and feedback, and exposes monitoring views for trace count, latency, error rate, feedback, and cost. Teams already building with LangChain may benefit from the native ecosystem integration."),
      p("LangSmith is a good fit when the team needs to understand the agent or application path that leads to an MCP call. It should not automatically be treated as a substitute for MCP server analytics. The public observability documentation reviewed here describes application and agent tracing; verify any MCP server-specific instrumentation separately for the runtime and transport you operate."),
      links([{ label: "LangSmith observability tutorial", href: "https://docs.langchain.com/langsmith/observability-llm-tutorial" }]),
      h("Which tool is right for which team?"),
      ul([
        "Choose TrackMCP when your primary owner is the MCP server team and you need clients, tools, catalog context, server-boundary errors, latency, sessions, and explicit workflow signals in one focused view.",
        "Choose Sentry when application errors and performance are central, your MCP server is in a supported JavaScript environment, and you want MCP signals alongside Sentry's existing workflow.",
        "Choose Datadog when your organization already operates Datadog across infrastructure, APM, logs, security, and agent observability, and MCP data should join that platform.",
        "Choose Grafana and OpenTelemetry when your team wants composable, vendor-neutral instrumentation and is prepared to operate the collection, storage, dashboards, and semantics.",
        "Choose Langfuse when model, prompt, evaluation, feedback, and agent traces are the main unit of investigation, with MCP as one part of the wider application.",
        "Choose LangSmith when your application is closely tied to LangChain or when agent and pipeline tracing is the primary debugging and evaluation workflow.",
      ]),
      h("What to evaluate before production"),
      ul([
        "Boundary coverage: does the tool observe the MCP server, the client, the host application, or only downstream services?",
        "Error semantics: can it distinguish transport status from an application error carried inside a successful tool result?",
        "Dimensions: can you break down behavior by client, tool, protocol version, transport, environment, session, and catalog version?",
        "Outcome semantics: does it distinguish a successful tool response from an explicit workflow completion signal?",
        "Privacy: where are arguments and results redacted, how are limits enforced, and what is the default capture mode?",
        "Operational safety: does telemetry fail open, remain bounded, and avoid blocking the tool call when the observability service is unavailable?",
        "Freshness: which MCP specification and SDK versions are supported, and how quickly does the integration track protocol changes?",
      ]),
      h("The practical answer is usually a layered stack"),
      p("MCP server observability and broader AI observability answer different questions. A team may use TrackMCP or another MCP-aware layer for server adoption and protocol behavior, Sentry or Datadog for application and infrastructure operations, and Langfuse, LangSmith, or an OpenTelemetry stack for broader agent traces and evaluations. The right architecture depends on the system boundary, data policy, and decisions the team needs to make."),
      p("Start with one production server and one representative workflow. Confirm that the data answers a real debugging or product question, inspect what is redacted, and record what remains unknown. That evaluation is more useful than choosing a winner from a generic feature-count table."),
      links([
        { label: "Start the MCP observability quickstart", href: "/mcp-server-analytics/quickstart" },
        { label: "Read how to monitor an MCP server in production", href: "/blog/how-to-monitor-an-mcp-server-in-production" },
        { label: "Read the MCP observability guide", href: "/blog/mcp-observability-guide" },
        { label: "Explore TrackMCP", href: "/track-mcp" },
        { label: "Open the TrackMCP dashboard", href: "https://app.trackmcp.com/dashboard" },
      ]),
    ],
  },
  {
    slug: "trackmcp-foundation-release",
    title: "TrackMCP Foundation Release: Safer, More Honest MCP Observability",
    tag: "Foundation Release",
    section: "releases",
    excerpt:
      "TrackMCP's Foundation Release adds bounded redacted telemetry, observed tool latency, authenticated server-boundary traces, and clearer MCP analytics semantics.",
    date: "Sep 6, 2026",
    read: "8 min read",
    updated: "Sep 6, 2026",
    keywords: [
      "MCP observability",
      "MCP analytics",
      "MCP server monitoring",
      "MCP trace explorer",
      "MCP telemetry",
      "MCP privacy",
      "MCP latency",
      "MCP tool analytics",
      "MCP production monitoring",
    ],
    related: [
      "mcp-server-analytics-guide",
      "mcp-observability-guide",
      "how-to-monitor-an-mcp-server-in-production",
      "mcp-tool-schemas",
      "mcp-server-slos",
    ],
    body: [
      p("Today we are releasing the TrackMCP Foundation Release, the first production foundation for observing MCP servers with useful boundaries and honest semantics. The release is live on AWS App Runner at app.trackmcp.com. It gives MCP server teams a reliable way to measure what crosses their server boundary: which clients connect, which tools are discovered and called, how long calls take, where errors appear and whether retry metadata is available on events, and which explicit application-emitted workflow outcomes are recorded."),
      p("The important part is not a bigger pile of telemetry. It is making the telemetry consistent, bounded, and clear about what it does not know. TrackMCP is designed for teams operating MCP servers in production, where a successful HTTP response can still hide a tool error, a retry loop, or an unfinished workflow."),
      callout("The short version", "TrackMCP observes the MCP server boundary. It does not claim to see a host's private model reasoning, every client-side turn, or whether a final answer was correct."),
      h("What shipped today"),
      h("A canonical MCP event contract"),
      p("The Foundation Release introduces a versioned canonical event contract with schema version 1. Legacy events are normalized into that shape where possible, but they are not rewritten into history they never contained. Older events can still be missing modern metadata. The contract gives new events stable IDs, validated event types, required-string and timestamp checks, and explicit limits for payloads and batches."),
      ul([
        "Invalid negative measurements are rejected instead of entering analytics as plausible-looking values.",
        "Duplicate events are handled idempotently, so retries do not silently inflate calls, errors, or latency samples.",
        "Payload and batch limits are enforced before ingestion accepts data.",
        "Retry metadata is available when an event includes it; the product does not infer a retry loop from arbitrary traffic.",
        "Event meanings stay consistent across the SDKs, ingestion, traces, and dashboard calculations.",
      ]),
      p("This is the foundation for trustworthy MCP analytics. A p95 number is only useful when the events contributing to it mean the same thing, and a trace is only useful when duplicate delivery does not create a second fictional call."),
      h("TypeScript and Python SDK parity"),
      p("The TypeScript and Python SDKs now provide matching server-boundary behavior. Each wraps an existing MCP server at the server boundary, so teams do not have to rewrite individual tool handlers. Telemetry is sent asynchronously and remains fail-open: if TrackMCP is unavailable, the MCP call can continue while delivery is retried within bounded queue limits."),
      code(`import { withTrackMCP } from "@trackmcp/sdk";
import { server } from "./mcp";

export default withTrackMCP(server, {
  apiKey: process.env.TRACKMCP_KEY,
  service: "my-mcp-server",
});`),
      code(`import os
from trackmcp import with_trackmcp
from mcp.server import server

app = with_trackmcp(
    server,
    api_key=os.environ["TRACKMCP_KEY"],
    service="acme-mcp-server",
    environment="production",
)`),
      h("Privacy and safety controls"),
      p("Telemetry is privacy-sensitive by default because tool arguments and results can contain customer data, credentials, queries, or documents. The default is redacted payload mode, with metadata-only capture available when arguments and results must not be collected. Full payload capture is opt-in and still bounded. Sanitization happens in the SDK before data leaves the server process, and ingestion applies a second defensive limit."),
      ul([
        "Sensitive keys are scrubbed recursively, including dotted paths, bearer tokens, credentialed URLs, binary and data URIs, and base64-like values.",
        "Depth, breadth, string length, and serialized byte limits prevent one event from becoming an unbounded payload.",
        "The SDK payload budget is about 32 KiB, with a maximum depth of 6, up to 50 keys or items per container, and strings capped at 2,048 characters.",
        "Failed deliveries are requeued in a bounded queue of 500 events or 2 MiB, with the oldest events dropped when the queue is full.",
        "Redaction hooks can mutate or drop events. A hook failure is isolated so telemetry remains fail-open and tool execution is not blocked.",
      ]),
      h("Metadata that helps explain behavior"),
      p("When the handshake or discovery flow exposes it, TrackMCP records client name and version, protocol metadata, tool descriptions, and stable hashes. It also records transport, session, and provenance details where available. These fields make it possible to compare behavior across clients and deployments without pretending that a server-boundary event contains the host's private context."),
      h("What this lets MCP teams do"),
      h("Measure real latency and reliability"),
      p("The dashboard exposes tool-level p50 and p95 latency from event durations, alongside trace and session context and the client or server metadata available for the selected data. These are measurements from the calls TrackMCP received, not fabricated estimates or sample marketing numbers. Teams can investigate a slow tool in the context of the server-boundary events around it and the workflow paths it affects."),
      h("Separate activity from useful outcomes"),
      p("The Foundation Release distinguishes tool responses from explicit application-emitted workflow outcomes. A successful tool response is evidence about that call, not proof that the user's task succeeded. When an application emits a completed or failed workflow event, TrackMCP can report that stronger signal. When there is not enough data, the dashboard shows a missing or not-applicable state rather than inventing a conclusion."),
      h("Explore bounded, authenticated traces"),
      p("The Trace Explorer provides an ordered view of server-boundary events for an authenticated workspace. It can show protocol and tool events, catalog changes, workflow signals, errors, durations, available client metadata, correlation quality, completion source, and redaction or truncation counts. Session filters are preserved, and the response stays bounded for safe exploration."),
      callout("What a trace means", "A TrackMCP trace is a bounded, redacted view of events observed at your MCP server boundary. It is an investigation aid, not unrestricted raw replay or a universal agent trace."),
      h("Privacy is a product feature"),
      p("The safest telemetry is telemetry that has a clear purpose and a clear boundary. TrackMCP therefore makes metadata-only capture available when arguments and results should never be collected, keeps redacted payload mode as the default, and treats full mode as an explicit choice that remains subject to the same bounds. The SDK also supports additional redaction paths and keys for application-specific data."),
      p("These controls reduce exposure, but no telemetry system can promise that arbitrary application data contains no personal or confidential information. Teams still own their data classification, retention, access policy, and choice of payload mode."),
      h("What TrackMCP still cannot see"),
      p("The Foundation Release is intentionally a server-boundary product. It observes what reaches the instrumented MCP server, not everything that happens around an agent. In particular, TrackMCP does not currently provide:"),
      ul([
        "Private model reasoning, hidden chain-of-thought, system prompts, or host-side turns that never reach the server.",
        "A complete client or desktop-host view when the client does not expose its own events to the server.",
        "A guarantee that the model selected the best tool or that a final answer was correct.",
        "Unrestricted raw session replay or a proof of every step in an agent workflow.",
        "Automatic historical metadata that was not present in a legacy event.",
      ]),
      p("This boundary is a product constraint and a safety property. It keeps the current claims testable and gives teams a useful observability layer without requiring access to private model or user context."),
      h("How to start"),
      p("Create or open a TrackMCP workspace, create an API key, install the SDK for your runtime, and wrap the existing server. Keep the key in the server environment. The first useful signal is usually a small set of production calls from one service, followed by a check of tool errors, observed latency, client metadata, and any explicit workflow outcomes."),
      links([
        { label: "TrackMCP documentation", href: "/docs" },
        { label: "TypeScript SDK guide", href: "/docs/typescript" },
        { label: "Python SDK guide", href: "/docs/python" },
        { label: "Configuration reference", href: "/docs/reference" },
        { label: "REST API and trace response", href: "/docs/api" },
        { label: "Open the authenticated dashboard", href: "https://app.trackmcp.com/dashboard" },
      ]),
      h("What comes next"),
      p("The Foundation Release is the boundary, contract, privacy, and truth layer. The following items are future roadmap work, not capabilities shipped in this release:"),
      ul([
        "Explicit correlation handles that connect server events to an application job when a deployment opts in safely.",
        "Intent signals and richer tool-quality analytics for evaluating selection and argument behavior.",
        "Client-side MCP observation where an appropriate client integration is available.",
        "Alerts and additional operational workflows built on the canonical event model.",
      ]),
      p("We will keep separating measured behavior from inference as those capabilities develop. For now, the Foundation Release is available for teams that want a safer and more honest starting point for MCP production monitoring."),
      h("Frequently asked questions"),
      faq("What does TrackMCP observe?", "TrackMCP observes events at an instrumented MCP server boundary, including available client and protocol metadata, tool discovery and calls, durations, errors, sessions, and explicit application-emitted workflow outcomes."),
      faq("Does TrackMCP capture tool arguments and results by default?", "The default is redacted payload mode. Metadata-only capture is available when payloads should not be collected. Full mode is opt-in and remains bounded and subject to redaction controls."),
      faq("Are p50 and p95 latency values estimates?", "They are calculated from observed event durations for the selected data. TrackMCP does not fill missing measurements with fabricated values; insufficient data remains missing or not applicable."),
      faq("Does a successful tool call mean the agent completed its task?", "No. A successful tool response describes that call. A stronger completion signal comes from an explicit application-emitted workflow outcome, and even that signal does not tell TrackMCP whether an answer was correct."),
      faq("Can anyone access Trace Explorer?", "No. Trace Explorer is authenticated and workspace-scoped. It is available to signed-in users with the required dashboard or API authorization, and it returns bounded trace data rather than unrestricted replay."),
      faq("How do I install TrackMCP?", "Install the TypeScript package @trackmcp/sdk or the Python package trackmcp, then wrap your existing MCP server and provide TRACKMCP_KEY. The SDK documentation includes runtime-specific options and redaction examples."),
      links([
        { label: "Read the MCP server analytics guide", href: "/blog/mcp-server-analytics-guide" },
        { label: "Read the MCP observability guide", href: "/blog/mcp-observability-guide" },
        { label: "Start with the TrackMCP docs", href: "/docs" },
      ]),
    ],
  },
  {
    slug: "mcp-incident-response-runbook",
    title: "MCP Server Incident Response Runbook",
    tag: "Engineering",
    excerpt:
      "A practical runbook for diagnosing MCP outages, authorization failures, tool errors, retry loops, latency regressions, and incomplete agent workflows.",
    date: "Sep 4, 2026",
    read: "9 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP incident response", "MCP debugging", "MCP server outage", "MCP reliability runbook", "MCP troubleshooting"],
    related: ["how-to-monitor-an-mcp-server-in-production", "mcp-observability-guide", "mcp-server-slos"],
    body: [
      p("An MCP incident is any production condition that prevents supported clients from discovering, authorizing, calling, or completing important work through an MCP server. Use this runbook to classify the failure, isolate the first broken step, mitigate user impact, and add a regression check before closing the incident."),
      callout("Start with the user path", "Do not begin with the largest log file. Find whether the failure is transport, authorization, catalog, tool execution, dependency, or workflow outcome related."),
      h("1. Confirm the blast radius"),
      ul([
        "Which server, environment, region, and deployment version are affected?",
        "Which clients, tenants, users, tools, and workflows are affected?",
        "Did the condition begin after a deployment, schema change, credential change, dependency incident, or client update?",
        "Is the issue a complete outage, a degraded path, or a low-volume isolated failure?",
      ]),
      h("2. Classify the first failing layer"),
      ul([
        "Transport: the endpoint cannot be reached or returns unexpected protocol responses.",
        "Authorization: clients receive 401, 403, discovery, issuer, audience, or scope errors.",
        "Catalog: expected tools, resources, or prompts are missing, renamed, stale, or malformed.",
        "Tool execution: a handler, schema, dependency, timeout, or tool-level error is failing.",
        "Workflow: individual calls succeed but the agent retries, stalls, or never reaches the outcome.",
      ]),
      h("3. Inspect the first broken event"),
      p("Reconstruct one affected session or trace. Identify the last successful event, the first failure, and what the client did next. Compare the client, tool, method, server version, environment, duration, outcome class, and deployment timestamp. A later error may be a consequence of the first one."),
      h("4. Apply the smallest safe mitigation"),
      ul([
        "Rollback the deployment if a recent release changed the failing path.",
        "Disable or gate one broken tool if the rest of the catalog is healthy.",
        "Rate-limit a retry loop before it exhausts dependencies or increases cost.",
        "Restore or rotate credentials without exposing them to logs or traces.",
        "Use a dependency fallback or maintenance response when the upstream service is unavailable.",
        "Communicate affected clients, expected impact, and the next update time.",
      ]),
      h("5. Investigate common MCP failure patterns"),
      ul([
        "A green HTTP status with a tool-level error: parse the result and inspect the error state.",
        "Repeated identical arguments: inspect schema shape and error recovery guidance.",
        "High p95 with normal average latency: find the slow dependency behind the tail.",
        "One client failing: compare protocol version, catalog cache, authorization, and argument behavior.",
        "Connected sessions with no tool calls: inspect discovery, descriptions, and client permissions.",
      ]),
      h("6. Verify recovery"),
      p("Run a clean check through discovery, authorization, a critical tool, and a representative workflow. Confirm that the original symptom is gone, the mitigation did not introduce a security or catalog regression, and new telemetry shows healthy behavior for the affected client and tool."),
      h("7. Close with a durable fix"),
      ul([
        "Write a timeline with exact timestamps and versions.",
        "Record the root cause separately from contributing conditions.",
        "Add a unit, integration, synthetic, schema, or client-compatibility regression test.",
        "Update the alert or dashboard that should have shortened detection.",
        "Document any catalog, auth, transport, or client migration required.",
        "Review whether sensitive data appeared in logs, traces, or incident artifacts.",
      ]),
      h("Frequently asked questions"),
      faq("What is the first step in an MCP incident?", "Confirm the affected server, environment, clients, tools, workflows, and deployment timeline, then classify which layer failed first: transport, authorization, catalog, tool execution, dependency, or outcome."),
      faq("Why inspect the first failing event?", "Later errors can be consequences of an earlier catalog, authorization, schema, or dependency failure. The first broken event usually gives the shortest path to the root cause."),
      faq("Should I page on every MCP tool error?", "No. Page on user-visible outages, critical workflow failures, and sustained critical-tool degradation. Keep isolated, low-volume, or experimental-tool errors as investigation signals."),
      links([
        { label: "MCP production monitoring guide", href: "/blog/how-to-monitor-an-mcp-server-in-production" },
        { label: "MCP observability guide", href: "/blog/mcp-observability-guide" },
        { label: "MCP server SLOs", href: "/blog/mcp-server-slos" },
      ]),
    ],
  },
  {
    slug: "mcp-token-passthrough-security",
    title: "MCP Token Passthrough: Why It Is Dangerous and What to Do Instead",
    tag: "Security",
    excerpt:
      "Why an MCP server should not forward client tokens to downstream APIs, and how to preserve resource boundaries, authorization, and accountability.",
    date: "Sep 4, 2026",
    read: "8 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP token passthrough", "MCP confused deputy", "MCP token security", "MCP OAuth security", "MCP authorization"],
    related: ["mcp-server-security-checklist", "remote-mcp-oauth-guide", "mcp-authorization-errors"],
    body: [
      p("MCP token passthrough is the anti-pattern of accepting an access token from an MCP client without validating that it was issued for the MCP server, then forwarding that token to a downstream API. It breaks resource boundaries, weakens auditability, and can turn the MCP server into a confused deputy."),
      callout("The rule", "Validate the client token for the MCP resource. If the server needs to call another service, use a separate downstream credential or an explicit delegated exchange."),
      h("Why token passthrough creates risk"),
      ul([
        "Security controls can be bypassed when a token intended for another service is accepted here.",
        "The downstream API may trust the wrong audience or assume the token was already validated.",
        "Operators cannot reliably distinguish the MCP client from the upstream token’s identity.",
        "A stolen token can be replayed through the MCP server as a data-exfiltration proxy.",
        "The server becomes responsible for a credential whose audience and privileges it did not control.",
      ]),
      h("The safe trust-boundary pattern"),
      p("The MCP server acts as its own protected resource. It validates the inbound token’s issuer, audience, expiry, signature, scopes, and subject or tenant policy before processing the tool call. When it calls a downstream API, it authenticates to that API as a separately authorized client or performs a documented token exchange."),
      h("What to validate before a tool runs"),
      ul([
        "The token is presented in the Authorization header, not the query string.",
        "The token was issued by an accepted authorization server.",
        "The token is intended for this MCP server as its audience.",
        "The token is not expired, revoked, or malformed.",
        "The subject has the required tenant, role, and tool scope.",
        "The requested operation is allowed for this identity and environment.",
      ]),
      h("What to do when calling a downstream API"),
      ul([
        "Use a server credential with the minimum downstream scope.",
        "Use a user-bound delegated credential only when the downstream authorization model explicitly supports it.",
        "Store downstream tokens server-side with encryption and access controls.",
        "Bind stored credentials to the authenticated user or tenant.",
        "Never return downstream credentials to the MCP client or include them in tool results.",
        "Record the authorization decision and downstream correlation ID without logging secrets.",
      ]),
      h("How to detect passthrough in a review"),
      ul([
        "Search for code that copies the inbound Authorization header to an upstream request.",
        "Check whether the server validates audience before dispatching a tool.",
        "Inspect whether downstream requests use the same bearer token as the MCP request.",
        "Test a token issued for another service and verify that the MCP server rejects it.",
        "Check whether logs can distinguish the MCP subject from the downstream service identity.",
      ]),
      h("Frequently asked questions"),
      faq("What is MCP token passthrough?", "It is forwarding a client-provided access token to a downstream service without validating that the token was issued for the MCP server and without establishing a separate downstream trust relationship."),
      faq("What should an MCP server do instead?", "Validate the inbound token for the MCP resource, enforce local scopes and policy, and use a separately issued downstream credential or explicit delegated exchange for any upstream API call."),
      faq("Why is token passthrough related to the confused deputy problem?", "A downstream service may trust the MCP server while the MCP server forwards a token issued for another audience. The server can then be tricked into using someone else’s authority outside the intended trust boundary."),
      links([
        { label: "MCP security checklist", href: "/blog/mcp-server-security-checklist" },
        { label: "Remote MCP OAuth guide", href: "/blog/remote-mcp-oauth-guide" },
        { label: "MCP security best practices", href: "https://modelcontextprotocol.io/docs/2025-11-25/tutorials/security/security_best_practices" },
        { label: "MCP authorization specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization" },
      ]),
    ],
  },
  {
    slug: "mcp-tool-schemas",
    title: "MCP Tool Schemas: Validation Patterns That Reduce Agent Retries",
    tag: "Engineering",
    excerpt:
      "Design MCP input schemas that are explicit for developers, usable by agents, and resilient to common model-generated argument shapes.",
    date: "Sep 4, 2026",
    read: "10 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP tool schemas", "MCP input schema", "MCP validation", "MCP tool arguments", "agent retries"],
    related: ["measure-mcp-tool-adoption-workflow-completion", "why-agents-fail-on-your-tool-schemas", "writing-tool-descriptions-agents-use"],
    body: [
      p("An MCP tool schema should describe the arguments a tool needs while helping an agent produce a valid call. The best schema is not merely strict; it is explicit, unsurprising, compatible with the model’s likely output, and paired with an error that explains how to recover."),
      h("Schema correctness is not the same as agent usability"),
      p("A schema can be valid JSON Schema and still create repeated failures. Models often infer argument shapes from names, descriptions, examples, and surrounding context. If the schema expects an array while ordinary user intent produces one value, the server should either accept a safe coercion or return a precise correction."),
      h("A practical schema review"),
      ul([
        "Use one clear type for each field unless multiple shapes are deliberately supported.",
        "Mark only genuinely required fields as required.",
        "Give fields descriptions that explain meaning, format, and constraints.",
        "Use examples for identifiers, dates, enums, and nested objects.",
        "Avoid overlapping tools that accept nearly identical arguments.",
        "Keep destructive actions separate from read-only actions.",
      ]),
      h("Common mismatch: string versus array"),
      p("A send-email tool may expect a list of recipients while a model produces one string for a one-recipient request. If the intent is unambiguous and coercion is safe, normalize the value before validation or clearly accept both shapes. If accepting both could hide a mistake, reject it with a correction that names the field and expected shape."),
      h("Make validation errors useful to the agent"),
      p("An error should name the field, expected shape, received shape, and next action. “Invalid input” forces another guess. “The recipient field must be an array of email strings; received one string. Wrap the address in an array and retry” gives the agent a path to recovery."),
      h("Compatibility-friendly changes"),
      ul([
        "Adding an optional field is usually safer than making a field newly required.",
        "Keep old enum values during a deprecation window when clients may cache schemas.",
        "Version or alias a tool before changing the meaning of an existing argument.",
        "Record catalog and schema versions so failures can be tied to a deployment.",
        "Test the same intent across the clients and models you actually support.",
      ]),
      h("Measure whether a schema change worked"),
      p("Record the schema version, deployment time, affected tool, client, success rate, validation errors, retries, and workflow completion before and after the change. A rise in call volume is not enough; the change should reduce invalid calls or improve completion without increasing harmful actions."),
      h("A small regression fixture"),
      p("Create cases for one value, many values, missing required input, an invalid enum, and a malformed identifier. Assert the expected validation result, recovery behavior, and tool outcome for each case. Keep the fixture versioned with the server."),
      h("Frequently asked questions"),
      faq("What is an MCP tool schema?", "An MCP tool schema describes the input arguments a client can send to a tool, including types, required fields, constraints, and field-level meaning."),
      faq("Should MCP schemas be strict or forgiving?", "They should be explicit and as strict as the safety requirement demands. Safe, unambiguous coercion can reduce retries, while dangerous or ambiguous inputs should fail with an actionable error."),
      faq("How do I reduce MCP tool retries?", "Find repeated calls with similar arguments, inspect validation errors, make safe argument shapes easier to produce, and return errors that tell the agent exactly what to change."),
      links([
        { label: "MCP tools specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/server/tools" },
        { label: "Measure MCP tool adoption", href: "/blog/measure-mcp-tool-adoption-workflow-completion" },
        { label: "Why agents fail on tool schemas", href: "/blog/why-agents-fail-on-your-tool-schemas" },
      ]),
    ],
  },
  {
    slug: "mcp-server-slos",
    title: "MCP Server SLOs: What to Measure Beyond Uptime",
    tag: "Product",
    excerpt:
      "A practical service-level objective framework for MCP availability, discovery, tool success, latency, authorization, and workflow completion.",
    date: "Sep 4, 2026",
    read: "9 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP SLO", "MCP reliability", "MCP server SLOs", "MCP uptime", "MCP workflow reliability"],
    related: ["how-to-monitor-an-mcp-server-in-production", "mcp-observability-guide", "mcp-server-analytics-guide"],
    body: [
      p("An MCP server SLO should measure whether supported clients can complete important work, not only whether an endpoint responds. A useful starting model covers availability, discovery, critical tool success, tail latency, authorization, and workflow completion, with each objective tied to an owner and an action."),
      callout("Do not copy targets blindly", "There is no universal MCP SLO number. Start with a measured baseline, define the user promise, and set targets per environment and workflow criticality."),
      h("The six SLO layers"),
      ul([
        "Availability: the remote endpoint or process is reachable.",
        "Discovery: clients can retrieve the expected catalog and metadata.",
        "Authorization: supported identities can authenticate and unauthorized actions are denied.",
        "Tool success: critical tools return an acceptable result.",
        "Latency: critical tools and workflows stay within a useful response budget.",
        "Outcome: representative workflows reach their defined terminal result.",
      ]),
      h("Define the numerator and denominator"),
      p("Every SLO needs a precise eligible-event definition. For example, critical-tool success can be successful tool results divided by eligible tool calls, excluding intentionally rejected requests only when that exclusion is documented. Workflow completion can be completed workflows divided by started workflows, with synthetic traffic labeled separately."),
      h("Use p95 for the tail"),
      p("Average latency hides the calls that agents time out on. Track p50 for the typical call and p95 for the tail, per critical tool and, where possible, per workflow. A 300 millisecond tool called five times can create a very different user experience from one called once."),
      h("Error budgets for MCP"),
      p("An error budget converts a reliability target into a release and investigation policy. If a critical workflow burns its budget, pause risky catalog or dependency changes, investigate the first failing step, and prioritize reliability work. Keep low-value experimental tools from dominating the budget for a critical workflow."),
      h("Alert on a user-visible condition"),
      ul([
        "Page when the endpoint is unavailable or a critical workflow cannot complete.",
        "Warn when p95 latency, retries, or tool errors trend beyond the baseline.",
        "Investigate catalog drift, unused tools, or isolated low-volume failures without paging.",
        "Break every alert down by client, tool, environment, version, and deployment.",
      ]),
      h("A starting dashboard"),
      ul([
        "Availability and authorization success over time.",
        "Discovery success and catalog changes by client.",
        "Critical tool success, p50, p95, and timeout rate.",
        "Workflow starts, completions, failures, and retries.",
        "Budget consumed, active incidents, and recent deployments.",
      ]),
      h("Frequently asked questions"),
      faq("What is an MCP SLO?", "An MCP SLO is a measurable reliability objective for an MCP server, such as availability, discovery success, critical-tool success, latency, authorization, or workflow completion."),
      faq("Is MCP uptime enough?", "No. An endpoint can be available while discovery is broken, tools are failing inside successful responses, agents are retrying, or workflows are not completing."),
      faq("Which MCP latency percentile should I track?", "Track at least p50 and p95 per important tool. Use p50 to understand typical behavior and p95 to expose the slow tail that can cause agent timeouts and compounded workflow delay."),
      links([
        { label: "MCP production monitoring guide", href: "/blog/how-to-monitor-an-mcp-server-in-production" },
        { label: "MCP observability guide", href: "/blog/mcp-observability-guide" },
        { label: "TrackMCP REST API", href: "/docs/api" },
      ]),
    ],
  },
  {
    slug: "opentelemetry-for-mcp-servers",
    title: "OpenTelemetry for MCP Servers: What to Instrument and Why",
    tag: "Engineering",
    excerpt:
      "A practical guide to connecting MCP tool calls with traces, metrics, logs, downstream services, and privacy-safe observability.",
    date: "Sep 4, 2026",
    read: "11 min read",
    updated: "Sep 4, 2026",
    keywords: ["OpenTelemetry MCP", "MCP tracing", "MCP metrics", "MCP distributed tracing", "MCP observability"],
    related: ["mcp-observability-guide", "mcp-server-slos", "mcp-server-analytics-guide"],
    body: [
      p("OpenTelemetry can connect an MCP tool call to the downstream APIs, databases, queues, and services that determine its result. The useful design is to keep standard traces, metrics, and logs while adding MCP context such as client, tool, method, transport, outcome class, and correlation identity."),
      h("What belongs in a trace"),
      ul([
        "A server or transport span for the MCP request.",
        "A tool execution span with method, tool name, and outcome class.",
        "Child spans for downstream HTTP, database, queue, or model calls.",
        "A trace or workflow identity that lets operators reconstruct the path.",
        "Deployment, environment, service version, and protocol version.",
      ]),
      h("Recommended MCP dimensions"),
      p("Use low-cardinality attributes for filtering and dashboards. Client name, tool name, method, transport, environment, outcome class, and server version are usually more useful than putting raw arguments into every span."),
      h("Keep high-cardinality data out of default metrics"),
      p("User IDs, URLs, document IDs, free-form queries, and raw arguments can create cardinality and privacy problems. Keep them out of default metric labels. If a debugging trace needs a redacted identifier, put it behind access controls and retention limits."),
      h("Metrics to start with"),
      ul([
        "Tool calls by tool, client, environment, and outcome class.",
        "Tool duration as a histogram by tool and outcome class.",
        "Active workflows or sessions where the application has a meaningful lifecycle.",
        "Retries by tool and client.",
        "Workflow completions by workflow and outcome.",
      ]),
      h("Logs still matter"),
      p("Traces answer where time went. Metrics answer how often a condition occurs. Structured logs preserve the detailed event needed to inspect a failure. Link logs to traces with a correlation ID, redact sensitive values before emission, and record tool-level error state rather than only HTTP status."),
      h("Sampling and fail-open behavior"),
      p("Sample routine success traces when volume is high, but retain enough errors and incomplete workflows to investigate regressions. Bound queues and export timeouts. Observability must not block the tool call or turn an analytics outage into an MCP outage."),
      h("Connect the trace to the outcome"),
      p("A tool span is not the same as a useful result. When the application knows that a ticket was created, a deployment was verified, or a report was delivered, emit an explicit workflow outcome and link it to the trace. That is how infrastructure telemetry becomes product insight."),
      h("Frequently asked questions"),
      faq("Does OpenTelemetry replace MCP analytics?", "OpenTelemetry provides a strong foundation for traces, metrics, and logs. MCP analytics adds protocol- and product-specific views such as client mix, tool adoption, retries, sessions, and workflow outcomes."),
      faq("Should MCP arguments be stored in spans?", "Not by default. Prefer minimized metadata and redact sensitive fields locally. Store payload details only when there is a documented debugging need, access policy, and retention limit."),
      faq("What is the most useful MCP trace attribute?", "Tool name is usually the first useful dimension, followed by client, method, environment, outcome class, protocol or server version, and a correlation identity."),
      links([
        { label: "MCP observability guide", href: "/blog/mcp-observability-guide" },
        { label: "MCP server SLOs", href: "/blog/mcp-server-slos" },
        { label: "OpenTelemetry documentation", href: "https://opentelemetry.io/docs/" },
      ]),
    ],
  },
  {
    slug: "mcp-tool-selection-evaluation",
    title: "MCP Tool Selection Evaluation: A Dataset and Scoring Method",
    tag: "Product",
    excerpt:
      "A reproducible way to measure whether an agent selects the right MCP tool, sends valid arguments, recovers from errors, and completes the workflow.",
    date: "Sep 4, 2026",
    read: "10 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP tool selection", "MCP evaluation", "agent tool evaluation", "MCP tool testing", "MCP benchmark"],
    related: ["mcp-tool-schemas", "measure-mcp-tool-adoption-workflow-completion", "writing-tool-descriptions-agents-use"],
    body: [
      p("MCP tool-selection evaluation measures whether an agent chooses the right tool for a user intent and uses it successfully. Separate tool choice from argument validity, tool execution, recovery, and workflow completion so a single score does not hide where the system failed."),
      h("Build a labeled intent dataset"),
      p("Start with real or carefully constructed user intents that your server is expected to support. For each intent, define one expected tool or an acceptable set of tools, required arguments, disallowed side effects, and the terminal outcome."),
      h("Score the stages separately"),
      ul([
        "Selection accuracy: did the agent choose an acceptable tool?",
        "Argument validity: did the call satisfy the schema?",
        "Execution success: did the tool return an acceptable result?",
        "Recovery: did the agent respond correctly to an error or missing input?",
        "Workflow completion: did the end-to-end task reach its expected outcome?",
        "Safety: did the agent avoid disallowed or destructive tools?",
      ]),
      h("Keep the test conditions fixed"),
      p("Record client, model, tool catalog version, schema version, prompt, system instructions, temperature or determinism settings, server version, and test date. A change in any of these can change the result. If the test is synthetic, say so clearly."),
      h("Use a simple scorecard"),
      ul([
        "Selection accuracy = acceptable tool selections divided by eligible intents.",
        "Argument validity = valid tool calls divided by selected tool calls.",
        "Execution success = successful tool results divided by executed tool calls.",
        "Workflow completion = completed workflows divided by started workflows.",
        "Safety rate = safe runs divided by eligible intents.",
      ]),
      p("Keep the component scores visible. A single composite score can be useful for a trend line, but it should never replace the underlying measures. A server can select correctly and still fail because a dependency is slow; it can also complete a task while selecting a dangerous tool along the way."),
      h("Run description and schema experiments carefully"),
      p("When testing a tool-description or schema change, change one meaningful variable at a time, keep the intent set stable, and compare the result with a baseline. Look for higher selection accuracy and completion without a rise in unsafe calls, invalid arguments, or retries."),
      h("Publish limitations with every benchmark"),
      ul([
        "Number and source of intents.",
        "Clients, models, and versions tested.",
        "Prompt and catalog construction.",
        "Whether runs were synthetic, human-reviewed, or production-derived.",
        "Randomness, repeat count, and confidence or variance where applicable.",
        "What the benchmark cannot conclude about real-world users.",
      ]),
      h("Frequently asked questions"),
      faq("What is MCP tool-selection evaluation?", "It is a test of whether an agent chooses the right MCP tool for an intent and then produces valid arguments, receives a useful result, recovers from errors, and completes the workflow safely."),
      faq("Is tool-selection accuracy enough?", "No. Selection, argument validity, execution success, recovery, completion, and safety should be measured separately because each reveals a different failure mode."),
      faq("How many prompts do I need for an MCP benchmark?", "There is no universal minimum. Use enough representative intents to cover your supported jobs, repeat runs where randomness matters, and publish the sample size and limitations instead of presenting a small fixture as a definitive benchmark."),
      links([
        { label: "MCP tool schemas guide", href: "/blog/mcp-tool-schemas" },
        { label: "MCP tool adoption guide", href: "/blog/measure-mcp-tool-adoption-workflow-completion" },
        { label: "MCP tools specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/server/tools" },
      ]),
    ],
  },
  {
    slug: "mcp-authorization-errors",
    title: "MCP Authorization Errors: Debugging 401, 403, Discovery, and PKCE Failures",
    tag: "Troubleshooting",
    excerpt:
      "A symptom-first guide to diagnosing remote MCP authentication failures, from missing metadata to invalid audiences and redirect URI errors.",
    date: "Sep 4, 2026",
    read: "9 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP authorization errors", "MCP 401", "MCP 403", "MCP OAuth errors", "MCP PKCE error"],
    related: ["remote-mcp-oauth-guide", "mcp-server-security-checklist", "mcp-observability-guide"],
    body: [
      p("MCP authorization errors usually occur before a tool runs, but the visible symptom can appear as a failed connection, an incomplete discovery step, or a tool that works in one client and fails in another. Start by identifying where the flow stopped: protected-resource metadata, authorization-server discovery, user authorization, token exchange, or the MCP request itself."),
      h("The quick diagnostic table"),
      ul([
        "401 before discovery: inspect the WWW-Authenticate header and protected-resource metadata URL.",
        "403 after authentication: inspect scopes, tenant policy, and tool permissions.",
        "Redirect URI error: compare the exact registered URI, application type, and localhost behavior.",
        "PKCE failure: verify the code challenge method, verifier, and authorization-server metadata.",
        "Works in one client only: compare client version, discovery behavior, resource URI, and token storage.",
      ]),
      h("401: authentication is missing or invalid"),
      p("A 401 means the server cannot accept the presented authentication. Confirm that the client sends a bearer token on every protected HTTP request, that the token has not expired, and that the server points the client to the correct protected-resource metadata. Never solve a 401 by accepting an arbitrary token or disabling audience validation."),
      h("403: authentication succeeded but policy denied the request"),
      p("A 403 generally means the identity is known but lacks the required scope, role, tenant access, or tool permission. Check the requested tool and operation separately. A client may be allowed to list tools or read data without being allowed to mutate records or trigger a destructive action."),
      h("Discovery failures"),
      p("If discovery fails, verify the server’s canonical URI, metadata content type, authorization-server location, and HTTPS configuration. Keep trailing-slash and path conventions consistent. Test the metadata endpoints directly with a clean client and record the response without exposing tokens."),
      h("PKCE and redirect URI failures"),
      p("Desktop and CLI clients often use localhost redirect URIs. The authorization server must compare the redirect URI exactly and understand the client application type. PKCE failures usually mean the verifier was lost, the challenge was generated with an unsupported method, or the callback was handled by a different client instance."),
      p("Check the flow without logging secrets: record the MCP resource URI, authorization-server issuer, registered redirect URI, PKCE methods, token audience, and client version. Then repeat the flow with a fresh browser session."),
      h("When only one client fails"),
      p("Do not immediately change the server to match one client. Compare the clients’ protocol versions, resource parameters, metadata support, PKCE handling, token cache, and HTTP behavior. Capture a sanitized timeline with client name and version, status code, method, tool name when available, and error class."),
      h("Instrument the authorization path"),
      ul([
        "authorization_metadata_requested",
        "authorization_started",
        "authorization_callback_received",
        "token_exchange_succeeded or token_exchange_failed",
        "mcp_request_authorized or mcp_request_denied",
      ]),
      p("Record event names, timing, client, issuer, outcome class, and correlation ID. Do not record bearer tokens, authorization codes, refresh tokens, or unrestricted user data."),
      h("Frequently asked questions"),
      faq("What is the difference between MCP 401 and 403?", "A 401 means authentication is missing or invalid. A 403 means the identity is authenticated but does not have sufficient scope or permission for the requested resource or tool."),
      faq("Why does an MCP OAuth flow work in one client but not another?", "Clients can differ in metadata discovery, resource parameters, PKCE handling, redirect URI registration, token storage, and supported protocol versions. Compare those behaviors before changing the server policy."),
      faq("Should an MCP server return 401 for an invalid scope?", "No. An invalid or missing authentication is a 401 condition; a valid identity without sufficient permission is generally a 403 condition. Follow the applicable authorization specification and document exceptions."),
      links([
        { label: "Remote MCP OAuth guide", href: "/blog/remote-mcp-oauth-guide" },
        { label: "MCP authorization specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization" },
        { label: "MCP observability guide", href: "/blog/mcp-observability-guide" },
      ]),
    ],
  },
  {
    slug: "remote-mcp-oauth-guide",
    title: "Remote MCP Server Authentication with OAuth 2.1: A Practical Guide",
    tag: "Security",
    excerpt:
      "How to authenticate a remote MCP server with OAuth 2.1, protected-resource metadata, PKCE, resource indicators, and audience validation.",
    date: "Sep 4, 2026",
    read: "12 min read",
    updated: "Sep 4, 2026",
    keywords: ["remote MCP OAuth", "MCP authentication", "MCP OAuth 2.1", "MCP protected resource metadata", "MCP PKCE"],
    related: ["mcp-server-security-checklist", "mcp-authorization-errors", "mcp-2026-07-28-migration-guide"],
    body: [
      p("A remote MCP server should treat authentication as a resource-server problem, not as a shared API-key shortcut. The client discovers the authorization server, obtains a resource-specific access token, sends it as a bearer token, and the MCP server validates issuer, audience, scopes, and expiry before processing a request."),
      callout("Version note", "This guide reflects the MCP authorization model and the 2026-07-28 direction. Verify the exact behavior supported by the MCP client, authorization server, and SDK versions you deploy."),
      h("Remote MCP versus local stdio authentication"),
      p("Local stdio servers commonly receive credentials from the process environment because the client and server run together. A remote HTTP server has a network trust boundary and may serve many users or tenants, so it needs an authorization flow that can identify the resource, authorize the client, and constrain what the resulting token can access."),
      h("The actors in the flow"),
      ul([
        "MCP client: requests access to the remote server on behalf of a user or agent.",
        "MCP server: acts as the protected resource server and validates access tokens.",
        "Authorization server: authenticates the resource owner and issues tokens.",
        "Resource owner: the person or organization whose data or actions are protected.",
      ]),
      h("1. Discover the authorization server"),
      p("The MCP server advertises protected-resource metadata that identifies its authorization server. The client uses that metadata and the authorization-server metadata to learn the authorization endpoint, token endpoint, supported scopes, and PKCE capabilities. Keep the canonical MCP server URI consistent across discovery, authorization, and token requests."),
      h("2. Bind the token to the MCP resource"),
      p("A resource indicator identifies which MCP server the token is intended for. This reduces the chance that a token issued for one resource will be accepted by another. The server must reject tokens that are not intended for it, even when the token is otherwise valid and signed."),
      p("Send the access token in the Authorization bearer header on every protected request. Do not put it in a query string, a URL, a page title, or an ordinary application log."),
      h("3. Protect the authorization-code flow"),
      p("Public clients such as desktop and CLI applications should use authorization code flow with PKCE. Validate the state returned to the callback, use exact redirect URIs, require HTTPS except for approved localhost flows, and do not accept an authorization response from an unexpected issuer."),
      h("4. Validate every request"),
      ul([
        "Check that the bearer token is present and well-formed.",
        "Validate signature, issuer, expiry, and intended audience.",
        "Check scopes and tenant or subject policy for the requested tool.",
        "Return 401 for missing or invalid authentication and 403 for insufficient permission.",
        "Never put access tokens in query strings or logs.",
      ]),
      h("5. Use separate credentials downstream"),
      p("If the MCP server calls another API, treat that API as a separate trust boundary. Validate the token received from the client for the MCP resource, then use a separately issued downstream credential or an explicit delegated exchange. Do not forward the client’s token blindly."),
      h("Testing matrix"),
      ul([
        "No token: the server returns a safe 401 with the discovery hint.",
        "Expired token: the server rejects it without invoking the tool.",
        "Wrong audience: the server rejects it even if the signature is valid.",
        "Insufficient scope: the server returns 403 and does not perform the action.",
        "Valid read scope calling a write tool: policy denies the call.",
        "Two authorization servers: credentials are not reused across issuers.",
        "Multiple instances: every instance makes the same authorization decision.",
      ]),
      h("Frequently asked questions"),
      faq("Does every MCP server need OAuth?", "The authorization design depends on the deployment and transport. Remote HTTP servers protecting user or organizational resources need an appropriate authorization model; local stdio servers commonly use process credentials. Follow the applicable MCP specification and threat model."),
      faq("What is MCP Protected Resource Metadata?", "It is metadata that helps an MCP client discover which authorization server protects the MCP resource and how to begin the authorization flow."),
      faq("Why does MCP need resource indicators?", "Resource indicators bind the requested token to a specific MCP server, helping prevent tokens issued for one resource from being accepted by another."),
      links([
        { label: "MCP authorization specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization" },
        { label: "MCP security best practices", href: "https://modelcontextprotocol.io/docs/2025-11-25/tutorials/security/security_best_practices" },
        { label: "TrackMCP security checklist", href: "/blog/mcp-server-security-checklist" },
      ]),
    ],
  },
  {
    slug: "mcp-server-analytics-guide",
    title: "MCP Server Analytics: The Complete Guide to Measuring Agent Tool Usage",
    tag: "Guide",
    excerpt:
      "MCP server analytics shows which clients connect, which tools they use, where workflows fail, and whether agents reach a useful result.",
    date: "Sep 4, 2026",
    read: "12 min read",
    updated: "Sep 4, 2026",
    keywords: [
      "MCP server analytics",
      "MCP analytics",
      "MCP usage analytics",
      "agent tool analytics",
      "MCP workflow completion",
    ],
    related: ["mcp-observability-guide", "measure-mcp-tool-adoption-workflow-completion", "the-errors-hiding-inside-a-200-ok"],
    body: [
      p("MCP server analytics is the measurement of client connections, tool calls, sessions, errors, latency, adoption, and workflow outcomes for a Model Context Protocol server. It answers a question ordinary server logs cannot: did an agent actually get the work done?"),
      callout("The short version", "Track adoption first, then behavior, reliability, and outcomes. A server with many calls but few completed workflows is busy, not healthy."),
      h("Why MCP servers need their own analytics layer"),
      p("An MCP server is not used like a conventional API. A language model discovers a catalog, chooses a tool at runtime, generates arguments, reads the result, and may retry or switch tools. The useful unit is therefore not only the request. It is the sequence of calls that moves an agent toward a task."),
      p("HTTP logs can tell you that a request arrived. APM can tell you that the process responded in 180 milliseconds. Neither one reliably tells you whether the agent chose the right tool, whether a tool error was embedded in a successful response, or where a session stopped."),
      h("The four layers of MCP analytics"),
      ul([
        "Adoption: active clients, new versus returning clients, and the breadth of tool usage.",
        "Behavior: tool selection, call sequences, retries, and the point where sessions stop.",
        "Reliability: tool-level errors, p50 and p95 latency, timeouts, and transport failures.",
        "Outcomes: workflows that complete, fail, or require human intervention.",
      ]),
      h("Which MCP server metrics should you track?"),
      p("Start with a small metric set that leads to a decision. Every metric should tell someone what to investigate or improve next."),
      ul([
        "Active clients: distinct client identities or installation instances in a selected period.",
        "Tool adoption breadth: distinct clients that used each tool, not just total call volume.",
        "Tool success rate: successful tool results divided by observed tool calls, with transport failures kept separate.",
        "Workflow completion rate: completed workflows divided by started workflows, using a real business outcome where possible.",
        "Retry rate: repeated calls in the same session, especially repeated calls with similar arguments.",
        "Tail latency: p95 duration per tool, because one slow step compounds across an agent workflow.",
        "Unused tools: tools advertised in the catalog but not called during a meaningful observation window.",
      ]),
      h("Why completion rate is more useful than call volume"),
      p("Call volume is an activity metric. It can rise because agents are succeeding, or because they are stuck in a retry loop. Completion rate is closer to value: of the workflows that started, how many reached a defined useful state?"),
      p("Define completion in the application when you can. An issue created, a customer record updated, a report delivered, or a deployment verified is stronger than assuming that the final tool call was useful. If business events are not available, use a documented proxy such as a successful terminal tool followed by session end."),
      h("Capture at the protocol boundary"),
      p("The most maintainable instrumentation point is the shared server or transport boundary. It sees every tool call without requiring developers to edit every handler, and new tools inherit coverage automatically."),
      code(`import { withTrackMCP } from "@trackmcp/sdk";
import { server } from "./mcp";

export default withTrackMCP(server, {
  apiKey: process.env.TRACKMCP_KEY,
  service: "acme-mcp-server",
  environment: "production",
});`),
      p("Capture the tool name, client identity when available, start and end time, transport result, tool-level error state, session or trace identity, and a redacted representation of arguments and results. Keep business outcomes as explicit events from the application rather than inferring everything from protocol traffic."),
      h("Privacy is part of the analytics design"),
      p("Tool arguments often contain emails, account identifiers, queries, documents, or credentials. Redact sensitive paths in the server process before telemetry leaves it. Use sampling for high-volume or low-value events, define retention, and keep operator-visible traces separate from anything that could be returned to an agent."),
      h("How to turn analytics into product decisions"),
      ul([
        "High adoption and low success: fix schemas, validation, dependencies, or error messages.",
        "Low adoption and high success: improve the tool name and description, then test the change.",
        "High retries: make errors actionable, accept common argument shapes, or reduce timeouts.",
        "High p95 latency: find the dependency behind the slow tool and inspect the affected workflow paths.",
        "Zero calls: confirm the tool is not simply poorly described before removing it.",
        "High completion for one client and low completion for another: compare client-specific discovery, arguments, and transport behavior.",
      ]),
      h("MCP analytics versus logs and APM"),
      p("Logs are the raw record of emitted events. APM is primarily about service performance. MCP analytics adds protocol-aware context: who connected, what the agent selected, what the tool returned, what happened next, and whether the workflow completed. You generally want all three layers, but they answer different questions."),
      h("Frequently asked questions"),
      faq("What is MCP server analytics?", "MCP server analytics measures how AI clients discover and use an MCP server, including clients, tools, sessions, errors, latency, retries, adoption, and workflow outcomes."),
      faq("What is the most important MCP metric?", "Workflow completion rate is usually the best north-star metric because it measures whether sessions reach a useful result. Pair it with tool-level errors, latency, retries, and adoption breadth to diagnose movement."),
      faq("Can MCP analytics work with existing tools?", "Yes. MCP analytics can complement logs, APM, tracing, and OpenTelemetry. The MCP-specific layer adds client, tool, session, and outcome context that general-purpose infrastructure telemetry often does not contain."),
      faq("Should MCP telemetry include tool arguments?", "Only when there is a clear debugging need and sensitive fields are redacted locally. Default to minimized, structured metadata and document retention before collecting raw payloads."),
      links([
        { label: "TrackMCP TypeScript SDK docs", href: "/docs/typescript" },
        { label: "TrackMCP Python SDK docs", href: "/docs/python" },
        { label: "TrackMCP configuration reference", href: "/docs/reference" },
        { label: "MCP server tools specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/server/tools" },
      ]),
    ],
  },
  {
    slug: "mcp-observability-guide",
    title: "MCP Observability: Logs, Metrics, Traces, Sessions, and Workflow Outcomes",
    tag: "Engineering",
    excerpt:
      "MCP observability connects transport health to agent behavior so you can see not just whether a server is up, but whether work completes.",
    date: "Sep 4, 2026",
    read: "11 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP observability", "MCP logs", "MCP metrics", "MCP tracing", "MCP server telemetry"],
    related: ["mcp-server-analytics-guide", "how-to-monitor-an-mcp-server", "latency-that-matters-for-agents"],
    body: [
      p("MCP observability is the practice of connecting protocol events, tool execution, client behavior, and workflow outcomes into one investigation path. It lets an engineer move from “the agent failed” to the exact tool, argument shape, dependency, retry, or authorization step that caused the failure."),
      callout("A useful definition", "Monitoring asks whether a known condition is healthy. Observability helps you investigate behavior you did not predict."),
      h("What makes MCP observability different"),
      p("The caller is often a non-deterministic agent. It chooses tools at runtime, may call several tools in sequence, and may recover from an error or silently abandon a task. The server sees protocol traffic, but the reason for a choice may live in another process. That makes correlation and workflow context essential."),
      h("The MCP observability stack"),
      ul([
        "Transport health: connection failures, authorization responses, request duration, and availability.",
        "Protocol health: discovery, catalog changes, supported versions, and malformed envelopes.",
        "Tool health: call volume, tool-level errors, validation failures, retries, and latency percentiles.",
        "Session health: ordered calls, drop-off points, reconnects, and client-specific behavior.",
        "Outcome health: completed workflows, failed workflows, human handoffs, and business events.",
      ]),
      h("Why a green HTTP status is not enough"),
      p("MCP tool execution errors can be represented in the response content while the outer transport still succeeds. A status-code-only alert can therefore stay green while the agent receives an error, retries, and gives up. Parse the protocol result and record tool-level failure separately from transport failure."),
      code(`{
  "content": [{
    "type": "text",
    "text": "to: expected array"
  }],
  "isError": true
}`),
      h("The dimensions that make telemetry useful"),
      p("A metric without dimensions is a warning light. The following dimensions turn it into a queue of investigations."),
      ul([
        "service and environment: separate production from staging and one server from another.",
        "client name and version: compare Claude, Cursor, ChatGPT, custom agents, and unknown clients.",
        "tool name and protocol method: distinguish catalog discovery from execution.",
        "session or trace identity: reconstruct the order of calls.",
        "outcome class: success, tool error, validation error, timeout, authorization failure, or transport failure.",
        "duration percentile: use p50 for typical behavior and p95 for the tail that damages workflows.",
      ]),
      h("From a tool trace to a fix"),
      p("Start with the incomplete workflow, not the largest log file. Identify the last successful step, the first repeated or failed step, and whether the agent changed arguments. Then inspect the tool schema, dependency latency, error text, permissions, and client mix at that exact point."),
      ul([
        "Same arguments repeated: likely a schema mismatch or an error the agent cannot act on.",
        "Different arguments after a failure: the agent may be recovering; inspect whether the second attempt succeeds.",
        "One tool dominates incomplete sessions: prioritize that tool even if its overall error rate looks modest.",
        "Only one client fails: compare its transport, auth, catalog, and argument behavior before changing the server globally.",
      ]),
      h("OpenTelemetry and MCP-specific context"),
      p("OpenTelemetry is useful for exporting traces and connecting MCP work to downstream services. Add MCP attributes such as client, tool, method, session or trace identity, outcome class, and redaction status. Avoid putting secrets or unrestricted payloads into span attributes. A general trace should lead to the MCP event, and the MCP event should lead to the application dependency that actually failed."),
      h("Design for failure and privacy"),
      p("Telemetry should be fail-open: if the analytics endpoint is unavailable, the tool call should still run. Batch low-value events, bound queue size, redact locally, and expose a clear way to disable payload capture. Treat trace search and operator dashboards as a separate trust boundary from agent-visible context."),
      h("A practical minimum dashboard"),
      ul([
        "Overview: active clients, tool calls, tool-error rate, p95 latency, and completed workflows.",
        "Clients: client distribution, new versus returning usage, and completion by client.",
        "Tools: calls, adoption breadth, success rate, p95, retries, and unused tools.",
        "Sessions: ordered tool paths, incomplete-session drop-off, and top failure points.",
        "Investigations: raw event metadata, redacted arguments, result state, and downstream correlation IDs.",
      ]),
      h("Frequently asked questions"),
      faq("What is MCP observability?", "MCP observability connects transport, protocol, tool, session, client, and workflow signals so teams can investigate how an MCP server behaves in production."),
      faq("Is MCP observability the same as APM?", "No. APM is valuable for service performance and distributed traces, while MCP observability adds agent-specific context such as tool selection, client behavior, embedded tool errors, retries, and workflow completion."),
      faq("What should I alert on for an MCP server?", "Alert on transport availability, authorization failures, tool-level error spikes, p95 latency for critical tools, retry loops, and meaningful drops in workflow completion. Keep low-value catalog changes and isolated errors out of paging alerts."),
      faq("Should I collect full MCP payloads?", "Usually not by default. Collect minimized metadata, redact sensitive fields in-process, sample where appropriate, and retain full payloads only when there is a documented debugging and privacy need."),
      links([
        { label: "TrackMCP REST API", href: "/docs/api" },
        { label: "TrackMCP configuration reference", href: "/docs/reference" },
        { label: "MCP tools specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/server/tools" },
        { label: "OpenTelemetry for MCP in Next.js", href: "https://nextjs.org/docs/app/guides/open-telemetry" },
      ]),
    ],
  },
  {
    slug: "how-to-monitor-an-mcp-server-in-production",
    title: "How to Monitor an MCP Server in Production",
    tag: "Engineering",
    excerpt:
      "A production MCP monitoring checklist covering availability, discovery, authorization, tool behavior, latency, retries, and workflow completion.",
    date: "Sep 4, 2026",
    read: "10 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP server monitoring", "monitor MCP server", "MCP production monitoring", "MCP server health"],
    related: ["mcp-observability-guide", "mcp-production-readiness-checklist", "mcp-2026-07-28-migration-guide"],
    body: [
      p("To monitor an MCP server in production, check more than uptime. Verify that clients can discover the server, authenticate, see the expected catalog, call critical tools, recover from failures, and complete representative workflows."),
      h("The production monitoring model"),
      ul([
        "Availability: can the endpoint or process be reached?",
        "Discoverability: can a client retrieve the expected tools, resources, or prompts?",
        "Authorization: do valid clients authenticate and invalid clients fail safely?",
        "Execution: do critical tools return correct results with expected latency?",
        "Recovery: can agents understand errors, retry safely, and finish the task?",
        "Outcome: are real workflows producing the result users need?",
      ]),
      h("1. Monitor the transport and protocol"),
      p("A basic check should exercise the deployed transport rather than only pinging a port. Record response status, duration, protocol version, authorization behavior, and whether the expected discovery response is present. For remote HTTP deployments, include the full authentication and metadata-discovery path in a safe synthetic check."),
      h("2. Monitor the catalog"),
      p("A server can be available while its advertised tools have changed, disappeared, or become unusable. Compare the observed catalog with a reviewed expected set. Track catalog changes as deploy events so an adoption or completion drop can be correlated with a tool rename, schema change, or removal."),
      h("3. Monitor tool behavior, not only server behavior"),
      p("Rank tools by business criticality, not just volume. For each critical tool track call count, success, tool-level errors, validation errors, p50 and p95 latency, timeout rate, and retry rate. A single broken tool in a common workflow can be more damaging than a broad but low-volume warning."),
      h("4. Monitor agent workflows"),
      p("A synthetic tool call proves that one input worked. A workflow check proves that a sequence works. Define representative paths such as search → fetch → summarize, or inspect → update → verify. Keep test data isolated and label synthetic traffic so it does not distort product analytics."),
      h("5. Separate paging alerts from investigation signals"),
      ul([
        "Page: endpoint unavailable, authorization outage, critical tool error spike, or sustained completion-rate failure.",
        "Warn: p95 regression, rising retries, catalog drift, or one client degrading.",
        "Investigate: unused tools, one-off validation errors, low-volume client changes, or a single slow request.",
      ]),
      h("A useful MCP server SLO set"),
      p("Set targets per environment and workflow. Example starting points are illustrative, not universal standards: 99.9% availability for the remote endpoint, 99% successful discovery for supported clients, 99% success for read-only critical tools, and a documented p95 target for each workflow-critical tool. The right thresholds depend on the dependency and user promise."),
      h("6. Create an incident runbook"),
      ul([
        "Confirm whether the issue is transport, authorization, catalog, tool, dependency, or outcome related.",
        "Break down the signal by client, tool, environment, version, and deployment time.",
        "Inspect the first failing event and the preceding successful event in the session.",
        "Check whether the agent retried, changed arguments, or abandoned the workflow.",
        "Mitigate with rollback, feature flag, rate limit, dependency fallback, or tool disablement.",
        "Record the fix and add a regression check for the failure mode.",
      ]),
      h("7. Protect the monitoring data"),
      p("Monitoring data can contain the same sensitive values as the tool call. Redact secrets and personal data before transmission, avoid logging bearer tokens, separate synthetic credentials from production credentials, and define who can inspect traces. Monitoring must not create a second path for data leakage."),
      h("Frequently asked questions"),
      faq("What is the difference between MCP monitoring and MCP observability?", "Monitoring checks known health conditions such as availability and error thresholds. MCP observability connects those signals to client behavior, tool choices, sessions, retries, and workflow outcomes for deeper investigation."),
      faq("How do I know if an MCP server is really working?", "Exercise discovery, authorization, critical tool calls, and representative workflows. A process that returns HTTP 200 but produces repeated tool errors is not healthy from the agent’s perspective."),
      faq("Should synthetic MCP checks call write tools?", "Use isolated test accounts and reversible test data if a write path must be checked. Prefer read-only checks for routine monitoring and label synthetic traffic so it is excluded from adoption and outcome reporting."),
      links([
        { label: "TrackMCP docs quickstart", href: "/docs" },
        { label: "MCP authorization specification", href: "https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization" },
        { label: "MCP 2026-07-28 specification update", href: "https://blog.modelcontextprotocol.io/posts/2026-07-28/" },
      ]),
    ],
  },
  {
    slug: "mcp-production-readiness-checklist",
    title: "MCP Server Production Readiness Checklist",
    tag: "Guide",
    excerpt:
      "Use this checklist before exposing an MCP server to real users, agents, production data, or state-changing tools.",
    date: "Sep 4, 2026",
    read: "9 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP production readiness", "MCP server checklist", "production MCP server", "MCP deployment checklist"],
    related: ["how-to-monitor-an-mcp-server-in-production", "mcp-server-security-checklist", "mcp-2026-07-28-migration-guide"],
    body: [
      p("An MCP server is production-ready when it can be discovered, authenticated, operated, monitored, and safely changed without relying on a developer watching every agent session. Use this checklist before exposing it to production users or state-changing workflows."),
      h("Architecture and deployment"),
      ul([
        "Choose the transport and protocol version deliberately; document supported clients and fallbacks.",
        "Separate development, staging, and production servers and credentials.",
        "Make deployments reproducible and pin runtime, SDK, and dependency versions.",
        "Define timeouts, concurrency limits, rate limits, and dependency budgets.",
        "Plan horizontal scaling and confirm whether application state is safe to share or must be externalized.",
        "Provide a rollback path for server code, tool catalog, and authorization configuration.",
      ]),
      h("Tool design and catalog quality"),
      ul([
        "Use clear, distinct, action-oriented tool names.",
        "Describe when a tool should and should not be used.",
        "Make input schemas explicit and tolerant of common model-generated shapes where safe.",
        "Return actionable errors that tell the agent what to change.",
        "Separate read-only, write, destructive, and administrative capabilities.",
        "Test tool selection, not only handler correctness.",
        "Track unused tools and remove or improve tools that add catalog noise.",
      ]),
      h("Security and authorization"),
      ul([
        "Use HTTPS for remote authorization flows and validate redirect URIs exactly.",
        "Validate token audience and issuer; never accept or pass through tokens intended for another resource.",
        "Use least-privilege scopes and separate identities for users, agents, and service accounts.",
        "Protect authorization code flows with PKCE and state validation.",
        "Defend outbound requests against SSRF and restrict reachable hosts where possible.",
        "Treat tool descriptions, resources, and external content as untrusted input.",
        "Review state-changing tools for confirmation, idempotency, and rollback behavior.",
      ]),
      h("Observability and operations"),
      ul([
        "Capture client, tool, method, session or trace, duration, outcome, and environment.",
        "Measure tool-level errors separately from transport errors.",
        "Track p50 and p95 latency, retries, timeouts, and incomplete workflows.",
        "Define alerts for critical tools and representative workflows.",
        "Make telemetry fail-open so analytics outages do not block tool execution.",
        "Redact secrets and personal data locally before telemetry leaves the server.",
        "Create an incident runbook and test it with a synthetic failure.",
      ]),
      h("Testing and release gates"),
      ul([
        "Unit-test handlers, schemas, authorization checks, and error states.",
        "Run integration tests with the supported MCP clients.",
        "Test malformed arguments, expired tokens, missing scopes, timeouts, retries, and dependency failures.",
        "Run a representative multi-tool workflow against staging.",
        "Compare the tool catalog and schema diff during release review.",
        "Record the protocol, SDK, client, and server versions in test output.",
      ]),
      h("Go-live decision"),
      p("Do not treat this checklist as a binary certification. Record each item as pass, partial, not applicable, or blocked, and assign an owner. A server with no write tools may have a different risk profile from one that can send messages, mutate records, deploy infrastructure, or access sensitive data."),
      h("Frequently asked questions"),
      faq("What is the most important production check for an MCP server?", "Verify a representative workflow from discovery through a useful outcome. Availability alone does not prove that agents can select tools, pass valid arguments, recover from errors, and finish work."),
      faq("Does every MCP server need OAuth?", "Authorization requirements depend on the deployment and transport. Remote servers that protect resources need an appropriate authorization design; local stdio deployments commonly use environment-based credentials instead. Follow the applicable MCP specification and threat model."),
      faq("Should every MCP tool be exposed to every agent?", "No. Scope the catalog and permissions to the agent, user, tenant, and task where possible. Fewer, clearer, least-privilege tools are easier to select, audit, and operate."),
      links([
        { label: "MCP security best practices", href: "https://modelcontextprotocol.io/docs/2025-11-25/tutorials/security/security_best_practices" },
        { label: "MCP authorization", href: "https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization" },
        { label: "TrackMCP configuration reference", href: "/docs/reference" },
      ]),
    ],
  },
  {
    slug: "mcp-server-security-checklist",
    title: "MCP Server Security Checklist for Remote and Local Deployments",
    tag: "Security",
    excerpt:
      "A practical MCP security checklist for authorization, token handling, tool permissions, prompt injection, SSRF, telemetry, and audit trails.",
    date: "Sep 4, 2026",
    read: "11 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP server security", "MCP security checklist", "MCP OAuth security", "MCP tool permissions"],
    related: ["mcp-production-readiness-checklist", "mcp-token-passthrough-security", "mcp-server-analytics-guide"],
    body: [
      p("MCP security is the discipline of limiting what an AI client can discover, read, change, and cause a server to do. A secure deployment combines transport security, authorization, least privilege, input validation, outbound request controls, prompt-injection defenses, telemetry privacy, and an auditable trail of tool use."),
      callout("Security boundary", "An MCP server is not only an API wrapper. It is an interface that lets an agent turn language into actions, so permissions and auditability belong at the tool boundary."),
      h("Start with the threat model"),
      ul([
        "Who can connect to the server?",
        "Which users, agents, or tenants can access each tool?",
        "Which tools read sensitive data or change state?",
        "Can tool arguments cause server-side requests to arbitrary destinations?",
        "Can untrusted content influence a later tool call?",
        "What evidence will you need to investigate misuse or a mistaken action?",
      ]),
      h("Remote authorization"),
      p("For HTTP-based remote servers, follow the MCP authorization flow and the OAuth security model applicable to your deployment. Validate protected-resource metadata, authorization-server discovery, issuer and audience, scopes, redirect URIs, state, and PKCE. Return the correct distinction between unauthenticated and unauthorized requests."),
      h("Never use token passthrough"),
      p("Token passthrough means accepting a token from the client without validating that it was issued for the MCP server and forwarding it to a downstream API. It breaks trust boundaries, weakens accountability, and can let a server act as a confused deputy. Validate the inbound token for the MCP resource, then use a separately issued downstream credential when the architecture requires one."),
      h("Local and stdio deployments"),
      p("Local servers still need a threat model. Treat the process environment, package dependencies, filesystem, subprocesses, and network access as part of the security boundary. Do not assume that a server is safe because it runs on a developer laptop. Review installation commands, package provenance, requested permissions, and any ability to execute shell commands or access files."),
      h("Least privilege at the tool level"),
      ul([
        "Separate read from write and destructive operations.",
        "Use narrow scopes and tenant boundaries.",
        "Require confirmation for irreversible or high-impact actions.",
        "Make dangerous behavior explicit in the tool name and description.",
        "Use idempotency keys and audit identifiers for state-changing operations.",
        "Do not expose an entire backend API when an agent needs only a small safe subset.",
      ]),
      h("Prompt injection and untrusted content"),
      p("A resource, document, web page, issue, or tool result may contain instructions that are not authorized by the user. Treat retrieved content as data, not policy. Keep authorization decisions outside model-generated text, validate tool arguments independently, and require explicit approval for actions with material side effects."),
      h("SSRF and outbound requests"),
      p("Any tool that fetches a URL, calls an integration, or follows user-provided destinations can become a server-side request forgery path. Prefer allowlists, block private-network ranges where appropriate, validate redirects, limit protocols and ports, and enforce network-level egress controls."),
      h("Privacy-preserving telemetry"),
      p("Auditability does not require collecting everything. Record who or what called a tool, which tool and version was used, the outcome class, timing, authorization context, and a correlation identifier. Redact credentials, personal data, and sensitive content before transmission. Document retention and access for operators."),
      h("Security review questions"),
      ul([
        "Can an untrusted client enumerate tools it should not know about?",
        "Can a read-only identity invoke a write or destructive tool?",
        "Can a token issued for another service be accepted here?",
        "Can a tool reach internal metadata services or private network hosts?",
        "Can the agent cause the server to forward raw credentials downstream?",
        "Can operators reconstruct a high-impact action without exposing raw secrets?",
      ]),
      h("Frequently asked questions"),
      faq("What is the biggest MCP security mistake?", "Treating MCP as a harmless documentation or API layer instead of an action boundary. Overbroad tools, weak authorization, token passthrough, unrestricted outbound requests, and unreviewed state changes create avoidable risk."),
      faq("What is MCP token passthrough?", "Token passthrough is forwarding a client-provided token to a downstream service without validating that it was issued for the MCP server. MCP authorization guidance explicitly forbids this pattern."),
      faq("Should MCP servers log full prompts and tool arguments?", "Not by default. Log the minimum metadata needed for audit and debugging, redact sensitive fields before transmission, and apply access controls and retention limits to any payload capture."),
      links([
        { label: "MCP security best practices", href: "https://modelcontextprotocol.io/docs/2025-11-25/tutorials/security/security_best_practices" },
        { label: "MCP authorization specification", href: "https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization" },
        { label: "TrackMCP privacy and redaction docs", href: "/docs" },
      ]),
    ],
  },
  {
    slug: "mcp-2026-07-28-migration-guide",
    title: "MCP 2026-07-28 Migration Guide: Stateless Requests, Tasks, and Header-Based Routing",
    tag: "Reference",
    excerpt:
      "What the MCP 2026-07-28 release means for server teams: stateless protocol behavior, routing headers, cache hints, Tasks, authorization changes, and deprecations.",
    date: "Sep 4, 2026",
    read: "10 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP 2026-07-28", "MCP migration guide", "stateless MCP", "MCP Tasks", "MCP header routing"],
    related: ["mcp-production-readiness-checklist", "how-to-monitor-an-mcp-server-in-production", "mcp-server-security-checklist"],
    body: [
      p("The MCP 2026-07-28 specification moves the protocol toward a stateless, routable, cacheable core. The release changes how teams think about sessions, load balancing, server-to-client interactions, catalog caching, authorization hardening, Tasks, and deprecated transport or capability patterns."),
      callout("Version note", "This is a migration-oriented summary, not a substitute for the official specification or SDK migration notes. Verify behavior against the exact SDK and client versions you deploy."),
      h("What changed at a glance"),
      ul([
        "The protocol core is stateless and no longer requires the old initialize/initialized exchange or Mcp-Session-Id header.",
        "Requests carry protocol context and can be routed to any compatible instance behind a normal load balancer.",
        "Streamable HTTP requests include Mcp-Method and Mcp-Name headers for routing, authorization, and metering.",
        "List responses can carry cache hints and deterministic ordering.",
        "Tasks are an extension with polling-oriented task operations.",
        "Authorization hardening includes issuer validation, credential binding, and movement from Dynamic Client Registration toward Client ID Metadata Documents.",
        "Legacy HTTP+SSE and several older capabilities have a deprecation window rather than an immediate removal.",
      ]),
      h("1. Audit assumptions about sessions"),
      p("If your application stores important state in a transport session identifier, make that state explicit. The new stateless core allows requests to land on different instances. Use an application-level handle or durable store when a workflow needs continuity, and pass the handle through the tool interface where appropriate."),
      h("2. Update gateways and middleware"),
      p("Header-based method and tool names give gateways a direct way to route, authorize, meter, and observe requests. Update WAF, rate-limit, policy, and telemetry middleware to preserve and validate those headers. Do not rely only on parsing request bodies after a proxy has already made an access decision."),
      h("3. Revisit catalog caching"),
      p("Tools, prompts, resources, and reads can carry cache hints. A client or gateway can use those hints to reduce repeated discovery traffic. If your catalog changes dynamically, define the freshness behavior and test whether cached descriptions remain consistent with the deployed handlers."),
      h("4. Plan for Tasks and long-running work"),
      p("Long-running workflows need a state model that is visible and recoverable. Treat task creation, progress, polling, failure, cancellation, and completion as explicit events. Track time to first progress, total duration, retries, and terminal outcome rather than treating one long request as the whole workflow."),
      h("5. Recheck authorization"),
      p("The release continues to harden authorization around issuer validation, binding credentials to the issuer that minted them, and moving toward Client ID Metadata Documents. Test discovery, authorization, token redemption, audience validation, refresh behavior, and exact error responses with each supported client."),
      h("6. Treat deprecations as an observability task"),
      p("Deprecations are easier to manage when you know who still uses the old path. Break telemetry down by transport, protocol version, client, and capability. Set a migration threshold, notify affected users, and keep a compatibility test until the deprecation window closes."),
      h("Migration checklist"),
      ul([
        "Read the official specification and SDK notes for the version you are adopting.",
        "Test stateless requests across multiple server instances.",
        "Remove hidden dependence on transport session state.",
        "Update gateway routing and authorization to use the new request headers safely.",
        "Test catalog cache freshness and deterministic ordering.",
        "Instrument Tasks as a lifecycle rather than one request duration.",
        "Test OAuth discovery, issuer, audience, PKCE, and client metadata behavior.",
        "Measure deprecated transport and capability usage before setting a removal date.",
      ]),
      h("Frequently asked questions"),
      faq("What is the biggest MCP 2026-07-28 change?", "The protocol core becomes stateless, allowing requests to be routed across server instances without relying on the old transport-level session exchange."),
      faq("Does stateless MCP mean my application cannot keep state?", "No. It means important application state should not be hidden in transport session state. Use an explicit application handle and durable state when a workflow needs continuity."),
      faq("What are Mcp-Method and Mcp-Name used for?", "The 2026-07-28 Streamable HTTP changes use these headers to expose method and tool names to gateways for routing, authorization, and metering."),
      faq("Should I remove HTTP+SSE immediately?", "The official announcement describes a deprecation window. Check your client and SDK compatibility, measure current usage, publish a migration path, and remove it only after your supported users have a safe upgrade route."),
      links([
        { label: "Official MCP 2026-07-28 announcement", href: "https://blog.modelcontextprotocol.io/posts/2026-07-28/" },
        { label: "MCP Registry and server metadata", href: "https://modelcontextprotocol.io/registry/about" },
        { label: "MCP authorization specification", href: "https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization" },
      ]),
    ],
  },
  {
    slug: "measure-mcp-tool-adoption-workflow-completion",
    title: "How to Measure MCP Tool Adoption and Workflow Completion",
    tag: "Product",
    excerpt:
      "A practical measurement framework for understanding which MCP tools agents adopt, where workflows stop, and what to improve next.",
    date: "Sep 4, 2026",
    read: "9 min read",
    updated: "Sep 4, 2026",
    keywords: ["MCP tool adoption", "MCP workflow completion", "MCP adoption metrics", "agent workflow analytics"],
    related: ["mcp-server-analytics-guide", "find-the-tools-no-agent-ever-calls", "writing-tool-descriptions-agents-use"],
    body: [
      p("MCP tool adoption is not the same as call volume. A useful adoption measure includes how many distinct clients use a tool, whether usage is growing, whether calls succeed, and whether the tool participates in workflows that reach a useful outcome."),
      h("Measure adoption in three dimensions"),
      ul([
        "Breadth: how many distinct clients use the tool?",
        "Depth: how often is it used and how does it fit into a session path?",
        "Quality: do calls succeed and do the workflows that include the tool complete?",
      ]),
      h("Start with the tool catalog"),
      p("The catalog is the set of capabilities an agent can consider. Record which tools were advertised, when their descriptions or schemas changed, and which clients discovered them. A zero-call tool may be unnecessary, but it may also be invisible because its name or description does not match the user’s language."),
      h("The MCP tool adoption scorecard"),
      ul([
        "Reach: distinct clients calling the tool divided by active clients.",
        "Frequency: calls per active client, with outliers separated.",
        "Success: successful results divided by calls.",
        "Retry burden: repeated calls per workflow or per successful call.",
        "Workflow contribution: completed workflows containing the tool divided by started workflows containing it.",
        "Trend: week-over-week change, segmented by client and server version.",
      ]),
      h("Read tool adoption inside workflows"),
      p("A tool that starts many sessions but is followed by failure is not necessarily healthy adoption. Group calls into sessions, find common paths, and compare completed versus incomplete paths. The same tool can be valuable in one workflow and a dead end in another."),
      h("What to do with each pattern"),
      ul([
        "High reach, high success: protect the tool with regression tests and capacity planning.",
        "High reach, low success: fix the schema, validation, dependency, or error response first.",
        "Low reach, high success: improve naming, descriptions, examples, and placement in the catalog.",
        "High calls, low breadth: inspect automation or one noisy client before calling it broadly adopted.",
        "Zero calls: review discoverability and user need before deleting the tool.",
        "High adoption, low completion: inspect the next step in the path; the tool may be exposing a downstream failure.",
      ]),
      h("How to measure a description change"),
      p("Treat tool descriptions as an agent-facing UX surface. Record the old and new wording, the deployment date, affected clients, and the hypothesis. Compare breadth, success, retries, and workflow completion over a stable window. Avoid changing the schema, handler, model, and description at the same time if you want a useful result."),
      h("Define completion explicitly"),
      p("The strongest completion event comes from the application: a ticket created, a deployment verified, or a report delivered. Emit a named workflow event with started, completed, and failed states. If that is not available, document the proxy used and keep it consistent over time."),
      h("Frequently asked questions"),
      faq("What is MCP tool adoption?", "MCP tool adoption is the breadth, frequency, and quality of client usage for an MCP tool, evaluated in the context of sessions and workflow outcomes rather than raw call count alone."),
      faq("How do I find unused MCP tools?", "Count calls for each advertised tool over a meaningful period and compare the result with client coverage and catalog changes. Before removing a zero-call tool, check whether its description or schema prevents discovery."),
      faq("What is MCP workflow completion rate?", "Workflow completion rate is completed workflows divided by started workflows. Define completion using an actual business result when possible, or a documented proxy when the application outcome is not available."),
      links([
        { label: "TrackMCP REST API", href: "/docs/api" },
        { label: "TrackMCP tool adoption article", href: "/blog/how-to-measure-mcp-tool-adoption" },
        { label: "MCP tool descriptions specification", href: "https://modelcontextprotocol.io/specification/2025-11-25/server/tools" },
      ]),
    ],
  },
  {
    slug: "why-your-mcp-server-needs-analytics",
    title: "Why your MCP server needs analytics",
    tag: "Guide",
    excerpt:
      "Server logs tell you a request happened. They don't tell you who called what, why it failed, or whether the user got what they came for.",
    date: "Feb 12, 2026",
    read: "6 min read",
    body: [
      p("You shipped an MCP server. Agents can connect, tools resolve, requests return 200. By every signal your infrastructure gives you, it works. And yet you cannot answer the questions that actually decide whether it is worth maintaining."),
      p("Who is using it? Which tools do they rely on? Where do sessions stall? Which calls fail in a way your logs never surfaced? These are product questions, and logs were never built to answer them."),
      h("Logs record events, not behavior"),
      p("A log line proves a request occurred. It does not tell you that Claude called search_docs, got a result, then tried run_query three times and gave up. Behavior lives in the sequence and the outcome, not in any single line."),
      h("The gap is the same one websites had"),
      p("Every click sat in raw web server logs long before Google Analytics existed. The logs were complete and useless for understanding people. Analytics turned that stream into sessions, sources, and funnels. MCP servers are at exactly that moment now."),
      h("What to measure instead"),
      ul([
        "Who connects: which clients and how usage grows",
        "What they use: tool adoption and the tools nobody touches",
        "Whether work completes: sessions that reach a useful result",
        "Where it breaks: failures hidden inside successful responses",
      ]),
      p("You do not need to instrument each tool by hand. Because MCP is a protocol, you can capture all of this at the transport layer with a single wrapper, and start answering the questions that matter the day you launch."),
    ],
  },
  {
    slug: "the-errors-hiding-inside-a-200-ok",
    title: "The errors hiding inside a 200 OK",
    tag: "Engineering",
    excerpt:
      "MCP returns tool errors inside a successful response. Your HTTP logs and APM stay green while agents retry and give up.",
    date: "Feb 6, 2026",
    read: "7 min read",
    body: [
      p("The most expensive failures on an MCP server are the ones nothing alerts on. The transport returns 200. Your APM shows healthy latency. But the tool inside that response failed, and the agent quietly moved on."),
      h("Why this happens"),
      p("MCP encodes tool-level errors in the response body, often with an isError flag, rather than as an HTTP status. That is a reasonable protocol decision: a tool failing is not the same as the server failing. But it means every layer built around status codes is blind to it."),
      code(`{
  "content": [{ "type": "text", "text": "to: expected array" }],
  "isError": true
}`),
      h("What the agent does next"),
      p("It rarely surfaces the error to the user. It retries, sometimes with the same malformed arguments, then abandons the task. From the user's side the assistant just 'didn't do it.' From your side, everything looks fine."),
      h("How to catch it"),
      ul([
        "Read the payload, not just the status code",
        "Count isError responses per tool, separately from transport errors",
        "Track retries within a session to spot give-up loops",
        "Alert on error-rate spikes at the tool level",
      ]),
      p("Once you separate transport health from tool health, send_email failing 94 percent of the time stops being invisible and becomes the first thing you fix."),
    ],
  },
  {
    slug: "the-metrics-that-matter-for-an-mcp-server",
    title: "The metrics that matter for an MCP server",
    tag: "Guide",
    excerpt:
      "Not call volume for its own sake. The handful of numbers that tell you whether your server is adopted, reliable, and useful.",
    date: "Jan 29, 2026",
    read: "6 min read",
    body: [
      p("It is easy to drown an MCP dashboard in engineering metrics. Latency, throughput, error codes. They matter, but they are not the story. Here are the numbers that tell you whether the server is doing its job."),
      h("Adoption"),
      ul([
        "Active clients: how many distinct clients connect",
        "New vs returning connections: is usage growing and sticking",
        "Client mix: Claude, Cursor, ChatGPT, custom agents",
      ]),
      h("Behavior"),
      ul([
        "Tool adoption: which tools get called, which are ignored",
        "Most common workflow: the path agents actually take",
        "Where sessions stop: the step before people give up",
      ]),
      h("Outcomes"),
      ul([
        "Completion rate: sessions that reach a useful result",
        "Returning usage: clients that come back within a week",
      ]),
      h("Reliability (supporting)"),
      ul([
        "Silent failures: errors inside a 200 OK",
        "p95 latency per tool: the tail that ruins sessions",
      ]),
      p("Lead with adoption and outcomes. Keep reliability close, but do not let it define the product. A fast server nobody completes a task on is not a healthy server."),
    ],
  },
  {
    slug: "instrumenting-the-mcp-protocol-layer",
    title: "Instrumenting the MCP protocol layer",
    tag: "Engineering",
    excerpt:
      "How to capture every call without touching a single tool: wrap the transport, not the handlers.",
    date: "Jan 22, 2026",
    read: "8 min read",
    body: [
      p("The naive way to add analytics to an MCP server is to edit every tool. It does not scale, it drifts, and new tools ship uninstrumented. There is a better seam: the protocol itself."),
      h("One seam, total coverage"),
      p("Every tool call flows through the same request/response path. If you wrap the server once at that boundary, you see every call, its arguments, its result, timing, and the client, with no per-tool code."),
      code(`import { withTrackMCP } from "@trackmcp/sdk";
import { server } from "./mcp";

export default withTrackMCP(server, {
  apiKey: process.env.TRACKMCP_KEY,
  service: "acme-mcp-server",
});`),
      h("What the wrapper sees"),
      ul([
        "The tool name and arguments (after local redaction)",
        "The result and whether it carried isError",
        "Client type, duration, and transport status",
        "A session id, so calls can be replayed in order",
      ]),
      h("Keep it safe and cheap"),
      p("Redact sensitive fields in-process before anything leaves. Sample if volume is high. Fail open: analytics should never be able to break a tool call. Done right, instrumentation costs a line of code and a sub-millisecond hop."),
    ],
  },
  {
    slug: "find-the-tools-no-agent-ever-calls",
    title: "Find the tools no agent ever calls",
    tag: "Product",
    excerpt:
      "Half the tools most teams ship are never called. Dead tools are schema clutter, maintenance cost, and confusion for the model.",
    date: "Jan 15, 2026",
    read: "5 min read",
    body: [
      p("When you look at a real MCP server's usage, a pattern shows up fast: a few tools do almost all the work, and a long tail does nothing. Those dead tools are not free."),
      h("Why dead tools cost you"),
      ul([
        "They enlarge the tool list the model has to reason over",
        "They add schema and code you still maintain and secure",
        "They make the useful tools harder to choose correctly",
      ]),
      h("How to spot them"),
      p("Sort tools by call volume over a meaningful window. Anything at zero across weeks of real traffic is a candidate. Before deleting, check whether it is genuinely unused or just badly described, because a good tool with a bad description looks identical in the data."),
      h("Fix or remove"),
      p("If a tool should be used but isn't, rewrite its description and name so the model knows when to reach for it. If it truly has no job, remove it. Either way you shrink the surface and make the server easier for agents to use well."),
    ],
  },
  {
    slug: "how-to-measure-mcp-tool-adoption",
    title: "How to measure MCP tool adoption",
    tag: "Guide",
    excerpt:
      "Adoption is more than call count. It's how many clients use a tool, whether that's growing, and how it fits into workflows.",
    date: "Jan 9, 2026",
    read: "6 min read",
    body: [
      p("Call count alone can mislead. A tool called constantly by one flaky client is not the same as a tool adopted across your whole client base. To understand adoption, look at breadth, trend, and context."),
      h("Breadth"),
      p("Count distinct clients using each tool, not just calls. A tool used by every client is core; a tool used by one is niche, however loud its volume."),
      h("Trend"),
      p("Track week-over-week change. Tools gaining adoption tell you where the product is heading. Tools losing it early warn you before the drop shows up in totals."),
      h("Context"),
      p("Adoption lives inside workflows. A tool that is only ever the first step in a path that never completes is not really adopted; it is a dead end. Read tool usage alongside completion, not on its own."),
      p("Put together, these turn a flat list of counts into a map of what your server is actually for."),
    ],
  },
  {
    slug: "reading-mcp-sessions-like-funnels",
    title: "Reading MCP sessions like funnels",
    tag: "Guide",
    excerpt:
      "A session is a path from first request to result. Treat it like a funnel and the drop-off points become obvious.",
    date: "Dec 18, 2025",
    read: "6 min read",
    body: [
      p("Individual calls are noise until you group them into sessions. A session is one client's run from connect to outcome, and it behaves exactly like a funnel you would recognize from product analytics."),
      h("The stages"),
      ul([
        "Connected: the client established a session",
        "Reached a tool: it actually called something",
        "Completed a workflow: it got to a useful result",
      ]),
      h("Where people fall out"),
      p("The interesting number is the drop between stages. If most sessions connect but few reach a tool, your tool descriptions or discovery are the problem. If they reach a tool but rarely complete, the failure is downstream, often a single tool breaking a common path."),
      h("From funnel to fix"),
      p("Overlay completed versus stopped paths and the culprit usually names itself: the same tool, at the same step, ending sessions. That is where a fix pays off most."),
    ],
  },
  {
    slug: "why-agents-fail-on-your-tool-schemas",
    title: "Why agents fail on your tool schemas",
    tag: "Engineering",
    excerpt:
      "The single most common silent failure: a schema that expects one shape while agents reliably send another.",
    date: "Dec 10, 2025",
    read: "5 min read",
    body: [
      p("When a tool fails for almost every agent, the cause is rarely infrastructure. It is usually a mismatch between what the schema demands and what language models naturally produce."),
      h("A common example"),
      p("Your send_email tool expects to as an array of addresses. Models, trained on how humans write, send a single string. The call fails validation, the agent retries with the same string, and gives up."),
      code(`// schema wants:   { "to": ["a@x.com"] }
// agents send:     { "to": "a@x.com" }`),
      h("The fix is usually forgiveness"),
      p("Accept both shapes. Coerce a string into a single-element array. Accept common synonyms for enum values. Every bit of tolerance you add converts failed calls into completed ones without changing what the tool does."),
      h("How to find these"),
      p("Look for tools with high call volume and low success, then read the error text on the failing calls. The pattern is almost always a shape or type the model keeps getting wrong, and it is almost always cheap to accommodate."),
    ],
  },
  {
    slug: "logs-vs-apm-vs-mcp-analytics",
    title: "Logs vs APM vs MCP analytics",
    tag: "Guide",
    excerpt:
      "Three layers, three jobs. Why logs and APM leave a gap that agent traffic makes impossible to ignore.",
    date: "Dec 3, 2025",
    read: "6 min read",
    body: [
      p("Teams often assume their existing observability already covers the MCP server. It covers part of it. Understanding where each layer stops makes the gap obvious."),
      h("Server logs"),
      p("Logs record what happened: a request arrived, a response left. They are complete and low-level, and they say nothing about behavior, outcomes, or which agent did what."),
      h("APM and tracing"),
      p("APM watches performance: latency, throughput, traces across services. It was built for human and service traffic. It treats a 200 as success, so tool errors inside a 200 slip past it."),
      h("MCP analytics"),
      p("This layer reads the protocol: who connected, which tools they used, whether workflows completed, and what to fix. It is the analytics layer agents need, the same way websites needed one on top of raw logs."),
      quote("Logs recorded it. APM watched it. Neither explained it."),
      p("You likely want all three. But only the analytics layer answers the questions a product owner actually asks about an MCP server."),
    ],
  },
  {
    slug: "understanding-your-mcp-client-mix",
    title: "Understanding your MCP client mix",
    tag: "Guide",
    excerpt:
      "Claude, Cursor, ChatGPT, custom agents. Knowing who drives your usage changes what you build next.",
    date: "Nov 24, 2025",
    read: "5 min read",
    body: [
      p("Not all MCP clients behave the same. They differ in how they discover tools, how they format arguments, and how persistent they are on retries. Your client mix is a design input, not a vanity metric."),
      h("Why the mix matters"),
      ul([
        "A tool that works in one client may fail in another over argument shape",
        "Growth concentrated in one client is a risk worth knowing",
        "Custom agents often reveal power-user workflows worth supporting",
      ]),
      h("What to do with it"),
      p("Break every core metric down by client. If completion rate is high in Claude and low in Cursor, the problem is client-specific and so is the fix. If one client drives most growth, test changes against it first."),
      p("Client mix turns 'our server is doing fine' into 'our server is doing fine for these clients and not those,' which is the version you can act on."),
    ],
  },
  {
    slug: "workflow-completion-rate-the-north-star",
    title: "Workflow completion rate: the north-star for MCP",
    tag: "Product",
    excerpt:
      "One number captures whether your server actually helps: the share of sessions that reach a useful result.",
    date: "Nov 17, 2025",
    read: "6 min read",
    body: [
      p("If you could keep only one metric for your MCP server, make it workflow completion rate: of the sessions that start, how many reach a useful result. Almost everything else is a lever on this number."),
      h("Why it beats call volume"),
      p("Call volume rewards busy failure. A server where agents call tools constantly and never finish looks active and is broken. Completion rate cannot be gamed that way."),
      h("How to define 'complete'"),
      p("Where you can, tie completion to a real outcome: an issue created, a query answered, a message sent successfully. Where you cannot measure business value directly, use an honest proxy like a session that ends on a successful terminal tool rather than a retry loop."),
      h("Moving it"),
      ul([
        "Fix the tool that ends the most sessions early",
        "Make schemas forgiving so valid intents don't fail",
        "Improve descriptions so agents pick the right tool sooner",
      ]),
      p("Watch completion rate weekly, and let it decide what you fix first."),
    ],
  },
  {
    slug: "how-to-cut-agent-retries",
    title: "How to cut agent retries on your MCP server",
    tag: "Engineering",
    excerpt:
      "Retries are a tax on every session. Most come from a small set of avoidable, measurable causes.",
    date: "Nov 6, 2025",
    read: "5 min read",
    body: [
      p("Retries feel harmless because they sometimes succeed. But each one adds latency, cost, and a chance the agent gives up. Cutting them is one of the highest-leverage things you can do."),
      h("Where retries come from"),
      ul([
        "Schema mismatches that fail validation the same way twice",
        "Ambiguous errors the agent cannot recover from",
        "Slow tools that time out and get called again",
        "Tools that return partial results the agent re-requests",
      ]),
      h("How to find your worst offenders"),
      p("Group calls by session and look for repeated calls to the same tool with the same arguments. Rank tools by retries per successful call. The top of that list is your fix queue."),
      h("Fixes that stick"),
      p("Return errors that tell the agent exactly what to change. Accept the shapes models actually send. Cache or speed up the slow dependency behind a timeout-prone tool. Each one turns a loop into a single successful call."),
    ],
  },
  {
    slug: "latency-that-matters-for-agents",
    title: "Latency that matters for agents (p50 and p95)",
    tag: "Engineering",
    excerpt:
      "Averages hide the calls that ruin sessions. Track p50 for the typical call and p95 for the tail.",
    date: "Oct 28, 2025",
    read: "5 min read",
    body: [
      p("Latency matters for agents differently than for humans. An agent chains calls, so a slow tail on one tool compounds across a session and pushes it toward timeouts and retries."),
      h("Why averages lie"),
      p("A mean latency looks reassuring while a tenth of calls take ten times as long. Those tail calls are the ones agents time out on. Track percentiles, not averages."),
      h("The two numbers to watch"),
      ul([
        "p50: the experience of a typical call",
        "p95: the tail that quietly breaks sessions",
      ]),
      h("Act at the tool level"),
      p("Sort tools by p95, not overall latency. The fix is usually one dependency behind one tool. Speeding up that tail often lifts completion rate more than any broad performance work."),
    ],
  },
  {
    slug: "retention-for-mcp-servers",
    title: "Retention for MCP servers",
    tag: "Product",
    excerpt:
      "One-time usage is easy. The signal that your server is genuinely useful is clients that come back.",
    date: "Oct 20, 2025",
    read: "5 min read",
    body: [
      p("Launch traffic is flattering and temporary. The durable signal that your MCP server earned a place in an agent's toolkit is retention: clients that connect again next week without prompting."),
      h("What to measure"),
      ul([
        "Returning connections within 7 and 30 days",
        "Repeat usage of the same core workflow",
        "Breadth: returning clients using more than one tool",
      ]),
      h("Why breadth matters"),
      p("A client that only ever calls one tool is fragile; a small change can drop it. Clients that adopt several tools have woven your server into their work and tend to stay. Watch whether returning clients expand their usage over time."),
      h("Improving it"),
      p("Retention follows reliability and completion. Fix the failures that end sessions, make the common workflow smooth, and the clients that tried you once have a reason to come back."),
    ],
  },
  {
    slug: "instrument-your-mcp-server-in-one-line",
    title: "Instrument your MCP server in one line",
    tag: "Guide",
    excerpt:
      "You don't need an observability project to start. Wrap your server, deploy, and read the data the same day.",
    date: "Oct 10, 2025",
    read: "4 min read",
    body: [
      p("The reason most MCP servers ship without analytics is not disagreement about value; it is the assumed cost. But instrumenting at the protocol layer means the cost is one line and one deploy."),
      h("The whole setup"),
      code(`import { withTrackMCP } from "@trackmcp/sdk";
import { server } from "./mcp";

export default withTrackMCP(server, {
  apiKey: process.env.TRACKMCP_KEY,
  service: "acme-mcp-server",
});`),
      p("Python is the same shape with with_trackmcp. Your tools do not change. There are no per-tool decorators and no custom events required to get the core picture."),
      h("What you get on day one"),
      ul([
        "Active clients and client mix",
        "Tool adoption and the tools nobody calls",
        "Completed workflows and where sessions stop",
        "Silent failures hiding inside successful responses",
      ]),
      p("Start there. Add redaction and custom events later if you need them. The point is that the first insight is minutes away, not a quarter away."),
    ],
  },
  {
    slug: "writing-tool-descriptions-agents-use",
    title: "Writing tool descriptions agents actually use",
    tag: "Guide",
    excerpt:
      "A great tool with a vague description is invisible to the model. Descriptions are UX for agents.",
    date: "Oct 1, 2025",
    read: "6 min read",
    body: [
      p("An agent chooses tools by reading their names and descriptions. If a tool is never called, the problem is often not the tool but how it introduces itself. Descriptions are the interface agents actually see."),
      h("Write for the moment of choice"),
      p("The model is deciding, right now, which tool fits the user's intent. Say plainly what the tool does, when to use it, and when not to. Lead with the verb and the object: 'Create an issue in the project tracker.'"),
      h("Be specific about inputs"),
      ul([
        "Name arguments the way a model would guess them",
        "State accepted shapes and give a short example",
        "Avoid overlapping tools that compete for the same intent",
      ]),
      h("Close the loop with data"),
      p("Ship a description change, then watch adoption for that tool. If calls rise and success holds, the wording was the blocker. Treating descriptions as something you measure and iterate on is how underused tools come back to life."),
    ],
  },
  {
    slug: "what-is-mcp-analytics",
    title: "What is MCP analytics?",
    tag: "Guide",
    excerpt:
      "A plain-English definition: MCP analytics is product analytics for your MCP server. It shows who connects, what they call, and whether it works.",
    date: "Sep 24, 2025",
    read: "5 min read",
    body: [
      p("MCP analytics is the practice of measuring how AI clients actually use your MCP server: which clients connect, which tools they call, whether the work completes, and where it fails. It is product analytics for the Model Context Protocol, the same way Google Analytics is product analytics for a website."),
      h("The one-line definition"),
      quote("MCP analytics turns raw tool-call traffic into who, what, and whether it worked."),
      h("What it measures"),
      ul([
        "Clients: which agents connect and how usage grows",
        "Tools: call volume, adoption, and the tools nobody uses",
        "Workflows: the paths agents take and where they stop",
        "Outcomes: completion rate and returning usage",
        "Reliability: errors, including failures hidden inside a 200 OK",
      ]),
      h("How it differs from logging"),
      p("Logs record that a request happened. Analytics reconstructs behavior: it groups calls into sessions, attributes them to clients, and measures whether a task actually finished. Logs answer 'did it run?' Analytics answers 'did it work, for whom, and what should I fix?'"),
      h("Do you need it"),
      p("If you have shipped an MCP server and cannot answer which tool is most used or which one silently fails, you need MCP analytics. It installs at the protocol layer in one line and starts answering those questions from the first call."),
    ],
  },
  {
    slug: "what-is-the-model-context-protocol",
    title: "What is the Model Context Protocol (MCP)?",
    tag: "Guide",
    excerpt:
      "MCP is an open standard that lets AI models call your tools and data through a consistent interface. Here's the mental model.",
    date: "Sep 16, 2025",
    read: "6 min read",
    body: [
      p("The Model Context Protocol (MCP) is an open standard for connecting AI models to external tools and data. Instead of every model integrating with every service in its own way, MCP defines one consistent interface: a server exposes tools, and any MCP-compatible client can discover and call them."),
      h("The mental model"),
      p("Think of MCP as a universal port for AI. Your MCP server advertises a set of tools with names, descriptions, and input schemas. A client like Claude, Cursor, or ChatGPT reads that list, decides which tool fits the user's intent, and calls it with structured arguments."),
      h("The core pieces"),
      ul([
        "Server: exposes tools, resources, and prompts",
        "Client: an AI app that connects and calls them",
        "Transport: how messages pass between them",
        "Tools: named functions with input schemas and results",
      ]),
      h("Why it matters"),
      p("MCP turns a model from a text generator into something that can act: query a database, create an issue, send a message. That power is also why measurement matters. Once agents are calling real tools, you need to know which calls happen, whether they succeed, and what to improve."),
      h("Where analytics fits"),
      p("MCP standardizes the interface, which means you can capture every call at one seam. That is what makes MCP analytics possible in a single line, without touching each tool."),
    ],
  },
  {
    slug: "how-to-monitor-an-mcp-server",
    title: "How to monitor an MCP server",
    tag: "Engineering",
    excerpt:
      "Uptime and status codes aren't enough for agent traffic. Here's what to monitor on an MCP server and how to set it up.",
    date: "Sep 8, 2025",
    read: "7 min read",
    body: [
      p("Monitoring an MCP server is different from monitoring a typical web service. The traffic comes from AI agents, the errors hide inside successful responses, and the thing you care about is whether a task completed, not just whether the process is up."),
      h("Start with the protocol layer"),
      p("Wrap your server once at the request/response boundary. Because every tool call flows through the same path, one wrapper captures the name, arguments, result, timing, and client for every call, with no per-tool code."),
      code(`import { withTrackMCP } from "@trackmcp/sdk";
import { server } from "./mcp";

export default withTrackMCP(server, {
  apiKey: process.env.TRACKMCP_KEY,
});`),
      h("What to monitor"),
      ul([
        "Tool-level error rate, separate from transport errors",
        "Silent failures: isError responses inside a 200 OK",
        "p95 latency per tool, not just the average",
        "Completion rate: sessions that reach a useful result",
        "Client mix and retries per session",
      ]),
      h("Alert on the right things"),
      p("Alert when a tool's error rate spikes, when p95 latency climbs, or when completion rate drops. These are the signals that a real user experience is degrading, and none of them show up in a plain uptime check."),
    ],
  },
  {
    slug: "mcp-observability-explained",
    title: "MCP observability: logs, metrics, and traces",
    tag: "Engineering",
    excerpt:
      "The three pillars of observability mapped to MCP servers, plus the fourth thing agent traffic needs that they don't cover.",
    date: "Aug 28, 2025",
    read: "6 min read",
    body: [
      p("Observability is usually described as three pillars: logs, metrics, and traces. All three apply to an MCP server, but agent traffic adds a requirement the classic pillars miss: behavioral analytics."),
      h("Logs"),
      p("Logs are the raw event record: a call arrived, a result left. Essential for debugging a specific incident, but they say nothing about behavior across many calls or whether a user's task completed."),
      h("Metrics"),
      p("Metrics aggregate: request rate, latency percentiles, error counts. They tell you the system's health over time, but by default they treat a 200 as success and miss tool-level failures."),
      h("Traces"),
      p("Traces follow one request across services. Useful when a tool call fans out to several backends and you need to find the slow hop."),
      h("The missing pillar: behavior"),
      p("None of the three answers 'which tools do agents adopt, which workflows complete, and where do sessions stop?' That is the analytics layer, and it is what turns observability into product decisions for an MCP server."),
    ],
  },
  {
    slug: "how-to-debug-mcp-tool-errors",
    title: "How to debug MCP tool errors",
    tag: "Engineering",
    excerpt:
      "A practical workflow for finding and fixing the tool calls that fail, including the ones your monitoring calls a success.",
    date: "Aug 19, 2025",
    read: "6 min read",
    body: [
      p("Debugging MCP tool errors starts with a hard truth: many failures return a 200 OK. MCP puts tool errors in the response body, so your first job is to look at the payload, not the status code."),
      h("Step 1: separate transport from tool errors"),
      p("Count HTTP/transport failures separately from tool-level failures flagged with isError. A server that looks healthy at the transport layer can still be failing most of its tool calls."),
      h("Step 2: rank tools by failure"),
      p("Sort tools by call volume and success rate. A high-volume, low-success tool is your top priority, because it is breaking the most sessions."),
      h("Step 3: read the error text"),
      p("Open the failing calls and read the actual error. The pattern is usually a shape or type the model keeps getting wrong."),
      code(`// schema wants:  { "to": ["a@x.com"] }
// agents send:    { "to": "a@x.com" }  → validation error`),
      h("Step 4: make the tool forgiving"),
      p("Coerce common shapes, accept synonyms for enum values, and return errors that tell the agent exactly what to change. Most tool errors are fixed by tolerance, not by new features."),
    ],
  },
  {
    slug: "how-to-increase-mcp-tool-adoption",
    title: "How to increase adoption of your MCP tools",
    tag: "Product",
    excerpt:
      "If agents ignore a tool, the problem is usually discovery, not capability. A practical playbook to get your tools used.",
    date: "Aug 11, 2025",
    read: "6 min read",
    body: [
      p("A tool that never gets called is not necessarily a bad tool. More often it is a discovery problem: the model does not know when to reach for it. Increasing adoption is mostly about making tools easy for an agent to choose correctly."),
      h("Fix the description first"),
      p("Agents pick tools by reading names and descriptions. Lead with the verb and object, say when to use it and when not to, and name arguments the way a model would guess them."),
      h("Reduce competition between tools"),
      p("If two tools overlap, the model splits calls or picks wrong. Merge them or sharpen the boundary so each has a clear job."),
      h("Make the happy path forgiving"),
      ul([
        "Accept the argument shapes models actually send",
        "Provide sensible defaults for optional fields",
        "Return results the agent can use without a second call",
      ]),
      h("Measure and iterate"),
      p("Ship a change, then watch that tool's adoption and success rate. If calls rise and success holds, the wording was the blocker. Adoption is something you improve on a loop, not guess once."),
    ],
  },
  {
    slug: "mcp-server-security-redacting-data",
    title: "MCP server security: redacting sensitive data",
    tag: "Engineering",
    excerpt:
      "How to get analytics on your MCP server without letting sensitive arguments or results leave your process.",
    date: "Aug 1, 2025",
    read: "5 min read",
    body: [
      p("Tool calls often carry sensitive data: emails, tokens, query contents. You can still measure usage without shipping any of it, as long as redaction happens locally, before anything leaves your process."),
      h("Redact in-process"),
      p("Good MCP analytics strips sensitive fields on your server, not on a remote pipeline. You name the paths to remove, and the raw values never travel."),
      code(`withTrackMCP(server, {
  apiKey: process.env.TRACKMCP_KEY,
  redact: ["args.email", "args.api_key", "result.raw_response"],
});`),
      h("Prefer shape over content"),
      p("You rarely need the actual values to learn from usage. Knowing that a call happened, which tool, which client, and whether it succeeded is enough for adoption, workflows, and reliability."),
      h("Other safeguards"),
      ul([
        "Sample high-volume calls to reduce data footprint",
        "Fail open: capture must never block a tool call",
        "Scope API keys to a workspace and keep them server-side",
      ]),
      p("Done right, you get the full analytics picture while sensitive data stays inside your infrastructure."),
    ],
  },
  {
    slug: "track-which-agents-use-your-mcp-server",
    title: "How to track which AI agents use your MCP server",
    tag: "Guide",
    excerpt:
      "Claude, Cursor, ChatGPT, or a custom agent? Here's how to see your client mix and why it changes what you build.",
    date: "Jul 22, 2025",
    read: "5 min read",
    body: [
      p("Knowing which AI agents call your MCP server is one of the most useful things you can measure. Clients differ in how they discover tools, format arguments, and retry, so your client mix is a design input, not a vanity number."),
      h("How it's captured"),
      p("Each MCP connection identifies its client. Analytics at the protocol layer records that automatically, so you can break every metric down by client without extra work."),
      h("What to look for"),
      ul([
        "Distribution: which clients drive most usage",
        "Growth: which client is growing fastest",
        "Per-client success: a tool may fail in one client and not another",
      ]),
      h("Why it changes decisions"),
      p("If completion is high in one client and low in another, the fix is client-specific. If growth concentrates in a single client, test changes against it first. Client-level data turns a vague 'it works' into something you can act on."),
    ],
  },
  {
    slug: "mcp-server-metrics-checklist",
    title: "MCP server metrics: a monitoring checklist",
    tag: "Guide",
    excerpt:
      "A copy-and-keep checklist of the metrics worth tracking on an MCP server, grouped by adoption, outcomes, and reliability.",
    date: "Jul 14, 2025",
    read: "5 min read",
    body: [
      p("Here is a practical checklist of MCP server metrics worth tracking. Lead with adoption and outcomes; keep reliability close as support."),
      h("Adoption"),
      ul([
        "Active clients and client mix",
        "New vs returning connections",
        "Tool call volume and per-tool adoption",
        "Tools with zero calls (candidates to fix or cut)",
      ]),
      h("Outcomes"),
      ul([
        "Workflow completion rate",
        "Most common workflow and where sessions stop",
        "Returning usage within 7 and 30 days",
      ]),
      h("Reliability"),
      ul([
        "Tool-level error rate",
        "Silent failures (isError inside a 200 OK)",
        "p50 and p95 latency per tool",
        "Retries per successful call",
      ]),
      h("How to use it"),
      p("Review adoption and outcomes weekly to steer the product, and watch reliability for regressions. If a number moves the wrong way, it points you straight at what to fix."),
    ],
  },
  {
    slug: "how-to-measure-mcp-server-roi",
    title: "How to measure the ROI of your MCP server",
    tag: "Product",
    excerpt:
      "Tie your MCP server to outcomes leadership cares about: completed tasks, retained clients, and reduced failure cost.",
    date: "Jul 3, 2025",
    read: "6 min read",
    body: [
      p("To justify investment in an MCP server, connect it to outcomes, not activity. Call volume is easy to grow and easy to dismiss. Completed work, retained clients, and avoided failures are what make the case."),
      h("Value created"),
      ul([
        "Completed workflows tied to a real result (issue created, query answered)",
        "Returning clients that keep using the server",
        "Breadth: clients adopting more than one tool over time",
      ]),
      h("Cost avoided"),
      ul([
        "Silent failures caught before users churn",
        "Retries reduced, cutting latency and token cost",
        "Dead tools removed, cutting maintenance",
      ]),
      h("Frame it as a rate"),
      p("Completion rate is the cleanest ROI proxy: of the sessions that start, how many reach a useful result. Improving it moves every downstream number, and it cannot be gamed by busy failure."),
      h("Report the trend"),
      p("Leadership responds to direction. Show completion rate, returning usage, and error rate week over week, and the value of the server becomes self-evident."),
    ],
  },
  {
    slug: "answer-engine-optimization-for-mcp-tools",
    title: "Answer engine optimization for MCP tools",
    tag: "Guide",
    excerpt:
      "AEO isn't just for content. Making your tools easy for an AI to choose and call correctly is optimization for the answer engine.",
    date: "Jun 20, 2025",
    read: "6 min read",
    body: [
      p("Answer engine optimization (AEO) usually means making content easy for AI to cite. For an MCP server, there is a parallel discipline: making your tools easy for an AI to discover, choose, and call correctly. The 'answer engine' here is the agent, and your tools are what it reaches for."),
      h("The agent is your audience"),
      p("A model reads your tool names, descriptions, and schemas at the moment it decides what to do. If that text is vague, the tool is effectively invisible, no matter how good the implementation is."),
      h("Optimize for the moment of choice"),
      ul([
        "Clear names: verb + object, matching how a user phrases intent",
        "Descriptions that state when to use and when not to",
        "Schemas that accept the shapes models naturally produce",
        "No overlapping tools competing for the same intent",
      ]),
      h("Measure discoverability"),
      p("AEO for tools is measurable. Track adoption per tool and success rate after a description change. If the right tool starts getting called for the right intent, your optimization worked."),
      quote("For an MCP server, the best-optimized tool is the one the agent picks correctly without a retry."),
    ],
  },
  {
    slug: "ab-test-mcp-tool-descriptions",
    title: "How to A/B test your MCP tool descriptions",
    tag: "Product",
    excerpt:
      "Tool descriptions are UX for agents. Here's how to test wording changes and prove which version gets tools used correctly.",
    date: "Jun 9, 2025",
    read: "5 min read",
    body: [
      p("Because agents choose tools by reading descriptions, wording is one of your highest-leverage levers. And like any UX copy, it should be tested rather than guessed."),
      h("What to test"),
      ul([
        "The verb and object in the tool name",
        "When-to-use guidance in the description",
        "Argument names and the example shapes you show",
      ]),
      h("How to run it"),
      p("Change one description at a time so you can attribute the effect. Note the change and the date, then let real traffic run against it for a meaningful window."),
      h("What to measure"),
      ul([
        "Adoption: does call volume for the tool rise?",
        "Correctness: does success rate hold or improve?",
        "Retries: do give-up loops go down?",
      ]),
      h("Keep what wins"),
      p("If adoption rises and success holds, the new wording was better. Treat descriptions as a living surface you iterate on, and underused tools often come back to life."),
    ],
  },
  {
    slug: "mcp-vs-rest-api-analytics",
    title: "MCP vs REST API: how analytics differs",
    tag: "Guide",
    excerpt:
      "If you've measured a REST API, MCP will feel familiar but different. Here's what changes when your callers are AI agents.",
    date: "May 28, 2025",
    read: "6 min read",
    body: [
      p("If you have instrumented a REST API, MCP analytics will feel familiar: calls, latency, errors. But three things change when the callers are AI agents rather than human-written clients."),
      h("1. Errors live in the body"),
      p("A REST API signals failure with a status code. MCP often returns tool errors inside a successful response, so status-based monitoring misses them. You have to read the payload."),
      h("2. The caller reasons about your interface"),
      p("A REST client is coded once against your spec. An agent decides at runtime which tool to call based on your names and descriptions. That makes discoverability a first-class metric, not just correctness."),
      h("3. Sessions are the unit"),
      p("A REST endpoint is often measured per request. Agent value shows up across a session: a chain of calls toward a task. Completion rate, not request count, is the metric that matters."),
      h("What carries over"),
      p("Latency percentiles, error rates, and client breakdowns all still apply. The mindset shift is from 'did the request return 200?' to 'did the agent accomplish the task?'"),
    ],
  },
  {
    slug: "trackmcp-vs-building-mcp-analytics-in-house",
    title: "TrackMCP vs Building MCP Analytics In-House",
    tag: "Comparison",
    excerpt:
      "A practical way to decide whether to build MCP analytics into your platform or use a purpose-built observability layer.",
    date: "Sep 4, 2026",
    read: "8 min read",
    updated: "Sep 4, 2026",
    keywords: [
      "MCP analytics in-house",
      "MCP observability platform",
      "MCP server analytics tool",
      "TrackMCP vs building in-house",
    ],
    related: ["mcp-server-analytics-guide", "logs-vs-apm-vs-mcp-analytics", "mcp-observability-guide"],
    body: [
      p("The choice between TrackMCP and building MCP analytics in-house depends on what your team needs to own. A custom system can be the right fit when analytics is part of your core platform and you already maintain ingestion, identity, storage, dashboards, privacy controls, and alerting. TrackMCP is a better fit when you want protocol-aware visibility without turning those systems into another product to build and operate."),
      callout("The short answer", "Build in-house when you have a durable platform requirement and the engineering capacity to maintain it. Use TrackMCP when the immediate goal is to understand clients, tools, sessions, errors, latency, retries, and workflow outcomes on an existing MCP server."),
      h("What MCP analytics needs to capture"),
      p("MCP analytics is more than counting HTTP requests. A useful implementation connects client identity, discovery, tool calls, sessions, tool-level errors, latency, retries, and workflow outcomes. It should also distinguish a transport failure from an application error embedded inside a successful response."),
      ul([
        "A server-boundary integration that covers new tools without manual instrumentation.",
        "Structured event fields for client, method, tool, session, duration, result, and error state.",
        "A way to connect individual calls into sessions and multi-step workflows.",
        "Local redaction, workspace isolation, retention controls, and a documented failure mode.",
        "Queries and views that lead to an engineering or product decision.",
      ]),
      h("When building in-house makes sense"),
      ul([
        "Your organization already operates a shared telemetry platform and can add MCP semantics without creating a separate system.",
        "You need custom retention, residency, access, or data-processing controls that a hosted product cannot satisfy.",
        "MCP analytics is part of a broader internal platform used across many protocols and products.",
        "You have a clear owner for SDK maintenance, schema changes, ingestion reliability, dashboards, and support.",
        "The expected product value justifies maintaining another production data path.",
      ]),
      h("When a purpose-built layer is the better choice"),
      ul([
        "You need useful MCP-specific signals before you have time to build a telemetry product.",
        "Your current logs show requests but not tool adoption, session paths, retries, or completion.",
        "You want to instrument the existing server boundary rather than edit every handler.",
        "You need a short path from the first event to a working dashboard and a concrete investigation.",
        "Your team would rather spend engineering time improving tools and workflows than maintaining analytics infrastructure.",
      ]),
      h("The trade-offs to evaluate"),
      h("1. Time to first useful signal"),
      p("An in-house project usually begins with an event schema and expands into ingestion, storage, identity, dashboards, and operational controls. Estimate the time to answer one real question, such as which client experienced the highest tool error rate this week. Compare that with the time to install an SDK, make a call, and verify the same answer."),
      h("2. Ownership and maintenance"),
      p("MCP behavior changes at the protocol, SDK, transport, client, and tool layers. An in-house system needs an explicit owner for compatibility and migrations. A hosted layer still needs product verification, but it can reduce the amount of analytics infrastructure your team operates directly."),
      h("3. Data boundaries"),
      p("Decide what is allowed to leave the server process before choosing an implementation. Tool arguments and results can contain personal data, credentials, queries, and customer content. TrackMCP is designed around minimized metadata and local redaction; an in-house system gives you control, but you still need to implement and audit those controls."),
      h("4. Product context"),
      p("General-purpose logs and APM remain valuable for infrastructure diagnosis. They may not tell you whether an agent selected the right tool, retried after a schema error, or completed a business workflow. If those are the questions you need to answer, verify that your in-house design models MCP sessions and outcomes rather than only requests and spans."),
      h("A practical decision test"),
      ul([
        "Write down the three MCP questions your team needs answered this month.",
        "Define the minimum event fields and retention needed to answer them safely.",
        "Estimate the ongoing owner-hours for SDKs, ingestion, dashboards, incidents, and protocol changes.",
        "Instrument one representative server and verify one real workflow end to end.",
        "Choose the option that produces reliable evidence with the least long-term operational risk, not simply the option with the lowest initial code cost.",
      ]),
      h("What TrackMCP does and does not replace"),
      p("TrackMCP does not replace application logs, traces, infrastructure monitoring, or a company-wide data platform. It adds MCP-aware context to the stack: which clients connect, which tools are discovered and called, where sessions fail, and whether workflows reach a useful result. If your team already has a strong telemetry platform, the right answer may be to integrate MCP semantics into it. If that work is not prioritized, a purpose-built layer can provide the missing visibility sooner."),
      h("Frequently asked questions"),
      faq("Is building MCP analytics in-house always more flexible?", "It gives you more control over storage, retention, identity, and workflows, but flexibility also creates ownership. You must maintain the SDK, event model, ingestion path, dashboards, privacy controls, and compatibility as MCP deployments change."),
      faq("Can TrackMCP work alongside logs and APM?", "Yes. Logs and APM help diagnose application and infrastructure behavior. TrackMCP adds MCP-specific client, tool, session, retry, and workflow context so you can connect a technical failure to the agent behavior that followed."),
      faq("What should I compare before choosing a tool?", "Compare server-boundary coverage, event fields, session and workflow modeling, redaction, retention, workspace isolation, supported runtimes, time to first useful signal, and the ongoing work your team must own."),
      faq("What is the safest first step?", "Instrument one representative server with minimized metadata, verify one real workflow, and document exactly what data leaves the process. Use the result to validate both the technical signal and the privacy boundary before expanding."),
      links([
        { label: "MCP server analytics guide", href: "/blog/mcp-server-analytics-guide" },
        { label: "Logs vs APM vs MCP analytics", href: "/blog/logs-vs-apm-vs-mcp-analytics" },
        { label: "MCP observability guide", href: "/blog/mcp-observability-guide" },
        { label: "MCP server analytics quickstart", href: "/mcp-server-analytics/quickstart" },
      ]),
    ],
  },
];

export function getPost(slug: string) {
  return posts.find((post) => post.slug === slug);
}
